// @contract: C-296
// GENERATED by tools/typematrix/gen.py — the type x operation conformance
// matrix (cell: str_list x fn_pass). Regenerate rather than hand-edit; a cell
// that stops lowering moves to the wall inventory in the same PR.
fn roundtrip(v: List[String]) -> List[String] = v

fn main() -> Unit = {
  let v: List[String] = ["a", "bb", "ccc"]
  let w = roundtrip(v)
  println(list.join(w, "|"))
}
