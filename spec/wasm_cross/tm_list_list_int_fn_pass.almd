// @contract: C-296
// GENERATED by tools/typematrix/gen.py — the type x operation conformance
// matrix (cell: list_list_int x fn_pass). Regenerate rather than hand-edit; a cell
// that stops lowering moves to the wall inventory in the same PR.
fn roundtrip(v: List[List[Int]]) -> List[List[Int]] = v

fn main() -> Unit = {
  let v: List[List[Int]] = [
    [1, 2],
    [],
    [3]
  ]
  let w = roundtrip(v)
  println(list.join(list.map(w, (xs) => int.to_string(list.len(xs))), ","))
}
