// @contract: C-296
// GENERATED by tools/typematrix/gen.py — the type x operation conformance
// matrix (cell: list_variant x shadow_drop). Regenerate rather than hand-edit; a cell
// that stops lowering moves to the wall inventory in the same PR.
type V = | A(Int) | B(String)

fn main() -> Unit = {
  var v: List[V] = [A(1), B("t"), A(2)]
  v = [A(1)]
  println(list.join(list.map(v, (e) => match e {
    A(n) => int.to_string(n),
    B(s) => s,
  }), "/"))
}
