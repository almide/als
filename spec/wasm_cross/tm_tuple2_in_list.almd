// @contract: C-296
// GENERATED by tools/typematrix/gen.py — the type x operation conformance
// matrix (cell: tuple2 x in_list). Regenerate rather than hand-edit; a cell
// that stops lowering moves to the wall inventory in the same PR.
fn main() -> Unit = {
  let xs: List[(Int, String)] = [(4, "x"), (5, "y")]
  println(int.to_string(list.len(xs)))
  let h = list.get(xs, 0)
  println(match h {
    some(v) => match v {
      (n, s) => int.to_string(n) + s
    },
    none => "-",
  })
}
