// @contract: C-296
// GENERATED by tools/typematrix/gen.py — the type x operation conformance
// matrix (cell: list_list_int x interp). Regenerate rather than hand-edit; a cell
// that stops lowering moves to the wall inventory in the same PR.
fn main() -> Unit = {
  let v: List[List[Int]] = [
    [1, 2],
    [],
    [3]
  ]
  let msg = "got=${list.join(list.map(v, (xs) => int.to_string(list.len(xs))), ",")}"
  println(msg)
}
