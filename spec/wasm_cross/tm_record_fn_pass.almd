// @contract: C-296
// GENERATED by tools/typematrix/gen.py — the type x operation conformance
// matrix (cell: record x fn_pass). Regenerate rather than hand-edit; a cell
// that stops lowering moves to the wall inventory in the same PR.
type R = { x: Int, s: String }

fn roundtrip(v: R) -> R = v

fn main() -> Unit = {
  let v: R = R { x: 3, s: "r" }
  let w = roundtrip(v)
  println(int.to_string(w.x) + w.s)
}
