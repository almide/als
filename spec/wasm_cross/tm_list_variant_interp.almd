// @contract: C-296
// GENERATED by tools/typematrix/gen.py — the type x operation conformance
// matrix (cell: list_variant x interp). Regenerate rather than hand-edit; a cell
// that stops lowering moves to the wall inventory in the same PR.
type V = | A(Int) | B(String)

fn main() -> Unit = {
  let v: List[V] = [A(1), B("t"), A(2)]
  let msg = "got=${list.join(list.map(v, (e) => match e {
    A(n) => int.to_string(n),
    B(s) => s,
  }), "/")}"
  println(msg)
}
