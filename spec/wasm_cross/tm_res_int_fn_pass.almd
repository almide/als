// @contract: C-296
// GENERATED by tools/typematrix/gen.py — the type x operation conformance
// matrix (cell: res_int x fn_pass). Regenerate rather than hand-edit; a cell
// that stops lowering moves to the wall inventory in the same PR.
fn roundtrip(v: Result[Int, String]) -> Result[Int, String] = v

fn main() -> Unit = {
  let v: Result[Int, String] = ok(5)
  let w = roundtrip(v)
  println(match w {
    ok(n) => int.to_string(n),
    err(e) => e,
  })
}
