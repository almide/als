// @contract: C-296
// GENERATED by tools/typematrix/gen.py — the type x operation conformance
// matrix (cell: map_int x interp). Regenerate rather than hand-edit; a cell
// that stops lowering moves to the wall inventory in the same PR.
fn main() -> Unit = {
  let v: Map[String, Int] = map.from_list([("a", 1), ("b", 2)])
  let msg = "got=${int.to_string(map.get_or(v, "a", 0 - 1) * 10 + map.len(v))}"
  println(msg)
}
