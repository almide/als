// @contract: C-296
// GENERATED by tools/typematrix/gen.py — the type x operation conformance
// matrix (cell: list_tuple x fn_pass). Regenerate rather than hand-edit; a cell
// that stops lowering moves to the wall inventory in the same PR.
fn roundtrip(v: List[(Int, Int)]) -> List[(Int, Int)] = v

fn main() -> Unit = {
  let v: List[(Int, Int)] = [(1, 2), (3, 4)]
  let w = roundtrip(v)
  println(int.to_string(list.fold(w, 0, (a, p) => a + match p {
    (x, y) => x + y
  })))
}
