// @contract: C-296
// GENERATED by tools/typematrix/gen.py — the type x operation conformance
// matrix (cell: tuple2 x fn_pass). Regenerate rather than hand-edit; a cell
// that stops lowering moves to the wall inventory in the same PR.
fn roundtrip(v: (Int, String)) -> (Int, String) = v

fn main() -> Unit = {
  let v: (Int, String) = (4, "x")
  let w = roundtrip(v)
  println(match w {
    (n, s) => int.to_string(n) + s
  })
}
