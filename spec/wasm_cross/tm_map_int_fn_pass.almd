// @contract: C-296
// GENERATED by tools/typematrix/gen.py — the type x operation conformance
// matrix (cell: map_int x fn_pass). Regenerate rather than hand-edit; a cell
// that stops lowering moves to the wall inventory in the same PR.
fn roundtrip(v: Map[String, Int]) -> Map[String, Int] = v

fn main() -> Unit = {
  let v: Map[String, Int] = map.from_list([("a", 1), ("b", 2)])
  let w = roundtrip(v)
  println(int.to_string(map.get_or(w, "a", 0 - 1) * 10 + map.len(w)))
}
