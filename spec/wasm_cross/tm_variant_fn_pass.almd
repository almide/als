// @contract: C-296
// GENERATED by tools/typematrix/gen.py — the type x operation conformance
// matrix (cell: variant x fn_pass). Regenerate rather than hand-edit; a cell
// that stops lowering moves to the wall inventory in the same PR.
type V = | A(Int) | B(String)

fn roundtrip(v: V) -> V = v

fn main() -> Unit = {
  let v: V = A(9)
  let w = roundtrip(v)
  println(match w {
    A(n) => int.to_string(n),
    B(s) => s,
  })
}
