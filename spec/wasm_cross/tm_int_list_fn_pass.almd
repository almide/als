// @contract: C-296
// GENERATED by tools/typematrix/gen.py — the type x operation conformance
// matrix (cell: int_list x fn_pass). Regenerate rather than hand-edit; a cell
// that stops lowering moves to the wall inventory in the same PR.
fn roundtrip(v: List[Int]) -> List[Int] = v

fn main() -> Unit = {
  let v: List[Int] = [1, 2, 3]
  let w = roundtrip(v)
  println(list.join(list.map(w, (x) => int.to_string(x)), ","))
}
