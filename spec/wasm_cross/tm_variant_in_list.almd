// @contract: C-296
// GENERATED by tools/typematrix/gen.py — the type x operation conformance
// matrix (cell: variant x in_list). Regenerate rather than hand-edit; a cell
// that stops lowering moves to the wall inventory in the same PR.
type V = | A(Int) | B(String)

fn main() -> Unit = {
  let xs: List[V] = [A(9), B("z")]
  println(int.to_string(list.len(xs)))
  let h = list.get(xs, 0)
  println(match h {
    some(v) => match v {
      A(n) => int.to_string(n),
      B(s) => s,
    },
    none => "-",
  })
}
