// @contract: C-296
// GENERATED by tools/typematrix/gen.py — the type x operation conformance
// matrix (cell: record x in_list). Regenerate rather than hand-edit; a cell
// that stops lowering moves to the wall inventory in the same PR.
type R = { x: Int, s: String }

fn main() -> Unit = {
  let xs: List[R] = [
    R { x: 3, s: "r" },
    R { x: 4, s: "q" }
  ]
  println(int.to_string(list.len(xs)))
  let h = list.get(xs, 0)
  println(match h {
    some(v) => int.to_string(v.x) + v.s,
    none => "-",
  })
}
