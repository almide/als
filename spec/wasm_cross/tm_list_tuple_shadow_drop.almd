// @contract: C-296
// GENERATED by tools/typematrix/gen.py — the type x operation conformance
// matrix (cell: list_tuple x shadow_drop). Regenerate rather than hand-edit; a cell
// that stops lowering moves to the wall inventory in the same PR.
fn main() -> Unit = {
  var v: List[(Int, Int)] = [(1, 2), (3, 4)]
  v = [(1, 2)]
  println(int.to_string(list.fold(v, 0, (a, p) => a + match p {
    (x, y) => x + y
  })))
}
