// @contract: C-296
// GENERATED by tools/typematrix/gen.py — the type x operation conformance
// matrix (cell: int_list x shadow_drop). Regenerate rather than hand-edit; a cell
// that stops lowering moves to the wall inventory in the same PR.
fn main() -> Unit = {
  var v: List[Int] = [1, 2, 3]
  v = [1, 2]
  println(list.join(list.map(v, (x) => int.to_string(x)), ","))
}
