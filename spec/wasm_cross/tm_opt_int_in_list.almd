// @contract: C-296
// GENERATED by tools/typematrix/gen.py — the type x operation conformance
// matrix (cell: opt_int x in_list). Regenerate rather than hand-edit; a cell
// that stops lowering moves to the wall inventory in the same PR.
fn main() -> Unit = {
  let xs: List[Int?] = [some(7), none]
  println(int.to_string(list.len(xs)))
  let h = list.get(xs, 0)
  println(match h {
    some(v) => int.to_string(v ?? (0 - 1)),
    none => "-",
  })
}
