// @contract: C-296
// GENERATED by tools/typematrix/gen.py — the type x operation conformance
// matrix (cell: int_list x in_list). Regenerate rather than hand-edit; a cell
// that stops lowering moves to the wall inventory in the same PR.
fn main() -> Unit = {
  let xs: List[List[Int]] = [
    [1, 2, 3],
    [1, 2]
  ]
  println(int.to_string(list.len(xs)))
  let h = list.get(xs, 0)
  println(match h {
    some(v) => list.join(list.map(v, (x) => int.to_string(x)), ","),
    none => "-",
  })
}
