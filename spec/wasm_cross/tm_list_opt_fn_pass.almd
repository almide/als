// @contract: C-296
// GENERATED by tools/typematrix/gen.py — the type x operation conformance
// matrix (cell: list_opt x fn_pass). Regenerate rather than hand-edit; a cell
// that stops lowering moves to the wall inventory in the same PR.
fn roundtrip(v: List[Int?]) -> List[Int?] = v

fn main() -> Unit = {
  let v: List[Int?] = [some(1), none, some(3)]
  let w = roundtrip(v)
  println(int.to_string(list.fold(w, 0, (a, o) => a + (o ?? 0))))
}
