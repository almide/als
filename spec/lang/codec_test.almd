module codec_test

type Person: Codec = { name: String, age: Int }

test "codec encode-decode roundtrip" {
  let p = Person { name: "Alice", age: 30 }
  let v = p.encode()
  let p2 = Person.decode(v)
  assert_eq(p2.name, "Alice")
  assert_eq(p2.age, 30)
}
