// GENERATED by scripts/check-semantics-manifest.sh from
// docs/stdlib/semantics-manifest.toml — DO NOT EDIT (the gate
// regenerates and diffs). Each case executes a manifest probe; a
// failure here means the implementation drifted from the DECLARED
// semantic dimension (#419 class).
test "float.to_int unit=value" {
  assert_eq("${float.to_int(0.0 - 3.9)}", "-3")
}

test "float.from_int unit=value" {
  assert_eq("${float.from_int(2)}", "2")
}

test "float.to_fixed unit=count" {
  assert_eq("${float.to_fixed(3.14159, 2)}", "3.14")
}

test "float.to_bits unit=bit-pattern" {
  assert_eq("${float.to_bits(1.0)}", "4607182418800017408")
}
