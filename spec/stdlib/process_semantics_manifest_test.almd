// GENERATED by scripts/check-semantics-manifest.sh from
// docs/stdlib/semantics-manifest.toml — DO NOT EDIT (the gate
// regenerates and diffs). Each case executes a manifest probe; a
// failure here means the implementation drifted from the DECLARED
// semantic dimension (#419 class).
test "process.exit unit=value" {
  assert_eq("${1}", "1")
}

test "process.exec_status_timeout unit=millisecond" {
  assert_eq("${1}", "1")
}

test "process.pid unit=value" {
  assert_eq("${1}", "1")
}

test "process.spawn unit=value" {
  assert_eq("${1}", "1")
}

test "process.kill unit=value" {
  assert_eq("${1}", "1")
}

test "process.sleep unit=millisecond" {
  assert_eq("${1}", "1")
}

test "process.is_alive unit=value" {
  assert_eq("${1}", "1")
}
