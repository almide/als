// GENERATED by scripts/check-semantics-manifest.sh from
// docs/stdlib/semantics-manifest.toml — DO NOT EDIT (the gate
// regenerates and diffs). Each case executes a manifest probe; a
// failure here means the implementation drifted from the DECLARED
// semantic dimension (#419 class).
test "list.sum unit=value" {
  assert_eq("${list.sum([1, 2, 3])}", "6")
}

test "list.product unit=value" {
  assert_eq("${list.product([2, 3, 4])}", "24")
}

test "list.range unit=value" {
  assert_eq("${list.range(2, 5)}", "[2, 3, 4]")
}

test "list.binary_search unit=element-index" {
  assert_eq("${list.binary_search([10, 20, 30], 20) ?? (0 - 1)}", "1")
}

test "list.bundled_probe unit=value" {
  assert_eq("${list.bundled_probe(7)}", "8")
}
