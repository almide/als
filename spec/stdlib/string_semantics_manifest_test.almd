// GENERATED by scripts/check-semantics-manifest.sh from
// docs/stdlib/semantics-manifest.toml — DO NOT EDIT (the gate
// regenerates and diffs). Each case executes a manifest probe on
// multibyte input; a failure here means the implementation drifted
// from the DECLARED semantic dimension (#419 class, C-065).

test "string.len unit=codepoint" {
  assert_eq("${string.len("café7")}", "5")
}

test "string.length unit=codepoint" {
  assert_eq("${string.length("café7")}", "5")
}

test "string.slice unit=codepoint" {
  assert_eq("${string.slice("café7", 0, 4)}", "café")
}

test "string.pad_start unit=codepoint" {
  assert_eq("${string.pad_start("é", 3, "x")}", "xxé")
}

test "string.pad_end unit=codepoint" {
  assert_eq("${string.pad_end("é", 3, "x")}", "éxx")
}

test "string.to_bytes unit=byte" {
  assert_eq("${list.len(string.to_bytes("é"))}", "2")
}

test "string.from_bytes unit=byte" {
  assert_eq("${string.from_bytes([195, 169])}", "é")
}

test "string.get unit=codepoint" {
  assert_eq("${string.get("café7", 3) ?? "?"}", "é")
}

test "string.index_of unit=codepoint" {
  assert_eq("${string.index_of("café7", "7") ?? -1}", "4")
}

test "string.last_index_of unit=codepoint" {
  assert_eq("${string.last_index_of("café7", "7") ?? -1}", "4")
}

test "string.repeat unit=count" {
  assert_eq("${string.repeat("é", 2)}", "éé")
}

test "string.codepoint unit=scalar" {
  assert_eq("${string.codepoint("é") ?? -1}", "233")
}

test "string.from_codepoint unit=scalar" {
  assert_eq("${string.from_codepoint(233)}", "é")
}

test "string.count unit=count" {
  assert_eq("${string.count("ééa", "é")}", "2")
}

test "string.take unit=codepoint" {
  assert_eq("${string.take("café7", 4)}", "café")
}

test "string.take_end unit=codepoint" {
  assert_eq("${string.take_end("café7", 2)}", "é7")
}

test "string.drop unit=codepoint" {
  assert_eq("${string.drop("café7", 3)}", "é7")
}

test "string.drop_end unit=codepoint" {
  assert_eq("${string.drop_end("café7", 2)}", "caf")
}

test "string.run_length_encode unit=count" {
  assert_eq("${string.run_length_encode("ééa")}", "[(\"é\", 2), (\"a\", 1)]")
}
