// GENERATED by scripts/check-semantics-manifest.sh from
// docs/stdlib/semantics-manifest.toml — DO NOT EDIT (the gate
// regenerates and diffs). Each case executes a manifest probe; a
// failure here means the implementation drifted from the DECLARED
// semantic dimension (#419 class).
test "net.tcp_connect unit=port" {
  assert_eq("${1}", "1")
}

test "net.tcp_listen unit=port" {
  assert_eq("${1}", "1")
}

test "net.tcp_accept unit=handle" {
  assert_eq("${1}", "1")
}

test "net.tcp_read unit=byte-count" {
  assert_eq("${1}", "1")
}

test "net.tcp_read_exact unit=byte-count" {
  assert_eq("${1}", "1")
}

test "net.tcp_available unit=byte-count" {
  assert_eq("${1}", "1")
}

test "net.tcp_write unit=handle" {
  assert_eq("${1}", "1")
}

test "net.tcp_read_timeout unit=millisecond" {
  assert_eq("${1}", "1")
}

test "net.tcp_set_timeout unit=millisecond" {
  assert_eq("${1}", "1")
}

test "net.tcp_is_open unit=handle" {
  assert_eq("${1}", "1")
}

test "net.tcp_close unit=handle" {
  assert_eq("${1}", "1")
}

test "net.tcp_close_listener unit=handle" {
  assert_eq("${1}", "1")
}
