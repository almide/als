// GENERATED by scripts/check-semantics-manifest.sh from
// docs/stdlib/semantics-manifest.toml — DO NOT EDIT (the gate
// regenerates and diffs). Each case executes a manifest probe; a
// failure here means the implementation drifted from the DECLARED
// semantic dimension (#419 class).
import env

test "env.unix_timestamp unit=second" {
  let ts = env.unix_timestamp()
  assert_eq("${ts > 1700000000 and ts < 100000000000}", "true")
}

test "env.millis unit=millisecond" {
  let ms = env.millis()
  assert_eq("${ms > 1700000000000}", "true")
}

test "env.sleep_ms unit=millisecond" {
  env.sleep_ms(0)
  assert_eq("${true}", "true")
}
