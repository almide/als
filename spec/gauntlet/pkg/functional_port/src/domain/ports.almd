import self.domain.order

protocol OrderRepository {
  effect fn find(self, id: String) -> order.Order?
  effect fn save(self, o: order.Order) -> Self
  effect fn all(self) -> List[order.Order]
}

protocol Clock {
  fn now_iso(self) -> String
}
