# =============================================================# ALMIDE BEHAVIOR CONTRACT LEDGER — the language's cross-target law book
# =============================================================#
# Each [[contract]] is a NORMATIVE, observable promise the compiler keeps on
# BOTH backends (native Rust and wasm32). "Observable" = stdout, stderr, exit
# code. Native is the oracle; native == wasm is a hard invariant, NOT a "target
# difference". The ONE exemption is the platform-reporting pair `env.os` /
# `env.temp_dir`, which must report the host they run on and are bounded by
# C-189; a divergence anywhere else is a compiler bug. This ledger is the
# human-readable + machine-checkable index of every such promise, with a
# TRACEABLE link to the executable evidence that certifies it.
#
# WHY THIS EXISTS
# ---------------
# The cross-target equivalence GATE (tests/wasm_runtime_test.rs::wasm_cross_target_spec)
# and the wasm-runtime ORACLE-PAIRING registry (crates/almide-codegen/rt-oracle-registry.toml)
# already enforce equivalence at the test + routine level. This ledger adds the
# missing layer: a stable, named CONTRACT each fixture certifies, so that
#   (a) every cross-target fixture is traceable to the promise it locks, and
#   (b) every promise is traceable to executable proof — no claimed behaviour
#       rests on prose alone.
# The gate (scripts/check-contracts.sh) makes that bidirectional link mandatory.
#
# SCHEMA  (flat TOML, one scalar per line — kept awk-parseable like the registry)
# ------
#   [[contract]]
#   id        (string, REQUIRED)  ^C-[0-9]{3}$, unique. THE PRIMARY KEY.
#   spec      (string, REQUIRED)  the ALS normative section this contract
#                                 certifies ("ALS-xx"); must resolve to a real
#                                 `## ALS-xx` heading, and every heading must be
#                                 cited by >=1 contract (#565, both directions
#                                 gated by check-contracts.sh).
#   title     (string, REQUIRED)  one line.
#   statement (string, REQUIRED)  the normative behaviour, <= 3 sentences. When a
#                                 table / longer prose is needed, the statement is
#                                 a 1-line summary and `doc` points at the prose.
#   since     (string, REQUIRED)  the release in which the BEHAVIOUR became
#                                 normative — the first version a user can rely
#                                 on it. When a contract is written RETROACTIVELY
#                                 over behaviour that already shipped (C-099..C-115
#                                 documented fixtures that had accumulated since
#                                 0.27.6), `since` is the behaviour's release, NOT
#                                 the release the ledger entry landed in. Those two
#                                 coincide whenever the contract ships alongside the
#                                 change it certifies, which is the normal case.
#   status    (enum,   REQUIRED)  "active" | "flagged-for-revision".
#   doc       (string, OPTIONAL)  basename of a docs/contracts/*.md prose file.
#   evidence  (array,  REQUIRED)  >= 1 inline-table entries; each:
#                 path  (string, REQUIRED)  repo-root-relative; must exist on disk.
#                 class (string, REQUIRED)  one of scripts/lib/contract-classes.txt.
#                 name  (string, OPTIONAL)  REQUIRED for *.rs / *.lean / *.toml or
#                                           when class in {fuzz, lean, exhaustive};
#                                           the `fn`/theorem/routine the gate greps.
#                 n     (int,    OPTIONAL)  REQUIRED iff class="fuzz"; the sample count.
#
# THE FLOOR RULE: an `active` contract MUST carry >= 1 evidence of rank >= fixture
# (see scripts/lib/contract-classes.txt). doc-only / by-construction ALONE fail an
# active contract. status="flagged-for-revision" is exempt (may rest on doc-only):
# it records a KNOWN, currently-unfixable divergence (the ratchet may only shrink).
#
# CHANGE DISCIPLINE: changing any observable behaviour REQUIRES updating the
# contract statement AND its evidence in the SAME PR. A new behaviour = a new
# C-NNN + >= 1 fixture. Removing a divergence = flip status to active and drop the
# flag in the same PR. The flagged count is a ratchet; it may only go DOWN.
#
# REVERSE LINK: every spec/wasm_cross/*.almd declares the contract(s) it certifies
# on a `// @contract: C-NNN[, C-MMM]` header line. The link is BIDIRECTIONAL and
# the gate enforces symmetry.


# ── INTEGER DIV/MOD TOTALITY (PR #378) ───────────────────────────────
[[contract]]
id        = "C-001"
spec      = "ALS-T6"
title     = "Integer division/modulo by zero is total — it aborts, never traps"
statement = "Integer `/` and `%` by a zero divisor MUST abort with stderr `Error: division by zero` and exit code 1 on BOTH targets — never a native panic (exit 101) nor a wasm `unreachable` trap (exit 134). The `%`-by-zero message is identical to `/` (both `division by zero`). This holds for a runtime-shielded divisor AND a const-foldable literal `10 / 0`, which must still COMPILE: `/` and `%` route through the non-const-evaluable `almide_div!` / `__div_trap`, so rustc never const-evaluates the divide and the abort happens at runtime."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/int_div_by_zero.almd", class = "fixture" },
  { path = "spec/wasm_cross/int_mod_by_zero.almd", class = "fixture" },
  { path = "spec/wasm_cross/int_div_by_zero_literal.almd", class = "fixture" },
]

[[contract]]
id        = "C-002"
spec      = "ALS-T6"
title     = "Signed MIN / -1 overflow aborts, at the TRUE per-width MIN"
statement = "Signed integer `MIN / -1` and `MIN % -1` MUST abort with stderr `Error: integer overflow` and exit 1 on BOTH targets, using the true per-width MIN. Int8 `-128 / -1` must abort even though it lowers to i32 in wasm where `i32.div_s(-128, -1) = 128` does not trap; and wasm `i64.rem_s` DEFINES `MIN % -1 = 0` (no trap), so an explicit MIN/-1 guard before the op realigns wasm with native's `checked_div`/`checked_rem` returning None."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/int_div_overflow.almd", class = "fixture" },
  { path = "spec/wasm_cross/int_mod_overflow.almd", class = "fixture" },
  { path = "spec/wasm_cross/int8_div_overflow.almd", class = "fixture" },
]

[[contract]]
id        = "C-003"
spec      = "ALS-T12"
title     = "Non-aborting integer div/mod stay byte-identical"
statement = "Ordinary (non-aborting) integer `/` and `%` — including negative operands and the now-total guarded codepath — MUST produce byte-identical results on both targets. The checked-div rewrite must not perturb the happy path; shielded divisors exercise the same guarded path landing on the non-aborting branch."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/int_div_mod_ok.almd", class = "fixture" },
]

# ── FAN DETERMINISM (PR #377, #369) ──────────────────────────────────
[[contract]]
id        = "C-004"
spec      = "ALS-R3"
title     = "fan.any / fan.map / fan.settle are deterministic by list order"
statement = "`fan.any` tries thunks in list order and returns the first Ok. `fan.map` runs element fns sequentially in list order; `fan.settle` returns results in list order. All are byte-identical native == wasm. (any skips failures to find an Ok.) `fan.race` was REMOVED in 0.42.0: under the deterministic model it was exactly `thunks[0]()` — `desugar_fan.rs` replaced the call with thunk[0]'s body and never evaluated the rest — so its name promised a wall-clock race the language does not have. It is an E027 check-time tombstone, the same treatment `fan.timeout` got in 0.29.0. EXCEPTION: side-effect INTERLEAVING inside `fan { }` block arms and `fan.settle` thunks is wall-clock on native (both run on real threads) and sequential on wasm — they pin their RESULT (tuple / list) order only. The block was an undercount here until #915's audit: it spawns per-arm scoped threads on native, the same interleaving class as settle. The three MAPPER heads (`fan.map/any/settle(xs, f)`) accept an EFFECT callback in both spellings — an inline lambda calling an effect fn, and a bare effect-fn value — and the result is byte-identical on both legs (#1350; the slot's `is_effect: false` was a declaration bug, never a permission, since unification is effect-agnostic by #1055). A callback reaching a stdlib capability with no wasm implementation (`http.get`) walls on the wasm leg, but that is the capability's own gap — it walls identically with no `fan` in the program — not a mapper-purity rule."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/fan_deterministic.almd", class = "fixture" },
  { path = "spec/wasm_cross/fan_map_inline_lambda.almd", class = "fixture" },
  { path = "spec/wasm_cross/fan_pure_thunks.almd", class = "fixture" },
  { path = "spec/wasm_cross/fan_var_thunk_list.almd", class = "fixture" },
  { path = "spec/wasm_cross/fan_effect_mapper.almd", class = "fixture" },
]

[[contract]]
id        = "C-005"
spec      = "ALS-R3"
title     = "fan error propagation surfaces as the unified main-error abort"
statement = "The first element fn returning Err in `fan.map` (named or inline lambda) and the all-fail case of `fan.any` (defined `Err(\"fan.any: all candidates failed\")`) all propagate as `Error: <msg>` + exit 1 on BOTH targets via the effect-main termination — NOT a native panic (101) nor a wasm trap (134)."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/fan_map_err.almd", class = "fixture" },
  { path = "spec/wasm_cross/fan_map_inline_err.almd", class = "fixture" },
  { path = "spec/wasm_cross/fan_any_allfail.almd", class = "fixture" },
  { path = "spec/wasm_cross/option_none_unwrap_term.almd", class = "fixture" },
]

[[contract]]
id        = "C-006"
spec      = "ALS-R3"
title     = "fan.timeout does not exist — wall-clock deadlines live at the host boundary"
statement = "`fan.timeout` was removed in 0.29.0: a wall-clock timeout has no portable cross-target meaning (wasm has no clock, scheduler, or threads), and it was the sole stdlib surface whose result was not a function of the program + its inputs — the deadline firing depended on machine load, even native-to-native. Referencing it is a check-time tombstone error (E027) with a host-boundary migration hint, identical on both targets since type check precedes the target split. This retired the ledger's LAST flagged-for-revision entry: the ratchet stands at zero, so the equivalence claim carries no clause for divergences awaiting a fix. It is not exception-free in the absolute sense — the platform-reporting fns `env.os` / `env.temp_dir` differ by design and are exempt under C-189, which bounds that carve-out to exactly that surface (env.os and the env/fs temp_dir pair)."
since     = "0.29.0"
status    = "active"
doc       = "C-006-fan-timeout-removed.md"
evidence  = [
  { path = "tests/diagnostics/e027-fan-timeout-removed/broken.almd", class = "fixture" },
  { path = "tests/diagnostic_harness_test.rs", name = "broken_files_produce_expected_diagnostics", class = "fixture" },
]

# ── EAGER ABORTABLE TOP-LETS (Cluster-H) ─────────────────────────────
[[contract]]
id        = "C-007"
spec      = "ALS-T7"
title     = "Abortable top-level lets evaluate eagerly at startup"
statement = "A top-level `let` whose initializer can abort MUST be evaluated EAGERLY at program startup, in declaration order, on both targets — even if the binding is never used and even if it would be used only mid-main. wasm evaluates every top-let in `_start`; native forces abortable lazies at main entry. So an abortable top-let aborts before any of main's stdout is emitted."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/top_let_div_eager.almd", class = "fixture" },
  { path = "spec/wasm_cross/top_let_div_used.almd", class = "fixture" },
]

# ── COMPOUND / RECORD / RECURSIVE REPR INTERPOLATION (PR #379) ───────
[[contract]]
id        = "C-008"
spec      = "ALS-R2"
title     = "Compound interpolation renders the Almide-literal repr (containers)"
statement = "`\"${compound}\"` renders a value back to its Almide-literal form, recursively, byte-identically on both targets, for List, Map (`[\"a\": 1]`), Set, Tuple, Option (some), Result (ok), and arbitrary nesting. Backed by `AlmideRepr` (native) / the recursive rt repr emitters (wasm), gated by `ty_needs_repr`."
since     = "0.24.0"
status    = "active"
doc       = "C-008-009-010-repr.md"
evidence  = [
  { path = "spec/wasm_cross/compound_repr_interp.almd", class = "fixture" },
  { path = "spec/wasm_cross/repr_parity_pin.almd", class = "fixture" },
]

[[contract]]
id        = "C-009"
spec      = "ALS-R2"
title     = "Record / variant / anonymous-record interpolation repr (field sorting)"
statement = "`\"${value}\"` renders records (`P { x: 1 }`), variants (tuple/record/nullary payloads), and anonymous records to their Almide construction-literal form — the same format `deriving Repr` produces — byte-identically on both targets. Anonymous records have NO type name and render their fields in SORTED name order on BOTH targets (native synthesized `AlmdRec_*` stores fields sorted; the wasm walk sorts to match)."
since     = "0.24.0"
status    = "active"
doc       = "C-008-009-010-repr.md"
evidence  = [
  { path = "spec/wasm_cross/compound_repr_records_interp.almd", class = "fixture" },
  { path = "spec/wasm_cross/compound_repr_recursive_interp.almd", class = "fixture" },
]

[[contract]]
id        = "C-010"
spec      = "ALS-R2"
title     = "Recursive / generic ADT interpolation repr keyed by instantiation"
statement = "Self-recursive ADTs/records, mutually-recursive records, and generic records/variants render their repr via per-type `__repr_*` wasm functions that recurse by CALL on the finite value, not by infinite compile-time type expansion. Generic types key the repr fn by INSTANTIATION (`__repr_Tree_Int` vs `__repr_Tree_String`), substituting site type-args through recursive references, so concrete payloads repr identically to native trait dispatch. The instantiation is the ONLY key: each helper — the element repr and its `List`/`Option` walkers alike — is emitted EXACTLY ONCE per instantiation regardless of how many interpolation SITES reference it, so interpolating one value twice changes the output text and nothing else. (Once per site would emit the helper twice, and since a generated helper is a wasm export, that is a duplicate export name — an invalid module, rejected at parse before any Almide semantics run.)"
since     = "0.24.0"
status    = "active"
doc       = "C-008-009-010-repr.md"
evidence  = [
  { path = "spec/wasm_cross/compound_repr_recursive_interp.almd", class = "fixture" },
  { path = "spec/wasm_cross/recursive_generic_repr_interp.almd", class = "fixture" },
]

[[contract]]
id        = "C-011"
spec      = "ALS-R2"
title     = "Bare-float interpolation Display drops .0; float.to_string keeps it"
statement = "An integer-valued float in interpolation `\"${f}\"` renders via the Display form, which DROPS the `.0` suffix, whereas `float.to_string` keeps the `.0` for whole values. Both forms are byte-identical across targets."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/compound_repr_records_interp.almd", class = "fixture" },
  { path = "spec/wasm_cross/float_concrete.almd", class = "fixture" },
  { path = "spec/wasm_cross/num_edge_to_string.almd", class = "fixture" },
  { path = "spec/wasm_cross/int_float_ops.almd", class = "fixture" },
  { path = "spec/wasm_cross/float_interp_forms.almd", class = "fixture" },
]

[[contract]]
id        = "C-012"
spec      = "ALS-R4"
title     = "Const-folded non-finite floats emit named constants"
statement = "A float expression that const-folds to a non-finite value (inf/NaN) MUST emit the associated Rust constant (`f64::INFINITY` / `f64::NEG_INFINITY` / `f64::NAN`), not the bare identifier (`inff64`/`NaNf64` were invalid Rust = an E0425 ICE-class bug). Runtime-COMPUTED inf/NaN already printed identically and must keep doing so. Both targets byte-identical."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/const_fold_nonfinite_float.almd", class = "fixture" },
]

# ── DETERMINISM BELT: ORDERED CONTAINERS (PR #354) ───────────────────
[[contract]]
id        = "C-013"
spec      = "ALS-C1"
title     = "Map is a compact-ordered-dict: iteration is insertion order"
statement = "Map iteration order is INSERTION order on both targets (compact-ordered-dict: dense entries + hash index). Every order-exposing op (keys/values/fold/filter/iteration) reveals insertion order byte-identically; a regression to hash-order on either target breaks the gate."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/map_insertion_order.almd", class = "fixture" },
  { path = "spec/wasm_cross/map_filter.almd", class = "fixture" },
  { path = "spec/wasm_cross/map_fold.almd", class = "fixture" },
  { path = "spec/wasm_cross/map_index_order.almd", class = "fixture" },
]

[[contract]]
id        = "C-014"
spec      = "ALS-C2"
title     = "Set is insertion-ordered and deterministic"
statement = "Set iteration is INSERTION order on both targets and deterministic. Native Set was std HashSet (hash-bucket, per-process randomized) and is now an insertion-ordered Vec-backed AlmideSet, so every order-exposing op is native == wasm and deterministic. (Set[Float] equality on wasm, which used i32_eq, is fixed too.)"
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/set_insertion_order.almd", class = "fixture" },
  { path = "spec/wasm_cross/set_index_order.almd", class = "fixture" },
]

# ── DEEP STRUCTURAL EQUALITY ─────────────────────────────────────────
[[contract]]
id        = "C-015"
spec      = "ALS-C3"
title     = "Structural deep equality for compound elements and heap values"
statement = "`==` / `!=` and every containment / dedup / key-lookup op compare COMPOUND elements (tuples, nested lists, records incl. String fields) by type-directed DEEP equality on both targets — native via Rust `PartialEq`, wasm via per-type deep-eq functions (not pointer identity). Two distinct heap objects with identical contents compare equal; Map/Set equality is size + key/value containment, so containers built in a different insertion order are still equal."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/compound_eq.almd", class = "fixture" },
  { path = "spec/wasm_cross/deep_eq_heap.almd", class = "fixture" },
  { path = "spec/wasm_cross/map_set_eq.almd", class = "fixture" },
]

# ── UNICODE STRING SURFACE ───────────────────────────────────────────
[[contract]]
id        = "C-016"
spec      = "ALS-S1"
title     = "UTF-8 codepoint-aware string ops are byte-identical"
statement = "Codepoint-aware string ops (chars/reverse/slice/take/drop/first/last/get/codepoint/from_codepoint/pad) operate on Unicode scalars and are byte-identical native == wasm on ASCII and multibyte input (Latin-1 accent, CJK, 4-byte emoji, 3-byte currency). An EMPTY pad string falls back to a single SPACE (`pad.chars().next().unwrap_or(' ')` — the v0 arm returned a width-0 unit, silently making pad_start/pad_end a no-op; differential-fuzz seed 1784416403485348966). Native (Rust char-based) is the oracle; the wasm runtime must byte-match it for every scalar."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/string_codepoint.almd", class = "fixture" },
  { path = "spec/wasm_cross/string_ops.almd", class = "fixture" },
]

[[contract]]
id        = "C-017"
spec      = "ALS-S2"
title     = "Empty-pattern count / last_index_of follow native codepoint/byte semantics"
statement = "`count` with the empty pattern returns the CODEPOINT count + 1 (`s.matches(\"\").count() == s.chars().count() + 1`); `last_index_of` with the empty pattern returns the CODEPOINT count (the byte offset `s.rfind(\"\") == Some(s.len())` converted to the codepoint index, per the C-065 one-unit rule). Both are byte-identical native == wasm, including multibyte subjects where codepoint-count and byte-length differ; wasm no longer short-circuits the empty pattern to 0."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/string_empty_pattern.almd", class = "fixture" },
]

[[contract]]
id        = "C-018"
spec      = "ALS-S3"
title     = "Unicode string predicates match Rust char methods over the full domain"
statement = "`is_alpha` / `is_alphanumeric` / `is_upper` / `is_lower` derive from Rust's full-Unicode `char` methods on native; the wasm target walks oracle-derived range tables (rt_unicode_tables.rs) per codepoint, byte-identical to native for every interesting Unicode category. The tables are exhaustively probed against the std `char` methods over the complete scalar domain at emit time."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/string_predicates.almd", class = "fixture" },
]

[[contract]]
id        = "C-019"
spec      = "ALS-S3"
title     = "rt_string_extra ops (replace_first, strip_*, predicates, cmp) match native"
statement = "`replace_first`, `strip_prefix`/`strip_suffix`, `is_digit` (ASCII-only), `is_whitespace` (full-Unicode White_Space, empty-string vacuously true), and String `<` (BYTE-lexicographic with a length tiebreak, NOT codepoint order) are byte-identical native == wasm. Empty-pattern/affix edge cases follow native exactly: `replace_first` splices at position 0, `strip_prefix`/`strip_suffix` of an empty affix return the whole string."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/string_ops_drain.almd", class = "fixture" },
  { path = "spec/wasm_cross/string_rle.almd", class = "fixture" },
]

[[contract]]
id        = "C-020"
spec      = "ALS-T5"
title     = "Unicode case transforms (to_upper/to_lower/capitalize) are full-Unicode"
statement = "`to_upper` / `to_lower` / `capitalize` produce the SAME bytes as native (Rust `str::to_uppercase` / `str::to_lowercase` / first-char `char::to_uppercase`) for ALL scalars — accents, Greek, Cyrillic, the length-CHANGING maps (ß -> SS), and the context-sensitive Greek sigma (Final_Sigma: word-final ς vs medial σ). The wasm runtime embeds oracle-derived Unicode case tables and resolves Final_Sigma at runtime; the tables are exhaustively probed against the std `char` methods at emit time."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/string_case_unicode.almd", class = "fixture" },
]

[[contract]]
id        = "C-021"
spec      = "ALS-T1"
title     = "Whitespace trim / is_whitespace use the full Unicode White_Space property"
statement = "`trim` / `trim_start` / `trim_end` / `is_whitespace` and the leading/trailing trim inside `int.parse` / `float.parse` match native (Rust `str::trim*` / `char::is_whitespace`, the 25-codepoint Unicode White_Space property) byte-identically. The old wasm checked only {0x09,0x0A,0x0D,0x20}; `is_whitespace(\"\")` is now correctly true, routed through the oracle-derived `__is_unicode_ws`."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/string_whitespace.almd", class = "fixture" },
]

[[contract]]
id        = "C-022"
spec      = "ALS-S4"
title     = "string.from_bytes is UTF-8-lossy decode (inverse of to_bytes)"
statement = "`string.from_bytes` is a UTF-8 decode (`String::from_utf8_lossy`, U+FFFD per maximal invalid subpart), the exact inverse of `to_bytes`, byte-identical native == wasm — not the old Latin-1 `b as u8 as char` mapping that corrupted every non-ASCII string. `string.len` is BYTE length, so the round-trip is asserted on byte counts and content. The wasm runtime's byte-class derivation is differentially fuzzed (300k randomized byte sequences + boundary cases) against `from_utf8_lossy` at emit time."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/string_from_bytes.almd", class = "fixture" },
]

# ── FLOAT FORMATTING / PARSING (Dragon4 / dec2flt) ──────────────────
[[contract]]
id        = "C-023"
spec      = "ALS-T13"
title     = "float.to_string is shortest round-tripping decimal (Dragon4)"
statement = "`float.to_string` produces the SAME bytes as native Rust f64 Display — the shortest round-tripping decimal in fixed notation, with the `.0` integer suffix — byte-identical native == wasm. The wasm runtime implements Dragon4 with exact big-integer arithmetic (rt_dragon.rs); there is no fixed-precision drift (native 0.30000000000000004 vs the old wasm 0.3) and no `i64_trunc` trap for |x| >= 2^63."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/float_shortest_roundtrip.almd", class = "fixture" },
  { path = "spec/wasm_cross/num_edge_to_string.almd", class = "fixture" },
]

[[contract]]
id        = "C-024"
spec      = "ALS-T2"
title     = "float.parse is correctly-rounded round-to-nearest-even (Clinger AlgorithmM)"
statement = "`float.parse` produces the SAME f64 as native (`s.trim().parse::<f64>()`), i.e. correctly-rounded round-to-nearest-even, with byte-identical Err strings. The wasm runtime uses Clinger AlgorithmM with exact big-integer arithmetic (rt_dec2flt.rs, reusing the Dragon4 bignum); the old f64-accumulator parser (`acc*10+digit`) was not correctly rounded (\"0.3\" -> 0.30000000000000004, subnormals collapsing to 0)."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/float_parse.almd", class = "fixture" },
]

[[contract]]
id        = "C-025"
spec      = "ALS-T9"
title     = "float.to_fixed is round-half-to-even on the exact binary value"
statement = "`float.to_fixed(x, n)` reproduces native `format!(\"{:.N}\", x)` EXACTLY on both targets: the decimal is the f64's exact binary value m*2^e rounded to N places, round-HALF-to-EVEN on the exact value. The wasm implementation rides the Dragon4 big-integer machinery (rt_dragon.rs), so there is no 10^N i64 overflow (the old impl trapped for N >= 19) and no multiply-then-round error; carry propagation, signed-zero, and inf/NaN all match. A differential fuzz suite certifies the continuous input space."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/float_to_fixed.almd", class = "fixture" },
  { path = "tests/float_to_fixed_cross_target_test.rs", name = "to_fixed_cross_target_random_fuzz", class = "fuzz", n = 1000 },
]

# ── VENDORED-LIBM MATH ───────────────────────────────────────────────
[[contract]]
id        = "C-026"
spec      = "ALS-T10"
title     = "Vendored-libm trig / exp / log / pow are byte-identical cross-target"
statement = "`math.sin/cos/tan`, `math.exp/log/log2/log10`, and float `**` / `math.fpow` route through the SAME vendored musl-libm reference on both targets (native runtime/rs/src/libm.rs, wasm rt_libm.rs), so every result is byte-identical — printed via the (itself byte-identical) Dragon4 `float.to_string`. Special cases match libm: `log(0) = -inf` AND `log(-0.0) = -inf` for all three log variants (the v1 self-host dropped the sign-bit half of musl's `hx < 0x00100000 || hx>>31` gate, sending -0.0 to the NaN reject — differential-fuzz seed 1784416403485348966), `log(neg) = NaN`, `exp(-745) = 5e-324`, `fpow(-2,0.5) = NaN`, `fpow(-2,3) = -8`, `fpow(2,inf)` does not trap. Differential fuzz suites certify the continuous input space."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/trig_libm.almd", class = "fixture" },
  { path = "spec/wasm_cross/math_explog.almd", class = "fixture" },
  { path = "spec/wasm_cross/math_pow.almd", class = "fixture" },
  { path = "tests/libm_trig_cross_target_test.rs", name = "trig_cross_target_random_fuzz", class = "fuzz", n = 4000 },
  { path = "tests/libm_explog_pow_cross_target_test.rs", name = "explog_cross_target_random_fuzz", class = "fuzz", n = 1200 },
  { path = "tests/libm_explog_pow_cross_target_test.rs", name = "pow_cross_target_random_fuzz", class = "fuzz", n = 1200 },
]

# ── ENCODINGS / RADIX PARSING ────────────────────────────────────────
[[contract]]
id        = "C-027"
spec      = "ALS-T11"
title     = "base64 encode/decode (standard + URL-safe) is byte-identical incl. errors"
statement = "base64 encode/decode for BOTH alphabets (standard + URL-safe) produces byte-identical output native == wasm — RFC 4648 canonical padded form — including the decode error strings `invalid base64 character` and `invalid base64 length: <orig_len>`. Native runtime/rs/src/base64.rs is the oracle; the wasm runtime rt_encoding.rs must match it exactly."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/encoding_base64.almd", class = "fixture" },
]

[[contract]]
id        = "C-028"
spec      = "ALS-T8"
title     = "int.from_hex mirrors i64::from_str_radix incl. native quirks"
statement = "`int.from_hex` mirrors native `i64::from_str_radix(s.trim().trim_start_matches(\"0x\"), 16)` exactly, INCLUDING the quirks: `0X10` -> Err (the `0x` strip is lowercase-only), `0x0xff` -> Ok(255) (`trim_start_matches` strips `0x` repeatedly), `f_f` -> Err (no underscore skipping), `0x-ff` -> -255 (sign accepted after strip). The four std ParseIntError strings reproduce exactly, byte-identical native == wasm."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/int_from_hex.almd", class = "fixture" },
]

[[contract]]
id        = "C-029"
spec      = "ALS-T8"
title     = "int.parse error modes byte-match native ParseIntError"
statement = "`int.parse` byte-matches native `s.trim().parse::<i64>().map_err(|e| e.to_string())` for every failure mode — empty-after-trim, lone sign, embedded non-digit, and i64 overflow in both directions (incl. the i64::MIN edge with no positive counterpart). The Err payload STRING is part of the compared output, so the four std messages must reproduce exactly."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/int_parse_overflow.almd", class = "fixture" },
]

[[contract]]
id        = "C-030"
spec      = "ALS-T11"
title     = "hex.encode / hex.decode are byte-identical incl. positional error detail"
statement = "`hex.encode` / `hex.decode` produce byte-identical output native == wasm, including the positional decode error strings `hex string has odd length: N` and `invalid hex char at N` (hi nibble at `2*pair`, lo at `2*pair+1`). Native runtime/rs/src/hex.rs is the oracle; the wasm rt_encoding.rs runtime must mirror it exactly, including the error detail the old wasm strings dropped. `hex.encode`/`hex.encode_upper` also hold alongside `int.to_hex` in the SAME program — the two self-host sources' private helpers no longer collide (C-212)."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/encoding_base64.almd", class = "fixture" },
  { path = "spec/wasm_cross/hex_two_module_link.almd", class = "fixture" },
]

# ── JSON PATH OPS ────────────────────────────────────────────────────
[[contract]]
id        = "C-031"
spec      = "ALS-D1"
title     = "json get/set/remove_path edge cases match the infallible native oracle"
statement = "`json.get_path` / `set_path` / `remove_path` reproduce native runtime/rs/src/json.rs byte-identically across all edge cases: negative-Index normalization (`len + i`), Index over an object/scalar/null root as an infallible no-op, OOB Index no-op, and Field-over-non-object autovivification (replace with a fresh single-key object, seed descents with `Object(vec![])`). The wasm rt_value.rs path walkers had 8 divergences this locks down."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/json_path_edges.almd", class = "fixture" },
  { path = "spec/wasm_cross/json_value.almd", class = "fixture" },
]

# ── REGEX ENGINE ─────────────────────────────────────────────────────
[[contract]]
id        = "C-032"
spec      = "ALS-D4"
title     = "Regex engine is byte-identical to the native engine over a fuzzed grammar"
statement = "The regex engine produces byte-identical results native == wasm for alternation, escape atoms, class escapes, captures (incl. optional-miss), multibyte `.`/class on CJK, anchors, quantifiers (incl. {n}/{n,}/{n,m} braces; malformed braces stay literal), replace, split edges, full_match, find_all, empty alternation arms, and end-of-string empty-match replace. Native runtime/rs/src/regex.rs is the oracle; a deterministic differential fuzz over the regex grammar certifies the input space beyond the headline corpus."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/regex_engine.almd", class = "fixture" },
  { path = "spec/wasm_cross/regex_fuzz_batch.almd", class = "fixture" },
  { path = "tests/regex_fuzz_test.rs", name = "regex_differential_fuzz", class = "fuzz", n = 220 },
]

# ── KNOWN DIVERGENCE: COPY-ON-WRITE FOR ALIASED MUTABLES ────────────
[[contract]]
id        = "C-033"
spec      = "ALS-C5"
title     = "Value semantics for aliased mutables (copy-on-write)"
statement = "`var b = a; a[i] = v` leaves `b` unchanged on both targets: heap-typed vars that are copy-aliased AND mutated in place get a copy-on-write at every mutation site (AliasCowPass may-alias analysis -> needs_cow; wasm clones via __cow_check before IndexAssign/FieldAssign/ListSwap/Reverse/Rotate/CopySlice and in-place stdlib mutators; native counts mutation targets as uses so aliased binds clone-at-bind). The full alias-shape truth table (A-M: chains, branch arms, record fields, destructures, map/string in-place) behaves identically on both targets."
since     = "0.24.0"
status    = "active"
doc       = "C-033-cow-truth-table.md"
evidence  = [
  { path = "spec/wasm_cross/alias_cow.almd", class = "fixture" },
  { path = "spec/lang/alias_cow_test.almd", class = "fixture" },
  { path = "spec/wasm_cross/module_var_alias_cow.almd", class = "fixture" },
]

# ── LIST BOUNDS GRACEFULNESS ─────────────────────────────────────────
[[contract]]
id        = "C-034"
spec      = "ALS-C4"
title     = "Out-of-range list ops clamp / no-op gracefully (no OOB heap access)"
statement = "Out-of-range `list.insert` clamps the index to the end (`min(i, len)`), `list.remove_at` / `list.swap` with an index >= len are no-ops, `list.slice` clamps past-the-end indices (end -> len, start >= effective_end -> []), and `list.with_capacity(n)` starts empty without header corruption for any n — all byte-identical native == wasm with no OOB heap read/write. `with_capacity` is TOTAL on both targets: capacity is an unobservable hint, so a huge request neither aborts native nor corrupts the wasm header. Native clamps its EAGER reservation to a named byte ceiling (`MAX_WITH_CAPACITY_PREALLOC_BYTES` = 64 MiB, runtime/rs/src/list.rs), so `Vec::with_capacity(i32::MAX)` over a 24-byte element (~51.5 GB, machine-dependent) cannot abort; the v1 wasm self-host (stdlib/list_make.almd) reserves NOTHING — `list_with_capacity` returns a fresh EMPTY list and ignores `cap` — which is byte-equivalent precisely because the reservation is unobservable (there is no self-host `push` to observe it through). `push` grows normally on both legs. Native (runtime/rs/src/list.rs) is the oracle for each. On the v1 leg `list.slice` routes by ELEMENT repr like take/drop: String elements ride the deep-copying `list.slice_str` twin (same clamp edges), other heap elements wall — the Int twin's raw i64 element copy aliased String handles un-owned, a scope-end double free after correct output (differential-fuzz seed 20260718 index 680)."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/list_insert_clamp.almd", class = "fixture" },
  { path = "spec/wasm_cross/list_remove_at_oob.almd", class = "fixture" },
  { path = "spec/wasm_cross/list_swap_oob.almd", class = "fixture" },
  { path = "spec/wasm_cross/list_slice_oob.almd", class = "fixture" },
  { path = "spec/wasm_cross/list_sort_short.almd", class = "fixture" },
  { path = "spec/wasm_cross/list_with_capacity.almd", class = "fixture" },
  { path = "spec/wasm_cross/negative_offset_and_width_totality.almd", class = "fixture" },
  { path = "spec/wasm_cross/repeat_size_ceiling.almd", class = "fixture" },
]

[[contract]]
id        = "C-035"
spec      = "ALS-R1"
title     = "Effect-main errors terminate uniformly: Error: <msg> + exit 1"
statement = "An effect `fn main` that yields Err, and any uncaught abort surfaced through the main-error path, terminate with stderr `Error: <msg>` + exit 1 on BOTH targets — native via the Display `fn main` wrapper, wasm via `__main_runner` (proc_exit + stderr) — never a native panic (101) nor a wasm `unreachable` trap (134). This is the unified termination the div/mod and fan abort contracts rely on."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/int_div_by_zero.almd", class = "fixture" },
  { path = "spec/wasm_cross/match_result.almd", class = "fixture" },
  { path = "spec/wasm_cross/option_result.almd", class = "fixture" },
  { path = "spec/wasm_cross/early_return_control_flow.almd", class = "fixture" },
]

# ── RECORDS / VARIANTS / SIZED INTEGERS ──────────────────────────────
[[contract]]
id        = "C-036"
spec      = "ALS-C7"
title     = "Records, variants, and pattern matching are byte-identical"
statement = "Record construction/field-access, variant construction (tuple/record/nullary payloads), and `match` over them — including matches whose arms construct a Result with `err(..)`, and a LITERAL nested inside a container constructor (`some(\"x\")`, `some(5)`, `ok(0)`, `err(404)`) — produce byte-identical results native == wasm. The inner-literal equality is emitted on BOTH targets: wasm structurally checks the tag then ANDs in a value compare (previously `some(\"x\")` matched ANY `some(_)` because only the non-null tag was checked); native hoists a payload-nested STRING literal into a `Bind` + `==` guard (`Ok(__s) if __s == \"good\"`) — previously `ok(\"good\")` emitted `Ok(\"good\")` against a `Result<String,String>` value and FAILED TO BUILD (rustc E0308 &str vs String), so these programs could not run cross-target at all. The hoist is the single mechanism for a string literal in any payload position at any depth (`ok(\"x\")`, `err(\"x\")`, `Word(\"x\")`, `KV(\"k\", n)`, `Pair(1, \"one\")`, `ok(some(\"x\")))`, `(\"a\", 1)`, and literal+guard combos); a literal arm refines, later binding arms stay reachable, byte-identical to the wasm tag→value→guard order. Int/Bool/Float payload literals remain bare Rust patterns. A match typed `Never` no longer appends a wasm `unreachable` epilogue (trap exit 134) while native runs fine."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/records_variants.almd", class = "fixture" },
  { path = "spec/wasm_cross/match_result.almd", class = "fixture" },
  { path = "spec/wasm_cross/closures_and_variants.almd", class = "fixture" },
  { path = "spec/wasm_cross/playground_default.almd", class = "fixture" },
  { path = "spec/wasm_cross/match_container_literal.almd", class = "fixture" },
  { path = "spec/wasm_cross/match_payload_string_literal.almd", class = "fixture" },
]

[[contract]]
id        = "C-037"
spec      = "ALS-D5"
title     = "bytes.read_f16_le decodes IEEE-754 half floats identically"
statement = "`bytes.read_f16_le` (IEEE-754 half -> f64) is byte-identical native == wasm, including the exp-all-ones path rendering ±inf / NaN (zero and nonzero mantissa) rather than the finite ±f32::MAX — over ±inf, NaN, ±0, the smallest subnormal, a few normals, and the largest finite half. Native runtime/rs/src/bytes.rs::f16_bits_to_f64 is the oracle for the wasm expander."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/bytes_f16.almd", class = "fixture" },
]

[[contract]]
id        = "C-038"
spec      = "ALS-C8"
title     = "Sized-integer literals narrow to the declared field width"
statement = "A bare literal assigned to a sized-integer field/element (Int8/Int16/Int32) narrows to the declared width at the construction site, so native rustc accepts it (no E0308 from `5i64` in an Int8 field) and wasm writes the correct byte WIDTH (so the next field is not read as garbage). This holds for plain records, nested records, record-variant and tuple-variant payloads, and tuple/list/nested literal positions; field access reads back the exact stored value, byte-identical native == wasm."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/sized_int_record_fields.almd", class = "fixture" },
  { path = "spec/wasm_cross/sized_numeric_literals.almd", class = "fixture" },
]

[[contract]]
id        = "C-039"
spec      = "ALS-C6"
title     = "Type-changing map.map / set.map yield a collection of the new type"
statement = "Applying a closure whose return type differs from the collection's value/element type yields a collection of the NEW type (`Map[K,V]` with `(V)->B` => `Map[K,B]`; `Set[A]` with `(A)->B` => `Set[B]`), byte-identical native == wasm. The wasm emitter no longer keeps the SOURCE width — every width transition (i64<->i32 pointer, f64, tuple) uses the correct call_indirect signature and load/store width."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/map_set_typechange.almd", class = "fixture" },
  { path = "spec/wasm_cross/map_set_ops.almd", class = "fixture" },
]

# ── HOST-DETERMINISM (emit-time) ─────────────────────────────────────
[[contract]]
id        = "C-040"
spec      = "ALS-I2"
title     = "Codegen emit is host-architecture deterministic"
statement = "The compiler emits byte-identical output whether it runs natively (x86-64/aarch64) or as wasm32 (the browser playground), on the 1st compile or the Nth in a process. Anonymous-record naming (content hash, not a walk-order counter), egg-fused lambda param names (VarId-derived), monomorphization append order, and lifted-closure iteration order are all host-independent — enforced by HashMap->BTreeMap emit ordering and the check-host-determinism gate, with cross-arch byte fixtures."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "scripts/check-host-determinism.sh", class = "by-construction" },
  { path = "spec/wasm_cross/anon_records_and_fusion.almd", class = "fixture" },
  { path = "spec/wasm_cross/generics_mono.almd", class = "fixture" },
  { path = "spec/wasm_cross/closures_hof.almd", class = "fixture" },
]

# ── HEAP / RC / PERCEUS ──────────────────────────────────────────────
[[contract]]
id        = "C-041"
spec      = "ALS-I1"
title     = "Heap / RC primitives honour the Lean-certified Perceus discipline"
statement = "alloc/realloc-growth, refcount inc/dec, string alloc, and the arena checkpoint primitives (heap_save/heap_restore) produce byte-identical observable results native == wasm under heavy churn — no observable leak effect, double-free, or realloc clobber. Since 0.27.0 the wasm rc bodies are REAL by default (C-066): rc_dec frees to a reuse list, double-frees trap on a rc==0 sentinel, rc_inc of a freed block traps (resurrection), and heap_restore / the per-iteration loop region reset also clear the free list so allocator state never straddles a rolled-back frontier. Every heap block satisfies the free-list's SIZE INVARIANT `block_bytes == LIST_HEADER + cap*ELEM_SIZE` — `$alloc` keeps no size record and recomputes a freed block's size from its own `cap` field, so a block whose allocated size disagrees with its cap is handed back for a larger request and the reuse overruns into a live neighbour (#892/#903: the WASI host-floor String constructors sized `LIST_HEADER + len` with `cap := len`). The IR-level Inc/Dec balance is certified by the `0 sorry` Lean Perceus belt; these fixtures are the executable counterpart. The opaque checkpoint value (0 native vs a real pointer wasm) is never printed."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/rc_alloc_stress.almd", class = "fixture" },
  { path = "spec/wasm_cross/host_floor_string_alloc.almd", class = "fixture" },
  { path = "spec/wasm_cross/memory_stress.almd", class = "fixture" },
  { path = "spec/wasm_cross/heap_arena.almd", class = "fixture" },
  { path = "spec/wasm_cross/hof_closure_string_tail.almd", class = "fixture" },
  { path = "crates/almide-perceus-belt/AlmidePerceusBelt/Heap.lean", name = "perceus_fixes_heap", class = "lean" },
  { path = "crates/almide-perceus-belt/AlmidePerceusBelt/ClosureRc.lean", name = "closureScope_decs", class = "lean" },
]

[[contract]]
id        = "C-066"
spec      = "ALS-I1"
title     = "WASM heap is reclaimed by default (true Perceus)"
statement = "Since 0.27.0 the wasm target frees by default: construct-and-drop churn reuses freed blocks (free-list) and loop iterations whose heap values do not escape reclaim wholesale (region reset incl. the free list), so steady-state churn runs in O(1) memory instead of the pre-0.27 bump-allocate-and-leak model. Observable values stay byte-identical native == wasm under churn. `ALMIDE_WASM_FREES=0` is the opt-out back to the leak model (env-conditional emission is declared behavior under the host-determinism gates). Known bounded exception, tracked in docs/roadmap/active/wasm-frees-ownership-discipline.md: TCO self-call loops leak per iteration until the Stage C redesign lands. (The second exception this contract used to carry — the v0 emitter's stored-field over-count, one leaked reference per construct-from-temp — died with that emitter: the v1 MIR path decides ownership once under verify_ownership and the class did not re-derive, RSS flat at 200k/2M/10M iterations, #911; spec/churn/construct_from_temp_churn.almd pins the shape.)"
since     = "0.27.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/rc_reclaim_churn.almd", class = "fixture" },
  { path = "spec/wasm_cross/assign_alias_rc.almd", class = "fixture" },
  { path = "spec/wasm_cross/container_alias_return.almd", class = "fixture" },
  { path = "spec/wasm_cross/loop_outer_inplace_mutate_rc.almd", class = "fixture" },
  { path = "spec/churn/string_accumulator_churn.almd", class = "by-construction" },
  { path = "spec/churn/construct_from_temp_churn.almd", class = "by-construction" },
  { path = "spec/churn/record_churn.almd", class = "by-construction" },
  { path = "spec/churn/tco_loop_churn.almd", class = "by-construction" },
]

# ── FILESYSTEM (WASI PREOPEN RESOLUTION) ─────────────────────────────
[[contract]]
id        = "C-042"
spec      = "ALS-R6"
title     = "fs preopen-dir scan + path resolution is observable-equivalent"
statement = "The wasm fs runtime DISCOVERS the WASI preopen-dir table (`fd_prestat_get` + `fd_prestat_dir_name`, scanned from fd 3 upward on the first path op and cached for the process) and resolves a path to the LONGEST-PREFIX preopen fd + relative remainder, matching only at a '/' component boundary; the `/` preopen claims any absolute path, a `.` preopen claims a path that stayed relative, and an unmatched path falls back to fd 3 with one leading '/' stripped. Native std::fs has no preopen concept, and the two are observable-equivalent: a write + `./`-normalized read-back of the same path resolves to the same host file under the `/` preopen, CWD-independently (the `./` folding is the HOST's, on both targets). Until #1394 the dirfd was a hard-coded `3` at all 13 path-call sites — indistinguishable from this rule under one `/` preopen, and a silently wrong host file under more than one."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/fs_preopen_resolve.almd", class = "fixture" },
]

# ── RECURSIVE-ENUM HEAP INDIRECTION / NAME COLLISION ────────────────
[[contract]]
id        = "C-043"
spec      = "ALS-M3"
title     = "A user type named Box coexists with recursive-enum heap indirection"
statement = "A recursive enum needs heap indirection on the Rust target, emitted as `std::boxed::Box<Tree>`; a user `type Box = ...` no longer collides with it (the recursive-enum pass fully-qualifies the std indirection everywhere), so native compiles (no E0072/E0107/E0599) and the program is byte-identical native == wasm. wasm has no `Box` and was already unaffected."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/user_box_recursive_enum.almd", class = "fixture" },
]

# ── RESULT / OPTION MATCH ────────────────────────────────────────────
[[contract]]
id        = "C-044"
spec      = "ALS-M1"
title     = "Result/Option construction and matching are byte-identical"
statement = "Construction and `match` over Result (`ok`/`err`) and Option (`some`/`none`), including `??` fallback, `?` Result->Option, and matches whose arms build a Result, produce byte-identical results native == wasm. A Result-constructing match is not mis-typed `Never` (which trapped wasm exit 134 while native ran)."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/match_result.almd", class = "fixture" },
  { path = "spec/wasm_cross/option_result.almd", class = "fixture" },
  { path = "spec/wasm_cross/control_flow.almd", class = "fixture" },
]

# ── LIST[STRING] PARAM CODEGEN ───────────────────────────────────────
[[contract]]
id        = "C-045"
spec      = "ALS-M12"
title     = "A List[String] param works across join / len / index / iteration"
statement = "A `List[String]` function param renders as `&[String]` on Rust and can be consumed by ANY combination of `string.join`, `list.len`, indexing, and iteration on both targets, byte-identically. The join runtime accepts `&[String]` (matching the list-join runtime), so passing the `&[String]` param no longer trips E0308 (`&[String]` vs `&Vec<String>`); wasm was unaffected."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/list_string_param_join_len.almd", class = "fixture" },
  { path = "spec/wasm_cross/list_comprehensive.almd", class = "fixture" },
  { path = "spec/wasm_cross/list_sort_short.almd", class = "fixture" },
]

# ── RECORD SPREAD-UPDATE / CROSS-MODULE MONOMORPHIZATION ─────────────
[[contract]]
id        = "C-046"
spec      = "ALS-M2"
title     = "Record spread-update and cross-module monomorphization are byte-identical"
statement = "Record spread-update (`{ ...rec, field: v }`) and the monomorphizer's lifted-function generation across module / diamond-dependency boundaries (the spread-heavy record + pipe-chain + shared-import pattern that broke ceangal-anime) produce byte-identical results native == wasm. The spread copies all unmentioned fields and overrides the named ones; shared imports through a diamond no longer generate broken lifted functions on either target."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/cross_module_spread.almd", class = "fixture" },
]

# ── NUMERIC TOTALITY: pow / rotate (Cluster-2) ───────────────────────
[[contract]]
id        = "C-047"
spec      = "ALS-T6"
title     = "math.pow negative exponent and rotate non-positive width are total — they abort, never trap/wrap"
statement = "`math.pow(base, exp)` with a NEGATIVE exponent aborts with stderr `Error: negative exponent` + exit 1 on BOTH targets (never the old native `exp as u32` u32-wrap garbage nor the wasm `return 1`); `int.rotate_left`/`rotate_right` with a non-positive `bits` width abort with `Error: rotate width must be positive` + exit 1 (never a native rem-by-zero panic exit 101 nor a wasm `i64.rem_s` trap exit 134). Non-negative pow stays byte-identical including overflow, which wraps under exponentiation-by-squaring (`2^64=0`, `0^0=1`, exponents `>= 2^32` define the full i64 count and never hang)."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/int_pow_negative_exponent.almd", class = "fixture" },
  { path = "spec/wasm_cross/int_pow_overflow_wraps.almd", class = "fixture" },
  { path = "spec/wasm_cross/int_rotate_nonpositive_width.almd", class = "fixture" },
]

# ── NUMERIC WRAP/ROTATE WIDTH MASK (Cluster-2) ───────────────────────
[[contract]]
id        = "C-048"
spec      = "ALS-T14"
title     = "int.wrap_* / int.rotate_* saturate the mask to u64::MAX for bits >= 64"
statement = "`int.wrap_add`/`wrap_mul`/`rotate_left`/`rotate_right` model an N-bit register: for `bits >= 64` the low-bit mask is `u64::MAX`, matching native `if bits >= 64 { u64::MAX } else { (1u64 << bits) - 1 }`. The wasm emit computed `(1 << bits) - 1` directly, but `i64.shl` masks the shift amount mod 64 so `1 << 64` wrapped to 1 and the mask collapsed to 0 — every `bits >= 64` result silently diverged. Now byte-identical native == wasm via a `bits >= 64 ? -1 : (1<<bits)-1` select."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/int_wrap_rotate_width.almd", class = "fixture" },
]

# ── FLOAT IEEE SIGN / NaN-IGNORING MIN-MAX (Cluster-2) ───────────────
[[contract]]
id        = "C-049"
spec      = "ALS-T15"
title     = "float.sign is f64::signum; float/math min/max ignore NaN"
statement = "`float.sign` == Rust `f64::signum` — `copysign(1.0, x)` for every non-NaN x (so `sign(0.0)=1`, `sign(-0.0)=-1`, `sign(±inf)=±1`) and NaN for NaN — NOT the old three-way sign (0 for ±0.0/NaN). `float.max`/`float.min`/`math.fmax`/`math.fmin` IGNORE a NaN operand and return the other, and on ±0 ties ALL FOUR return the FIRST operand (`max(0,-0)=0`, `max(-0,0)=-0`). Both targets implement the same explicit NaN/tie decision tree — never the NaN-propagating raw `f64.max`/`f64.min` wasm instructions, and never the llvm.minnum/maxnum intrinsics, whose unspecified ±0-tie order let x86 `maxsd` return the SECOND operand when inlined. All byte-identical native == wasm == interp."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/float_sign_minmax_ieee.almd", class = "fixture" },
]

# ── STRING CODEPOINT GRANULARITY: split("") / rle (Cluster-2) ────────
[[contract]]
id        = "C-050"
spec      = "ALS-S5"
title     = "string.split(\"\") and string.run_length_encode are codepoint-granular"
statement = "`string.split(s, \"\")` splits per CODEPOINT with a leading and trailing empty string — native `s.split(\"\")` yields `[\"\", c0, c1, …, \"\"]` (and `[\"\", \"\"]` for the empty string). `string.run_length_encode` groups runs by CODEPOINT (native `s.chars()`), the run string being the whole codepoint. The wasm emit used to return `[s]` for an empty delimiter (no split) and group rle by BYTE (emitting invalid single-byte strings for multibyte input). Now byte-identical native == wasm, including multibyte."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/string_split_rle_codepoint.almd", class = "fixture" },
]

# ── TRANSCENDENTAL: log_gamma vendored-libm bit-exactness (Cluster-2) ─
[[contract]]
id        = "C-051"
spec      = "ALS-T10"
title     = "math.log_gamma is bit-identical (both targets use the vendored musl-libm log)"
statement = "`math.log_gamma` is bit-identical native == wasm: the same Lanczos polynomial, `t = x + 7.5`, and pinned `0.5·ln(2π)` constant, with BOTH `ln` evaluations routed through the vendored musl-libm `log` rather than the platform `f64::ln`. Native previously called the platform `ln`, which drifts ~1 ULP per OS from the wasm vendored port (the StrictMath/fdlibm cross-target-determinism decision shared with sin/cos/exp/log)."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/math_log_gamma.almd", class = "fixture" },
]

# ── EMPTY-COLLECTION FOLD: ANNOTATE THE COLLECTION (folds into C-058) ─
[[contract]]
id        = "C-052"
spec      = "ALS-C10"
title     = "A fold over an empty collection requires the collection to carry an element type (no codegen defaulting)"
statement = "A `list`/`map`/`set` `.fold` over an EMPTY collection whose element/key/value types the closure never uses still requires the EMPTY collection to carry an element type. This is the SAME undecidable-empty-collection class as C-058: an unannotated empty `[]` / `map.new()` / `set.new()` here is a frontend compile error (E018) on BOTH targets, not a slot codegen defaults — the former `Int` defaulting (`default_unconstrained_fold_params`) was a layer violation and has been removed. Once the empty collection is ANNOTATED, its element type pins the closure params the normal way and the fold builds + prints byte-identically native == wasm even though the closure is never invoked. Non-empty folds that DO use the params still infer normally."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/empty_collection_fold.almd", class = "fixture" },
]

# ── LIST TOTAL-ORDER OPS (min / max / sort / sort_by / unique_by) ─────
[[contract]]
id        = "C-053"
spec      = "ALS-C9"
title     = "list.min/max/sort/sort_by/unique_by are type-directed and total, native == wasm"
statement = "`list.min`/`list.max` (extremum), `list.sort`/`list.sort_by` (ascending order), and `list.unique_by` (first-of-each-key dedup) are byte-identical native == wasm for EVERY totally-ordered element type — Int, Bool (false<true), String, Tuple (lexicographic), nested List (lexicographic with shorter-first tiebreak), Option (none<some), and variants (tag order) — and for any Eq key type the `unique_by`/`sort_by` callback returns. The wasm side routes all ordering through one type-directed total-order emitter (`emit_ord_cmp3`, the ordering twin of `emit_eq_typed`) instead of a hard-coded i64 element load; an unresolved element type is an emit-time ICE, never a silent wrong comparison or unbounded display loop."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/list_total_order.almd", class = "fixture" },
]

# ── LIST/STRING Int-COUNT & INDEX TRUNCATION (Cluster-B) ─────────────
[[contract]]
id        = "C-054"
spec      = "ALS-T16"
title     = "List/string Int counts and indices are i64-clamped before narrowing — no truncation, no OOB"
statement = "Every list/string op that takes an `Int` (i64) COUNT or INDEX clamps it on the FULL i64 value BEFORE the wasm `i32_wrap_i64` narrowing, so a value at/over the 2^32 boundary can never wrap to a small in-range value, and a NEGATIVE value takes native's unsigned `n as usize` path — never the wrong lo-clamp-to-0 result (the C-034 `with_capacity` rule generalized via named `emit_clamp_count_to_i32` (UNSIGNED `min_u`) / `emit_clamp_count_signed_i32` (SIGNED `max(0).min`) / `emit_clamp_index_to_len_i32` / `emit_checked_index_i32` helpers). For UNSIGNED COUNTS — `list.take`/`drop`/`take_end`/`drop_end`/`chunk`/`windows`, `list.slice` start/end, `string.take`/`drop`/`take_end`/`drop_end`, `string.pad_start`/`pad_end` widths — the count is `min(n as usize, hi)`: a NEGATIVE count is enormous as `usize` and saturates to `hi`, so `take(-1)` returns the WHOLE collection, `drop(-1)` returns empty, `chunk(-1)` groups into ONE chunk (was a div-by-0 TRAP), `windows(-1)` is empty (was a wrong non-empty length), and `string.take(s, 2^32)` returns the whole string (was empty); a count `>= 2^32` likewise saturates instead of wrapping to a small in-range count. The size-bounded `chunk`/`windows`/`pad` use an `i32::MAX` sentinel so a genuinely huge count BOTH-FAILS (wasm `memory.grow` / native abort) rather than wasm silently emptying/short-cutting. The `repeat` family (`string.repeat`/`list.repeat`/`bytes.repeat`) clamps SIGNED: a negative count yields the EMPTY string/list/bytes on BOTH targets — previously `string.repeat(s, -2^31)` panicked natively (Rust capacity overflow, raw exit 101) while the wasm leg trapped on the negative alloc size (exit 134): differential-fuzz, neither a T6 form. bytes/list already clamped; string now matches. The ONE exception is `string.slice` start/end, whose native oracle clamps SIGNED (`(x.max(0) as usize).min(len)`): a negative `string.slice` start maps to 0, handled by the separate SIGNED helper. For INDICES — `list.get`/`get_or`/`set`/`insert`/`remove_at`/`swap`/`update` — the index is UNSIGNED (`i as usize`) so a negative or `>= 2^32` index takes native's no-op / default / append path. Previously `list.drop_end(xs, 2^32-1)` returned a corrupt length exposing uninitialized heap, `list.take_end(xs, 2^32-1)` TRAPPED (exit 134), `list.chunk(xs, -1)` TRAPPED on wasm while native made one chunk, and `string.take(s, 2^32)` returned empty on wasm while native returned the whole string — all now byte-identical native == wasm at the 2^31 / 2^32 boundary and for negative counts on small inputs. The fixtures exercise COUNT boundaries (not SIZE boundaries), so no allocation exceeds a few KiB and the result is machine-independent. The same rule binds byte OFFSETS and pad WIDTHS. `bytes.read_*` guards were `p + N > b.len()` computed on `pos as usize`, so a NEGATIVE pos became ~1.8e19 and the addition WRAPPED back into range — the slice index then panicked (exit 101, a forbidden T6 form); every guard in the native runtime is `checked_add` now, and `bytes.read_f16_le`'s wasm self-host gained the negative guard its bytes_core siblings already had (without it `i64::MIN` reached a wrapped address and returned the value at offset 0 where native returned the 0.0 default). `string.pad_start`/`pad_end` took `width as usize` natively, so a NEGATIVE width asked for an astronomical pad and panicked with a capacity overflow where the wasm self-host returned the string unchanged; the width now clamps SIGNED, matching the self-host and the repeat family. `bytes.copy_to_ptr`'s cap is UNSIGNED (native `b.len().min(cap as usize)`), so a negative cap saturates to the whole buffer; the wasm self-host compared it SIGNED and returned the raw negative count. Finally, a `repeat` whose RESULT would exceed a shared 2^31-byte ceiling ABORTS in the T6 form on both (`Error: repeat result too large` + exit 1) instead of failing in two different forms — native a raw allocation failure, wasm an out-of-bounds trap; the C-161 rule applied to the repeat family, with `ALMIDE_REPEAT_MAX_BYTES` (runtime/rs/src/string.rs) and the `string_repeat.almd` guard as two of THREE copies that must stay in sync: the native runner's shim (`render_native.rs` `rt_string_repeat`) is the third, and it HAD drifted to a bare `s.repeat(n as usize)` — a let-bound `string.repeat(s, -1)` (the shape that routes through the shim; a concat-position call links the self-host instead) was a capacity-overflow PANIC at exit 101 on the native leg while wasm printed normally (differential fuzz, seed 1785015406589852000 index 1012). The drift survived because the fixture exercised `repeat 2` and `repeat 0` but never a NEGATIVE count — the statement's central repeat claim was exactly the untested cell. The shim now carries the same clamp + ceiling, and the fixture pins negative counts in BOTH call positions, i64::MIN included. The HEAP-ELEMENT chunk/windows twins (`list_chunk_str`/`__do_windows_str`, the List[String] route in the SAME stdlib file) missed the negative-count clamps their Int variants gained with this contract: `windows(xs, i32::MIN)` computed `total - n + 1` ≈ 2^31 windows and looped two billion times on wasm against native's instant `[]` (differential fuzz, seed 1785165458340124000 index 803, a chunk_str.almd mutation), and a negative chunk count reached an alloc as a negative length. Both twins now carry the Int variants' clamps verbatim, and the chunk_str fixture pins the negative counts on the heap-element route."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/list_count_index_truncation.almd", class = "fixture" },
  { path = "spec/wasm_cross/string_count_truncation.almd", class = "fixture" },
  { path = "spec/wasm_cross/map_wide_int_key.almd", class = "fixture" },
  { path = "spec/wasm_cross/chunk_str.almd", class = "fixture" },
]

# ── FLOAT LIST ORDERING = IEEE-754 totalOrder (Cluster-B) ────────────
[[contract]]
id        = "C-055"
spec      = "ALS-C9"
title     = "list.min/max/sort/sort_by over Float use IEEE-754 totalOrder, valid + identical on both targets"
statement = "`list.sort`/`list.min`/`list.max` over `List[Float]` and `list.sort_by` with a Float key ORDER by IEEE-754 totalOrder (the ordering twin of `f64::total_cmp`): a +NaN takes the greatest totalOrder position (after +inf), a -NaN the least (before -inf), and `-0.0 < +0.0`. This makes Float ordering legal and byte-identical native == wasm, fixing the divergence where native REJECTED it at compile (`f64: Ord` unsatisfied) while wasm either accepted it with NaN-unaware `f64.le` or emitted an INVALID module (the `sort_by` Float-key path stored an f64 into an i64 local → `local.set` validator error). Native routes `List[Float]` ordering to `total_cmp`-based `_float` runtime variants (IntrinsicLowering swaps the symbol); wasm mirrors with the sign-magnitude bit trick inside `emit_ord_cmp3`'s Float arm (`key = bits ^ ((bits >>_s 63) >>_u 1)`, a monotone i64 key) and pre-transforms `sort_by` Float keys onto the i64 storage path; interp mirrors with `f64::total_cmp`. On the v1 leg the cached-keys desugar (`sort_by` → `sort_by_keys` + a materialized key map) admits ONLY Int keys: it types the synthesized keys list as List[Int], so an ungated String key linked the scalar `list.map` (indirect-call type mismatch trap, differential-fuzz seed 20260718 index 866) and a Float key would sort by raw i64 BIT patterns (wrong order for negatives) — String keys wall honestly (`sort_by_str_key_x`), Float keys ride the totalOrder route. NaN-SIGN CAVEAT: the totalOrder POSITION of a NaN depends on its sign bit, which for a COMPUTED NaN (`0.0/0.0`) is implementation-defined — native/wasm const-fold it to +NaN (top) while the interp's runtime `divsd` yields -NaN (bottom), so the 3-way fixture stays sign-deterministic (uses ±inf, not a divided NaN); the +NaN-at-top positioning is verified native == wasm directly. ASYMMETRY (named deliberately): this list-extremum/ordering totalOrder is a DIFFERENT contract from the SCALAR `float.min`/`float.max`/`math.fmin`/`math.fmax`, which keep their C-049 NaN-IGNORING semantics — a resolved-but-unsupported sort element type is still a clean emit-time ICE, never an invalid module."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/list_float_total_order.almd", class = "fixture" },
  { path = "spec/wasm_cross/sort_by_call_count.almd", class = "fixture" },
]

# ── list.product OVERFLOW WRAPS (Cluster-B) ──────────────────────────
[[contract]]
id        = "C-056"
spec      = "ALS-T16"
title     = "list.product wraps on i64 overflow, consistent with list.sum and plain `*`"
statement = "`list.product` follows the language's integer-overflow law — two's-complement WRAPPING at runtime, identical to `list.sum`, to plain `a * b` (C-001/C-047 family), and to the wasm `i64.mul` accumulator. Native folds with `wrapping_mul` (identity 1) so the result is the SAME on any build profile; the old `Iterator::product` PANICKED under debug `-C overflow-checks` (exit 101) while wrapping in release and on wasm — a profile-dependent split now removed. `[i64::MAX, 2]` yields `-2` byte-identically native == wasm; the empty product is `1`. The interp gained a matching `list.product` arm so the 3-way oracle covers it instead of skipping."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/list_product_overflow.almd", class = "fixture" },
]

# ── ASSIGN-OF-UNIT REJECTION (in-place mutator misuse, Cluster-A) ─────
[[contract]]
id        = "C-057"
spec      = "ALS-M9"
title     = "Assigning a Unit-returning in-place mutator's result is a checker error on both targets"
statement = "An in-place stdlib mutator (`list.push`, `string.push`, `map.set` on a mut receiver — anything returning Unit) MUST be used as a STATEMENT, not assigned. `b = list.push(b, x)` assigns Unit to a non-Unit binding: native used to fail only at rustc (E0308 'expected Vec, found ()') while WASM erased Unit and silently RAN the program — a build-success asymmetry (compiled on WASM, not native). The checker now rejects the Unit-to-non-Unit assignment (E001) with a type-directed hint (call it as a statement, or rebuild with `b = b + [item]` for a List / `s + \"…\"` for a String), so BOTH targets reject identically. The well-typed statement form is byte-identical native == wasm."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/inplace_mutator_statement.almd", class = "fixture" },
]

# ── UNDECIDABLE EMPTY COLLECTION = COMPILE ERROR (Rust/Swift rule, never defaulted) ─
[[contract]]
id        = "C-058"
spec      = "ALS-C10"
title     = "An empty collection with an uninferable element type is a compile error on both targets, never silently defaulted"
statement = "An empty collection whose element type CANNOT be inferred from any surrounding context — an empty `[]` / `[:]` literal, `set.new()`, `map.new()`, or `list.with_capacity(n)` consumed only by element-agnostic ops, and the iterable of a `for _ in []` — is a COMPILE ERROR on BOTH targets (frontend `E018`), resolved by a type annotation. Almide follows Rust/Swift: an undecidable empty collection demands an explicit element type and is NEVER silently defaulted to an arbitrary one (observability is irrelevant, exactly as in Rust/Swift — `for _ in []` is an error even though the element is never read). This replaces the former defaulting behavior (old C-055, which set the element to `Int` in codegen so both targets built): defaulting an undecidable element in codegen was a layer violation and observably mis-typed record fields. Because the check runs in the type checker before code generation, BOTH targets reject identically (previously native rustc failed with E0282/E0283 on the emitted `Vec::<_>::new()` while WASM, carrying no element type, ran — a build-success asymmetry). The ANNOTATED forms of every producer build and print byte-identically native == wasm; a bare list literal also accepts the inline `[]: List[T]` call-arg annotation. A rigid generic parameter (`fn f[T]() -> List[T] = []`) is a concrete element type and is accepted. The fold over an empty collection (former C-052 defaulting) folds into this rule: the empty collection must be annotated."
since     = "0.24.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/empty_collection_annotated.almd", class = "fixture" },
  { path = "tests/diagnostics/e018-empty-collection-element/broken.almd", class = "fixture" },
]

# ── COMPILE-TIME RECURSION IS HOST/PROFILE-INDEPENDENT (no stack-overflow crash) ─
[[contract]]
id        = "C-059"
spec      = "ALS-I2"
title     = "Compilation does not overflow the native stack on wide or deep input, identically on every host and build profile"
statement = "Whether a program COMPILES is a function of the program, not of the host OS's default thread-stack size or the compiler's build profile. Two mechanisms make this so. (1) The compiler driver runs the whole parse → check → lower → emit pipeline on a dedicated worker thread with a fixed 256 MiB stack (overridable by `ALMIDE_COMPILER_STACK`, the analogue of rustc's `RUST_MIN_STACK`), so the achievable depth is decoupled from the OS main-thread limit — which is ~8 MiB on Linux/macOS but only ~1 MiB on Windows. Before this, a program that compiled on Unix could overflow the stack mid-codegen on Windows (it was a release-blocking crash in the Windows CI matrix build, a `float.to_fixed` fuzz body that exceeded the 1 MiB Windows main stack), and deeper inputs split debug-vs-release (unoptimized frames are ~48 KiB, far larger). (2) The Perceus RC-insertion pass (`pass_perceus`) walks the function-body statement chain ITERATIVELY — `split_chain` linearizes the `FnBody` continuation chain and the per-node passes fold over it — so a function body of N sibling statements costs O(1) native stack rather than recursing once per statement (formerly ~48 KiB/frame, overflowing the 256 MiB driver near ~5460 statements and a 1 MiB stack near ~20). A WIDE machine-generated body is therefore NOT rejected and NOT crashed: it compiles and runs byte-identical native == wasm. Expression NESTING (the orthogonal dimension) stays bounded deterministically by the parser's `MAX_DEPTH = 500`, a constant identical on both targets and both profiles — so the one remaining limit is a clean diagnostic, never a SIGSEGV. The de-recursion is behavior-preserving: the codegen snapshots and the full cross-target gate are byte-for-byte unchanged."
since     = "0.25.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/wide_function_body.almd", class = "fixture" },
  { path = "tests/compiler_stack_test.rs", name = "wide_function_body_compiles_in_small_stack", class = "fixture" },
]

# ── A Value REPRS AS ITS JSON TEXT, BYTE-IDENTICAL NATIVE == WASM ─────
[[contract]]
id        = "C-060"
spec      = "ALS-D2"
title     = "A Value reprs as its JSON text byte-identically on native and WASM, bare and as a Repr-record field"
statement = "A `Value` (the dynamic JSON-like type) renders to its JSON text — `null`, `true`/`false`, the integer's decimal, the float's Display form (`format!(\"{}\", f)`, which drops an integer-valued float's trailing `.0`), the JSON-escaped string, `[…]`, `{…}`, recursively — byte-identically on native and WASM, in EVERY repr path: a bare `\"${v}\"` interpolation, AND a `Value` field inside a `Repr`-deriving record/variant reached through both the `.repr()` (AlmideRepr) path and the `{}` (Display / interpolation) path. Before this, a `Value` field in a `Repr` type failed to COMPILE natively — `Value` impl'd neither `AlmideRepr` nor `Display`, so the derived `self.<field>.almide_repr()` and the `<Type>_repr` `{}` both errored (E0599 / E0277) — while WASM emitted the placeholder `Value {  }` (it treated `Value` as an empty record). Both targets now route through the SAME serializer (native `almide_rt_value_stringify`, WASM `__value_stringify`). The WASM float branch was additionally corrected to the native Display form via `__float_display` — it previously used the round-trip `float_to_string`, so an integer-valued float diverged (`3` native vs `3.0` WASM) — which also makes `value.stringify` / `json.stringify` of a float agree native == wasm."
since     = "0.26.7"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/value_repr.almd", class = "fixture" },
]

# ── A mut Map PARAMETER MUTATED IN PLACE BUILDS + PERSISTS, IDENTICAL ─
[[contract]]
id        = "C-061"
spec      = "ALS-M13"
title     = "A mut Map parameter mutated in place builds on both targets and the mutation persists, byte-identical"
statement = "A `mut Map` function parameter mutated in place via `map.insert` is promoted to a mutable borrow (RefMut) — exactly like a `mut List` + `list.push` — so it BUILDS on native (previously `E0596`: `Map`/`Set` were not recognized as heap types in borrow inference, so the param was forced to an immutable owned binding and the in-place mutator's `&mut m` had no `mut` binding to borrow) and the mutation PERSISTS to the caller, byte-identically wasm. A read-then-write through the same `mut` param (`map.get_or` then `map.insert`) works because RefMut covers both reads and writes. This closes the `list.push` (compiled) vs `map.insert` (E0596) asymmetry — a build-success divergence where wasm built but native did not."
since     = "0.26.9"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/mut_map_param.almd", class = "fixture" },
]

[[contract]]
id        = "C-062"
spec      = "ALS-D7"
title     = "The RawPtr / linear-memory bridge moves bytes byte-identically on both targets"
statement = "`bytes.as_ptr` / `as_mut_ptr` return a RawPtr to a Bytes data region; `from_raw_ptr(ptr, len)` copies `len` bytes from a raw offset into a fresh Bytes; `copy_to_ptr(b, ptr, cap)` copies `min(len(b), cap)` bytes through the pointer and returns the count. An EMPTY buffer has no data region, so `as_ptr`/`as_mut_ptr` return NULL on both targets and every bridge op through null moves nothing (`copy_to_ptr` returns 0, `from_raw_ptr` returns the empty Bytes — the wasm self-host now carries the native oracle's null/non-positive guards verbatim): before, native handed out `Vec`'s DANGLING sentinel and a safe program writing through it was UB — silent heap corruption, death with no output — while the wasm self-host handed out `handle + 12` of the 0-len block and overwrote the NEXT allocation (differential fuzz, 2026-08-10 night, a `bytes.new(-1)` destination). A RawPtr is a real `*mut u8` on native and an i32 linear-memory offset on wasm — the pointer VALUE differs per target by nature and is never observed; only the bytes moved through it are, so stdout is byte-identical. These four intrinsics used to ICE in WASM emit (`[ICE] … no WASM runtime fn`); v0.26.12 turned that into a clean build error, and v0.26.15 implements the wasm bridge (offset arithmetic + `memory.copy`). `as_mut_ptr` is `@mutating` so its Bytes argument is a mutable borrow on native (the buffer must be RefMut to hand out a writable pointer)."
since     = "0.26.15"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/bytes_rawptr.almd", class = "fixture" },
]

# ── PARSE A glTF/JSON DOCUMENT + WALK ITS ARRAYS, BYTE-IDENTICAL ─
[[contract]]
id        = "C-063"
spec      = "ALS-D3"
title     = "Parsing a heterogeneous-nested glTF/JSON document and walking its arrays by element is byte-identical on both targets"
statement = "`json.parse` of a glTF-style document (heterogeneous nested objects — accessors with float `min`/`max` arrays and `type` strings, materials with nested extensions, meshes with primitives), followed by `json.get_array` + `list.get_or` element access across `meshes`, every `accessor`'s nested `min`/`max` arrays, and `bufferViews`, produces byte-identical stdout native ⇄ wasm. Regression lock for #470, reported on a real ~10 MB VRM as a wasm-only element-access trap (`memory access out of bounds`) where `list.len` of a `get_array` result was correct but its backing disagreed; the reported flow no longer reproduces on 0.26.18+ (the host-buffer / RawPtr bridge it relied on was reworked in v0.26.15, C-062), and this fixture keeps the parse + array-element walk locked."
since     = "0.26.19"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/json_gltf_walk.almd", class = "fixture" },
]

# ── EFFECT-FN AUTO-UNWRAP IS BINDING-POSITION-UNIFORM, BYTE-IDENTICAL ─
[[contract]]
id        = "C-064"
spec      = "ALS-M4"
title     = "The effect-fn Result auto-unwrap rule is identical across binding positions and type-directed, byte-identical on both targets"
statement = "In an effect fn, a Result[T, E]-typed RHS auto-unwraps (auto-`?`) in every binding-shaped position — `let`, `var`, plain reassignment `x = effectCall()` (#485), and index assignment — unless the TARGET keeps the Result: an explicit `Result[..]` annotation (`let r: Result[Int, String] = step()`) or a Result-typed assign target stores the Result intact, while an UN-annotated bind of a `-> Result[..]`-declared effect fn still auto-unwraps. The full matrix prints byte-identical stdout and exits 0 on native ⇄ wasm."
since     = "0.26.20"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/effect_assign_unwrap.almd", class = "fixture" },
]

# ── STRING POSITION API SPEAKS ONE UNIT: CODEPOINTS ──────────────────
[[contract]]
id        = "C-065"
spec      = "ALS-S1"
title     = "The string position API is codepoint-indexed end-to-end on both targets"
statement = "Every position/count in the `string` module speaks CODEPOINTS (#419): `len` returns the codepoint count, `index_of`/`last_index_of` return codepoint indices (the byte offsets of `str::find`/`rfind` converted via prefix codepoint count), `get` takes a codepoint index (negative or past-the-end -> none), and `slice` takes codepoint start/end clamped to [0, count] with the `end = i64::MAX` default degrading to the end of the string. Composition is therefore closed: `string.take(s, string.index_of(s, sub) ?? 0)` splits at the right place on multibyte input. Byte-identical native == wasm across ASCII, CJK, and mixed subjects."
since     = "0.26.20"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/string_codepoint_index.almd", class = "fixture" },
  { path = "spec/wasm_cross/string_lines.almd", class = "fixture" },
]

[[contract]]
id        = "C-067"
spec      = "ALS-T6"
title     = "The xs[i] index syntax aborts on out-of-bounds (read and write)"
statement = "The index-operator path `xs[i]` (both read `xs[i]` and write `xs[i] = v`) is the ASSERT-in-bounds form: an out-of-bounds, negative, or >= 2^32 index ABORTS with the unified `Error: index out of bounds\n` + exit 1, byte-identical native == wasm — distinct from the stdlib `list.get`/`set` ops (C-054) which take native's no-op / default path. The check runs on the FULL i64 before the wasm i32 narrowing (so an index >= 2^32 can never truncate into range and read/write the wrong slot) and applies to the WRITE path too (previously wasm had no write-path bounds check — an OOB `xs[i] = v` was a silent heap write into adjacent allocator memory, exit 0; native panicked exit 101). Native routes through the `almide_index!`/`almide_index_set!` macros, wasm through `emit_index_bound_guard` + the unified `div_trap`, so neither a native panic (exit 101) nor a wasm `unreachable` trap (exit 134) is observable."
since     = "0.27.4"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/index_bounds.almd", class = "fixture" },
  { path = "spec/wasm_cross/index_bounds_write_heap.almd", class = "fixture" },
  { path = "spec/wasm_cross/index_bounds_i64.almd", class = "fixture" },
  { path = "spec/wasm_cross/index_bounds_write_only_var.almd", class = "fixture" },
]

[[contract]]
id        = "C-068"
spec      = "ALS-M4"
title     = "Auto-? is target-directed in construction positions"
statement = "An effect-fn call in a CONSTRUCTION position — a record field, list element, tuple slot, or map value — whose declared TARGET type is `Result[T, E]` KEEPS its Result (the auto-inserted `?` is stripped), exactly like the binding-statement arms (let/assign/index-assign/field-assign). The implicit Result-lift of an effect call plus an unconditional unwrap previously produced invalid native Rust (E0308: a `?` against a Result-typed slot) while wasm RAN and stored a SILENTLY WRONG value (the unwrapped i64 0 instead of the Result). Now byte-identical native == wasm: the Result is preserved at every construction position."
since     = "0.27.4"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/autotry_construction.almd", class = "fixture" },
  { path = "spec/wasm_cross/autotry_result_first_arg.almd", class = "fixture" },
]

[[contract]]
id        = "C-069"
spec      = "ALS-M4"
title     = "Effect-fn tail self-recursion loop-converts to O(1) stack on both targets"
statement = "A self-recursive `effect fn` whose recursive call is in tail position runs in O(1) native/wasm stack (loop-converted), identically to the non-effect equivalent — not O(n) frames that overflow at ~1e5 depth. The auto-? desugar moved into the frontend (cc70ebc4), so the tail self-call reaches TailCallOpt already wrapped as `Try{Call self}` (Rust arm) or `Ok(Try(Call self))` (wasm arm, where ResultPropagation precedes TCO); TCO sees through both wrappers — the `?` is subsumed by the loop (an Err can only originate at a base case) and the `Ok` propagation wrapper is tail-transparent for effect fns. The Err short-circuit at a base case still fires. Before the fix every implicitly-lifted recursive effect fn crashed at ~1e5 depth (native stack overflow exit, wasm call-stack-exhausted trap) while the identical non-effect fn was O(1). The v1 leg's OWN loop rewrite must ALSO see through the wrapper: tco_collect classified `Try{Call self}` as the recursive arm but tco_rewrite lacked the twin arm, so an err-CAPABLE effect fn's recursive arm compiled to 'exit with the last base's kind' — a carried accumulator delivered its INITIAL value, and `checked(n - 0)` terminated with ok(0) where native spins (fuzz seed-20260718 index 946)."
since     = "0.27.4"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/effect_tco.almd", class = "fixture" },
]

[[contract]]
id        = "C-070"
spec      = "ALS-M1"
title     = "Nested constructor patterns match and bind identically on both targets"
statement = "A NESTED pattern as the argument of a user-defined constructor pattern — `Wrap(A(n))`, `Box(Some(n))`, `Held(Circle { r })`, `Node(Leaf(a), Leaf(b))` — emits the inner discriminant test(s) into the arm guard and binds the inner payload(s) recursively, byte-identical native == wasm. The wasm match backend's constructor arm-binding loop previously handled only top-level `Bind`/`Literal` args: a nested ctor / Some / None / variant-record arg got NO inner discriminant test and bound garbage (0), so wasm SILENTLY took the wrong arm (exit 0) while native was correct (and a nested ctor on a recursive boxed variant additionally emitted invalid Rust). Now `emit_arg_tests` recurses the discriminant tests into the guard and `bind_arg` recurses the binding through nested ctor / Some / variant-record payloads. The NATIVE side of the same promise (#757): the `matches!` shape-guard the #610 box rewrite synthesizes must carry EVERY refutable constraint of the boxed inner pattern — the inner variant tag and inner literals, at any depth — so a non-matching value REFINES to the next arm. Erasing them to `_` admitted the value past the guard and panicked at the body's `let-else unreachable!()` (exit 101) while wasm refined correctly."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/nested_ctor_pattern.almd", class = "fixture" },
  { path = "spec/wasm_cross/nested_variant_tag_box.almd", class = "fixture" },
]

[[contract]]
id        = "C-071"
spec      = "ALS-I1"
title     = "Single-part interpolation RC balance"
statement = "A degenerate one-part interpolation `\"${e}\"` that the WASM emitter short-circuits to return `e`'s pointer directly does not double-free `e`'s heap buffer at teardown: when `e` is a heap-String alias the binding acquires its own reference, so native and WASM both produce the value and exit 0 (no 'unreachable' trap). Literal, non-String, surrounded, and multi-fragment interpolations stay genuinely fresh."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/r5_wasm_interp_alias_rc.almd", class = "fixture" },
]

[[contract]]
id        = "C-072"
spec      = "ALS-M2"
title     = "Inferred named-record repr parity"
statement = "A record literal inferred without a type annotation reprs identically on native and WASM: when its field set matches a declared record type, both print `TypeName { ... }` in the type's DECLARATION field order with correct values even when the literal is written out of declaration order; a truly anonymous record prints prefix-less in sorted field-name order on both targets."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/r5_wasm_inferred_record_repr.almd", class = "fixture" },
]

[[contract]]
id        = "C-073"
spec      = "ALS-M1"
title     = "Tuple pattern testing a variant constructor"
statement = "A tuple match arm that tests a variant constructor in any element position (nullary in pos 1/2 or both, or a payload-carrying constructor whose binder is captured) compiles and runs byte-identically on native and WASM; the WASM module passes structural validation (no 'expected i32 but nothing on stack')."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/r5_wasm_tuple_variant_pattern.almd", class = "fixture" },
]

[[contract]]
id        = "C-074"
spec      = "ALS-S6"
title     = "Iterative split/replace on large inputs"
statement = "string.split and string.replace produce byte-identical results on native and WASM for large inputs (5000+ segments, 6000+ occurrences) and exit 0 with no 'call stack exhausted' trap; an empty `from` pattern inserts the replacement at every codepoint boundary (Rust `str::replace` parity)."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/r5_wasm_split_replace_iterative.almd", class = "fixture" },
]

[[contract]]
id        = "C-075"
spec      = "ALS-I3"
title     = "lowmisc round-5 cluster: borrowed-param owning binding, effect-Option auto-try strip, matching-error ! passthrough"
statement = "Binding a borrow-rendered param (List &[T] / Map &AlmideMap) to a local converts to the owned type; an effect-fn Option[T] bound to a let (the binding carries the explicit `!` since the E041 migration; C-216 pins explicit == implicit) and consumed by ?? strips the effect Result first leaving Option[T]; and `!` propagation whose source error type equals the enclosing fn's declared error type carries the custom variant error through `?` unchanged (no Debug stringify). All three compile and run byte-identically on native Rust and wasm32: stdout 'list=[7, 8]' / 'map_len=2' / 'miss=999' / 'hit=42' / 'fetch=item', exit 0."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/r5_lowmisc_param_try_err.almd", class = "fixture" },
]

[[contract]]
id        = "C-076"
spec      = "ALS-M3"
title     = "Producer-side in-module variant construction is target-stable"
statement = "A producer function declared inside the submodule that owns a variant type, constructing the variant via the bare constructor (tuple `Circle(r)` or record-variant `Circle { radius: r }`) and returning it, compiles and runs byte-identically on native Rust and wasm32 (stdout, stderr, exit). The construction expression's type is pinned to the owner-qualified `mod.Type`, so the #433 name-pinning verifier never fires on a valid in-module construction."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/r5_mod_producer_variant.almd", class = "fixture" },
]

[[contract]]
id        = "C-077"
spec      = "ALS-T7"
title     = "Cross-module heap-global init order is dependency-respecting"
statement = "An importing module's top-level let that reads an imported module's heap global (string/list/etc.), directly or through a function call, observes the imported global already initialized on both native Rust and wasm32 (byte-identical stdout, stderr, exit). WASM eager top-let init runs in a dependency-respecting order computed interprocedurally; native's lazy/forced init agrees (C-007)."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/r5_mod_global_init_order.almd", class = "fixture" },
]

[[contract]]
id        = "C-078"
spec      = "ALS-M2"
title     = "Phantom record generic param is stripped on the Rust target"
statement = "A record type with a generic param used by no field (a phantom param) compiles and runs byte-identical native == wasm. Rust forbids an unused type param (error[E0392]), so the Rust struct and every reference to it are emitted without generics; the param carries no runtime data (codegen erases types), so distinct instantiations share one representation — matching wasm, which already erases type params."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/phantom_type_param.almd", class = "fixture" },
]

[[contract]]
id        = "C-079"
spec      = "ALS-M3"
title     = "Variant cases with distinct anonymous-record payloads are target-stable"
statement = "A variant whose cases carry anonymous-record payloads with different field-name sets compiles and runs byte-identical native == wasm, including for a payload type that appears only in the declaration (never constructed). The anonymous-record collector scans declaration field/payload types, so every payload lowers to its generated struct (`AlmdRec_<fields>`) instead of native falling back to the bare field name as a type (rustc E0425)."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/anon_record_variant_payloads.almd", class = "fixture" },
]

[[contract]]
id        = "C-080"
spec      = "ALS-M5"
title     = "Empty map.from_list / set.from_list resolves its element from the result type"
statement = "`map.from_list([])` / `set.from_list([])` with an annotated result type compiles and runs byte-identical native == wasm. The empty-list argument's element type flows only through the generic call's return (Map[K,V] from List[(K,V)], Set[E] from List[E]); the checker can leave it an unresolved List slot, which native tolerates but the WASM concretization gate refuses. ConcretizeTypes derives the argument element from the call's resolved Map/Set type — matching both the Module-call and the lowered RuntimeCall (`almide_rt_map_from_entries`) forms — pinning the empty list (and its ANF temp) before the gate."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/empty_from_list_mono.almd", class = "fixture" },
]

[[contract]]
id        = "C-081"
spec      = "ALS-M5"
title     = "Generic fn in an inferred-param lambda resolves its type parameter"
statement = "A generic function called inside a `list.map` (or similar HOF) lambda whose parameter type is inferred from the collection element compiles and runs byte-identical native == wasm. The generic's type parameter, which flows in only via the HOF's `(A)->B` slot after the lambda body is inferred, is fresh-instantiated before back-propagation instead of leaking its literal name into the union-find, so it resolves to the concrete element type. Verified at two instantiations (Int and String element types)."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/generic_fn_in_inferred_lambda.almd", class = "fixture" },
]

[[contract]]
id        = "C-082"
spec      = "ALS-M5"
title     = "Calling a closure-typed lambda parameter yields the call result, not the closure"
statement = "A lambda whose body CALLS one of its parameters — `(f) => f(10)` over a `List[(Int)->Int]` via `list.map` — compiles and runs byte-identical native == wasm. When the called parameter's type is an unresolved inference var at the call site, the call constrains it to `(args)->?ret` and yields `?ret` (resolved from context), not the parameter's own closure type — so the HOF result is `List[?ret]`, not a list of closures. Verified for Int and String closure element types."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/call_closure_lambda_param.almd", class = "fixture" },
]

[[contract]]
id        = "C-083"
spec      = "ALS-M8"
title     = "A negated i64::MIN literal is representable, not folded to zero"
statement = "The literal `-9223372036854775808` (and its underscored/hex forms) compiles to a real `i64::MIN` and runs byte-identical native == wasm. The magnitude 9223372036854775808 overflows a signed `i64` parse, which previously clamped to 0 so the unary minus produced 0; lowering now folds the leading minus INTO the integer-literal parse (`-<digits>`) for decimal, hex, octal, and binary, so the whole negative value is parsed in range."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/i64_min_literal.almd", class = "fixture" },
]

[[contract]]
id        = "C-084"
spec      = "ALS-D6"
title     = "Codec/value decode error messages are byte-identical across targets"
statement = "value.as_* type mismatches and Codec decode failures produce the SAME error string on both backends. The WASM emitter mirrors the native oracle (runtime/rs/src/value.rs): type errors name the Value variant (`expected Float`, `expected Int`, `expected Str`, `expected Bool`, `expected Object`, `expected Array`) and a missing required field interpolates its name (`missing field '<key>'`, built at runtime via __concat_str), replacing the prior lowercase / `key not found` placeholders."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/codec_decode_errors.almd", class = "fixture" },
]

[[contract]]
id        = "C-085"
spec      = "ALS-D6"
title     = "Float decode widens an integral JSON number to f64"
statement = "Because a JSON number has no int/float distinction, value.as_float, json.as_float, and Codec Float-field decode (required, Option, and defaulted) all accept an integer JSON value and widen it to f64 — matching the native oracle (which already coerces in json.as_float/get_float and now in value.as_float) and serde. This keeps Codec roundtrips total when a Float value serializes without a decimal point. Verified byte-identical native == wasm for required, Option, and defaulted Float fields plus direct value.as_float / json.as_float."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/codec_float_int.almd", class = "fixture" },
]

[[contract]]
id        = "C-086"
spec      = "ALS-I1"
title     = "Pass-through stdlib combinators give their result its own reference"
statement = "Combinators that return an INPUT cell unchanged — option.filter (some), option.or_else (some), option.flatten, result.map_err (ok), result.map (err), result.flat_map (err) — SHARE a +1 onto the returned cell on the pass-through path (like list.get's some-box), so the let-bound result owns its own reference. Without it the result and the input aliased one cell and both scope-end Decs double-freed it: a __rc_dec trap, or a SILENT wrong value read from the freed cell through a longer combinator chain. Verified byte-identical native == wasm across all six combinators, a four-stage find|>map|>filter|>flat_map chain, and an OOB-defaulted loop lookahead (list.get(xs, i+1) ?? default)."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/alias_combinator_rc.almd", class = "fixture" },
]

[[contract]]
id        = "C-087"
spec      = "ALS-T3"
title     = "JSON number and \\u string decoding are byte-identical across targets"
statement = "json.parse decodes \\uXXXX escapes (BMP 1/2/3-byte UTF-8 plus high+low surrogate pairs to astral code points) and numbers identically on both backends. The WASM parser hands every float token to the same correctly-rounded parser float.parse uses (__dec2flt) instead of an ad-hoc digits/divisor * 10^exp, fixing the dropped -0.0 sign (#663) and the one-ULP exponent error (#667); its \\u decoder UTF-8-encodes the code point (#651). The native oracle's BMP-only \\u decoder gained the same surrogate-pair joining so emoji and other astral escapes round-trip. Integers keep their exact i64 path."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/json_number_unicode.almd", class = "fixture" },
  { path = "spec/wasm_cross/json_string_span.almd", class = "fixture" },
]

[[contract]]
id        = "C-088"
spec      = "ALS-M7"
title     = "A Rust-keyword function name compiles on both targets"
statement = "A user function whose name is a Rust keyword (box, move, dyn, unsafe, …) compiles and runs byte-identical native == wasm. The Rust emitter raw-escapes the name (box → r#box) at BOTH its definition and every call site, driven by one shared keyword predicate (is_rust_keyword) — an incomplete per-site list previously left the definition or the call unescaped, producing invalid Rust. WASM identifiers have no keyword conflict."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/reserved_keyword_fn.almd", class = "fixture" },
]

[[contract]]
id        = "C-089"
spec      = "ALS-M5"
title     = "A default parameter referencing an earlier parameter is filled with its argument"
statement = "A default parameter value that references an earlier parameter (fn rect(w, h: Int = w)) compiles and runs byte-identical native == wasm. Defaults are inlined at the call site, so emitting the callee-local parameter name referenced a binding absent there (rustc E0425); lowering now substitutes each earlier parameter with its actual argument (chaining through earlier-filled defaults) before lowering the default expression."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/default_param_ref.almd", class = "fixture" },
]

[[contract]]
id        = "C-090"
spec      = "ALS-D7"
title     = "bytes.from_list on a List[Int] parameter compiles on both targets"
statement = "bytes.from_list applied to a List[Int] function parameter compiles and runs byte-identical native == wasm. A List[Int] parameter renders as &[i64], which the native runtime rejected when it took &Vec<i64> (rustc E0308). The runtime now takes a slice &[i64] — the parameter passes directly and a local Vec<i64> still deref-coerces."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/bytes_from_list_param.almd", class = "fixture" },
]

[[contract]]
id        = "C-091"
spec      = "ALS-M1"
title     = "A nested sub-pattern in let-destructuring binds every leaf"
statement = "A let-destructuring pattern containing a nested tuple (or record) sub-pattern binds every leaf, byte-identical native == wasm. The WASM emit handled only a flat Bind element and skipped nested sub-patterns — their leaves stayed zeroed; it now recurses, loading each sub-aggregate pointer and binding through it, mirroring the local pre-scan that already allocated those leaf locals."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/nested_tuple_destructure.almd", class = "fixture" },
]

[[contract]]
id        = "C-092"
spec      = "ALS-M2"
title     = "A generic record field is sized by its instantiated type at construction"
statement = "A generic record field is allocated and laid out by its instantiated type, byte-identical native == wasm. The WASM record emit sized the block from the DECLARED field list, so a generic field (value: T) used the unresolved param's default size (4 bytes); a trailing concrete heap field then wrote past the under-allocated block and read back empty, corrupting both the record and any spread of it. Construction now derives size and field order from the concrete (generic-substituted) layout of the result type."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/generic_record_field_size.almd", class = "fixture" },
]

[[contract]]
id        = "C-093"
spec      = "ALS-M3"
title     = "Mutually-recursive variant types compile on both targets"
statement = "Variant types forming a recursion cycle across two or more types (type A = A(B); type B = B(A)) compile and run byte-identical native == wasm. The native enum emit boxed a variant field only when it named the ENCLOSING type, so a mutual cycle emitted both enums without indirection and rustc rejected them as infinitely sized (E0072). Recursion detection is now cycle-based (a type that can reach itself through variant-field references), and the Box-rendering, pattern-deref, and boxed-fields-annotation sites all box a field that references ANY cycle member."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/mutual_recursive_types.almd", class = "fixture" },
]

[[contract]]
id        = "C-094"
spec      = "ALS-M5"
title     = "A protocol-method UFCS call on an inferred lambda param resolves the element type"
statement = "A higher-order call whose lambda argument has an UNANNOTATED parameter used as a protocol-method receiver (`list.map(xs, (e) => e.name())` where `xs: List[T]`, `T: Labelled`) compiles and runs byte-identical native == wasm. The checker resolves the callee signature up front and pins the lambda's inferred param to the expected element type (carrying its protocol bound) before inferring the body, so `e.name()` resolves via the protocol path instead of collapsing `e` into a closure type (which native rendered as `Rc<dyn Fn() -> String>`, rustc E0308). Verified for a stdlib HOF and a user-defined HOF."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/protocol_ufcs_inferred_lambda.almd", class = "fixture" },
]

[[contract]]
id        = "C-095"
spec      = "ALS-D6"
title     = "json.stringify_pretty is byte-identical indented output across targets"
statement = "json.stringify_pretty produces the same indented JSON on both backends. The WASM arm was a stub (compact output plus a stray newline); it now has a real recursive pretty-printer (__json_stringify_pretty) mirroring the native oracle (json.rs stringify_value): two-space indent per depth, one array element / object member per line, comma-then-newline separators, and inline [] / {} for empty containers. Scalars match the compact form. String quoting is the canonical 5-escape rule (backslash, quote, \\n, \\r, \\t; everything else raw UTF-8) — the SAME rule the compact stringify and the wasm __json_quote use. Previously the native pretty printer used Rust's {:?} (escape_debug), which escaped combining marks as `\\u{301}` — not valid JSON escaping and a byte divergence against the wasm leg (differential-fuzz: a `cafe` + U+0301 key). Verified byte-identical native == wasm over a nested object/array document, a top-level array, a top-level scalar, and combining-mark strings/keys."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/json_stringify_pretty.almd", class = "fixture" },
]

[[contract]]
id        = "C-096"
spec      = "ALS-R5"
title     = "process.args works on WASM and matches native"
statement = "process.args() runs on the WASM target (previously an emit ICE) and agrees with native. It maps to std::env::args() — argv[0] (the program path) followed by program args — built on WASM from the WASI args_sizes_get / args_get imports into the same List[String] layout native produces. The result List and each inner String are framed with the canonical 8-byte [len][cap][data] header (a 4-byte header made every element/byte read at the wrong offset — #645). Verified byte-identical native == wasm on the argument COUNT and on an argv[0]-independent content invariant (string.len of element 0 is non-zero), which reads the list at index 0 and walks element 0's UTF-8 data, exercising both the list and string header."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/process_args.almd", class = "fixture" },
]

[[contract]]
id        = "C-097"
spec      = "ALS-M5"
title     = "generic + on a type parameter concatenates strings/lists identically across targets"
statement = "A `+` whose operand is a type parameter T lowers to the default AddInt; when T instantiates to String or List, monomorphization re-dispatches it to ConcatStr / ConcatList (mirroring lowering's own type dispatch). A `fn dup[T](x: T) -> T = x + x` therefore concatenates for String and List and adds for Int, byte-identical native == wasm. A genuinely non-concatenable instantiation (e.g. a record) stays AddInt and is still rejected by the IR-verify gate."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/generic_concat.almd", class = "fixture" },
]

[[contract]]
id        = "C-098"
spec      = "ALS-D6"
title     = "cross-module derived Codec methods dispatch on WASM and match native"
statement = "A derived Codec method whose subject or element type is defined in another module (mod.Type.encode/decode, direct field, List[mod.Type], Option[mod.Type]) now dispatches on the WASM target instead of an emit ICE, and roundtrips byte-identically to native. The call site carries only Type.method (the subject type has no module) while the fn is registered under its module_origin-qualified name mod.Type.method; the WASM dispatcher resolves it by a unique func_map suffix match, and a __decode_list_mod.Type codec helper is routed before the dotted-name split that used to mis-parse the embedded dot. Native is unaffected (the #411-B BuiltinLoweringPass already flattens the name, but it is Rust-only)."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "tests/crossmod_matrix_test.rs", name = "crossmod_shape_matrix", class = "fixture" },
]

[[contract]]
id        = "C-099"
spec      = "ALS-M8"
title     = "comparison/equality operators byte-match native across all operand types on the v1 wasm path"
statement = "On the v1 wasm path the comparison operators (==, !=, <, <=, >, >=) produce stdout byte-identical to native for every operand type. Heap `==`/`!=` (Value, List[Int|String|Value|Float|Bool]) lowers to a scalar deep-equality call returning a 0/1 cond, so `if heap == heap …` branches on a single arm instead of the both-arms linearization that silently ran BOTH arms; equality is deep/structural element-wise (Value PartialEq, f64 == for floats, i64 0/1 for bools). String ordering (< <= > >=) compares lexicographic content via string.cmp rather than the i64 string handle, and Bool ordering follows value semantics (false < true) instead of being unhandled. Native is the oracle and native == wasm output is the certified invariant."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/value_eq.almd", class = "fixture" },
  { path = "spec/wasm_cross/list_eq.almd", class = "fixture" },
  { path = "spec/wasm_cross/list_eq_float_bool.almd", class = "fixture" },
  { path = "spec/wasm_cross/string_bool_ordering.almd", class = "fixture" },
  { path = "spec/wasm_cross/tuple_eq.almd", class = "fixture" },
  { path = "spec/wasm_cross/option_eq.almd", class = "fixture" },
  { path = "spec/wasm_cross/option_heap_eq.almd", class = "fixture" },
  { path = "spec/wasm_cross/result_eq.almd", class = "fixture" },
  { path = "spec/wasm_cross/heap_result_if_eq_cond.almd", class = "fixture" },
]

[[contract]]
id        = "C-100"
spec      = "ALS-M12"
title     = "Self-hosted String classification/transform ops byte-match native on wasm"
statement = "The self-hosted String classification predicate `string.is_alphanumeric` and the ASCII-fold transform `string.to_upper` MUST produce stdout byte-identical to native on the v1 wasm path. `is_alphanumeric` returns true only when every codepoint is ASCII alphanumeric and false for the empty string, matching native (`abc123`/`Z9` -> true; `ab-c`/`` -> false). `to_upper` maps `a-z` to `A-Z`, leaves all other bytes (digits, symbols, spaces) and the empty string unchanged, matching native `to_uppercase` over ASCII. Both ops were previously unlinked on v1 and are now certified equivalent across native Rust and wasm32."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/string_is_alphanumeric.almd", class = "fixture" },
  { path = "spec/wasm_cross/string_to_upper.almd", class = "fixture" },
  { path = "spec/wasm_cross/split_empty_sep.almd", class = "fixture" },
  { path = "spec/wasm_cross/replace_empty_pat.almd", class = "fixture" },
]

[[contract]]
id        = "C-101"
spec      = "ALS-M12"
title     = "List ops over heap elements (String/Value) byte-match native and are leak/double-free free"
statement = "List operations whose elements are heap-backed handles (List[String], List[Value]) — list.min/list.max, list.sort, list.insert/remove_at/swap, list.set, and list.update — produce stdout byte-identical to native on the v1 wasm path. Comparison-based ops order and select by element CONTENT (lexicographic byte order, matching v0's Rust String Ord), not by the raw i64/i32 handle the generic impls compared, which silently picked the wrong element. The typed _str/_value variants rc-copy each retained element to co-own it (so result and source can both drop without double-free), free each removed/replaced element exactly once, and recurse via __drop_value so a replaced Object/Array element's nested children are not leaked. For update, the closure is declared at its real element type — the generic f: (Int) -> Int CallIndirect expected an i64 result while a (Value/String)->same lambda returns an i32 handle (params are i64-widened; results are not), so the v1 indirect call trapped (#736) — and the closure's owned return is stored with an explicit slot acquisition so the scope's auto-release cannot free the stored element. The promise holds at scale: no leaks and no double-frees across repeated/large lists."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/list_minmax_str.almd", class = "fixture" },
  { path = "spec/wasm_cross/list_modify_heap.almd", class = "fixture" },
  { path = "spec/wasm_cross/list_set_value.almd", class = "fixture" },
  { path = "spec/wasm_cross/list_update_heap.almd", class = "fixture" },
  { path = "spec/wasm_cross/list_sort_str.almd", class = "fixture" },
  { path = "spec/wasm_cross/whilep_str.almd", class = "fixture" },
  { path = "spec/wasm_cross/chunk_str.almd", class = "fixture" },
  { path = "spec/wasm_cross/fold_heap_acc.almd", class = "fixture" },
  { path = "spec/wasm_cross/reduce_str.almd", class = "fixture" },
  { path = "spec/wasm_cross/closure_accumulator.almd", class = "fixture" },
  { path = "spec/wasm_cross/value_object_leak_loop.almd", class = "fixture" },
]

[[contract]]
id        = "C-102"
spec      = "ALS-M6"
title     = "List iteration, call-result element materialization, and tail-recursive list traversal byte-match native"
statement = "On the v1 wasm path, list element/iteration constructs produce stdout byte-identical to native Rust. A `List[String]` literal whose element is a string-returning module or named call (e.g. `[string.slice(s, 0, 1)]`) materializes a fresh owned String moved into the slot and freed by `DropListStr` at scope end. `list.enumerate` over a `List[String]` builds a `List[(Int,String)]`, and `enumerate |> list.find(closure)` evaluating a closure over the heap tuple's `.1` yields an `Option[(Int,String)]` that constructs, matches, destructures `some((idx, line))`, and drops (`$__drop_list_int_str`) identically across targets. A tail-self-recursive fn over a shrinking list (`list.drop(cs, 1)` per call with `match list.first(cs)`) is rewritten to an invariant borrowed list plus a synthetic scalar index — the cert-clean scalar-TCO form with no heap back-edge merge — covering direct-arm, block-arm-with-let, and string-eq element-use scans."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/list_call_element.almd", class = "fixture" },
  { path = "spec/wasm_cross/list_enumerate_find.almd", class = "fixture" },
  { path = "spec/wasm_cross/list_iter_tco.almd", class = "fixture" },
]

[[contract]]
id        = "C-103"
spec      = "ALS-D6"
title     = "Self-hosted dynamic Value model (merge, array/as_array roundtrip, tuple TCO) byte-matches native and is leak-free in a loop"
statement = "On the v1 wasm path the self-hosted dynamic Value model produces stdout byte-identical to native Rust. value.merge does a shallow Object merge that keeps a's keys in a's positions with b's values overriding, then appends b's new keys, and falls back to b when a is non-object (empty-merge is identity). value.array stores and value.as_array reads back the List[Value] payload with correct Ok/Err tag dispatch, and append-accumulator tail recursion building a List[Value] with a (Value, Int) heap-result tuple return lowers identically. The recursive Value drop is leak-free across 20000 create+drop iterations for the 3-level Array -> element-Value -> String and Result -> List[Value] -> element-Value -> String free chains, so the loop completes within page 1 instead of OOB-trapping."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/value_merge.almd", class = "fixture" },
  { path = "spec/wasm_cross/value_array_leak_loop.almd", class = "fixture" },
  { path = "spec/wasm_cross/value_as_array_leak_loop.almd", class = "fixture" },
  { path = "spec/wasm_cross/value_as_array_roundtrip.almd", class = "fixture" },
  { path = "spec/wasm_cross/value_array_tuple_tco.almd", class = "fixture" },
]

[[contract]]
id        = "C-104"
spec      = "ALS-M6"
title     = "Tail-recursive accumulator shapes lower to bounded-stack loops byte-matching native"
statement = "A self-tail-recursive function whose recursive call carries a growing heap accumulator MUST produce byte-identical stdout, stderr, and exit code on BOTH targets, executing in bounded stack on the v1 wasm path. This holds across the accumulator-construction shapes the TCO desugars must compose: let-bound and nested heap-result `if` arms (tail-duplicated into branched recursion), multi-leaf concat spines (`acc + c0 + c1`), List append, recursion inside a `match` returning `Result[List, String]` (from `!`-unwrap desugaring) on both ok and err paths, and effect-fn `(Value, Int)` tuple-result accumulators that destructure an effectful `!` result each step. A general (non-append) heap recursive arg that is NOT TCO-able instead lowers to a real bounded recursive call that still matches native exactly. Loop-carried accumulators are rc-balanced (drop-old/alloc-new, payload Dup) so there is no leak or double-free, and v1 output equals v0 in every case."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/general_recursion_tail.almd", class = "fixture" },
  { path = "spec/wasm_cross/tco_let_bound_if.almd", class = "fixture" },
  { path = "spec/wasm_cross/tco_multi_concat.almd", class = "fixture" },
  { path = "spec/wasm_cross/tco_nested_list_accumulator.almd", class = "fixture" },
  { path = "spec/wasm_cross/tco_over_match_result.almd", class = "fixture" },
  { path = "spec/wasm_cross/effect_tuple_result_tco.almd", class = "fixture" },
]

# ── APPEND-ACCUMULATOR TCO CROSS-TARGET BYTE-MATCH (option C) ─────────
[[contract]]
id        = "C-105"
spec      = "ALS-M6"
title     = "var/append accumulator loops (scalar, owned-handle, cross-dep, mutual-recursion) byte-match native on wasm"
statement = "A tail-self-recursive function that grows a heap accumulator by `acc + [x]` on every self-call is rewritten by the option-C TCO into a loop carrying one OWNED slot (seed `acc + []`, drop-old/acquire-new per iteration, cert `i(id)m`), and a mutually-recursive parser-loop pair is first reduced to a direct self-call (single-call sibling inlined, or the function-wide fresh-VarId desugar applied) so the same TCO fires. On the v1 wasm path these accumulator loops produce stdout, stderr, and exit code byte-identical to the native Rust oracle for any iteration count: scalar `List[Int]` elements, owned-handle `List[String]` elements (rc-incrementing concat, no leak/double-free), cross-dependent appends that must read the pre-update loop indices (no off-by-one), and mutual-recursion shapes whose semantics are preserved by the inlining. The list BLOCK ownership is machine-proved leak/double-free-free; per-element leak-safety and the final byte-equality are certified by the fixtures themselves."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/append_accumulator.almd", class = "fixture" },
  { path = "spec/wasm_cross/append_accumulator_heap.almd", class = "fixture" },
  { path = "spec/wasm_cross/cross_dep_accumulator.almd", class = "fixture" },
  { path = "spec/wasm_cross/mutual_append.almd", class = "fixture" },
  { path = "spec/wasm_cross/block_line_collect.almd", class = "fixture" },
]

[[contract]]
id        = "C-106"
spec      = "ALS-M10"
title     = "Heap value bound from an if/match arm byte-matches native on the v1 wasm path"
statement = "On the v1 wasm path, a heap-allocated value (a List, String, tuple, record, or user Result/Option payload) that is produced inside an if-arm or match-arm — including appended/concatenated list arms, record and tuple arms, str-int Ok-tuple arms, string-interpolated Err arms, arms that re-wrap a payload through a stdlib module call, and arms nested inside another match — is lowered with a per-arm ownership frame so only the taken arm's transient heap is materialized and freed exactly once. Each arm's owned value is moved out of the if/match cleanly, bound payloads borrow versus dup correctly, and the subject drops after the arms rather than via an unconditional rc_dec of an untaken arm. The resulting stdout, stderr, and exit code are byte-identical to the native Rust target across both taken-arm cases, with no leaks or rc_dec(0) traps."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/heap_result_if_append.almd", class = "fixture" },
  { path = "spec/wasm_cross/heap_result_if_bind_chain.almd", class = "fixture" },
  { path = "spec/wasm_cross/heap_result_if_match_arm_frame.almd", class = "fixture" },
  { path = "spec/wasm_cross/heap_result_if_record_arm.almd", class = "fixture" },
  { path = "spec/wasm_cross/heap_result_if_str_int.almd", class = "fixture" },
  { path = "spec/wasm_cross/heap_result_tuple_return.almd", class = "fixture" },
  { path = "spec/wasm_cross/heap_result_err_interp.almd", class = "fixture" },
  { path = "spec/wasm_cross/nested_match_heap_arm.almd", class = "fixture" },
  { path = "spec/wasm_cross/result_heap_err_match.almd", class = "fixture" },
  { path = "spec/wasm_cross/result_heap_ok_match.almd", class = "fixture" },
  { path = "spec/wasm_cross/str_result_heap_bind.almd", class = "fixture" },
  { path = "spec/wasm_cross/result_match_rewrap_modcall.almd", class = "fixture" },
]

[[contract]]
id        = "C-107"
spec      = "ALS-I3"
title     = "heap Result-of-tuple / Result-of-list Ok payloads round-trip and byte-match native"
statement = "Producing a Result whose Ok payload is a heap-bearing tuple — (String, Int), (Value, Int), (List[String], Int), or (List[Value], Int) — and then match-consuming it (binding the Ok tuple, destructuring it, printing its parts) emits stdout, stderr, and an exit code on the wasm32 path that are byte-identical to native Rust. Each shape uses its own recursive drop (Op::DropResultStrInt / DropResultValueInt / DropResultListStrInt / DropResultListValueInt, the latter two via value_core's tuple drops) so the Result wrapper, the tuple block, and every heap element inside slot0 are freed exactly once; a flat DropListStr would leak the inner String/Value/list elements. This holds leak-free at scale (loops and recursive accumulation) and on both the Ok and Err arms."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/result_str_int_tuple.almd", class = "fixture" },
  { path = "spec/wasm_cross/result_value_int_tuple.almd", class = "fixture" },
  { path = "spec/wasm_cross/result_list_str_int_tuple.almd", class = "fixture" },
  { path = "spec/wasm_cross/result_list_value_int_tuple.almd", class = "fixture" },
]

[[contract]]
id        = "C-108"
spec      = "ALS-M11"
title     = "Unwrap `!` and let-unwrap desugaring byte-match native in every position"
statement = "The Result-unwrap operator `!` and its `let`-bound forms desugar to an Err-early-return match whose observable native↔wasm behavior is byte-identical for every syntactic position: nested in a call/ctor argument or BinOp operand (ANF-lifted before lowering), inside an `if` arm (lifted out of the arm), bound by `let s = e!`, destructured by `let (a, b) = e!`, and used before a self-recursive tail call (real recursion, not TCO). The Ok payload continues into the pushed-into-ok-arm continuation and the Err propagates as the function's return; a scalar `?? d` fallback in an operand position evaluates correctly instead of silently reading the default. Across all fixtures the wasm32 path emits the same stdout, stderr, and exit code as native Rust (v0==v1)."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/unwrap_in_callarg.almd", class = "fixture" },
  { path = "spec/wasm_cross/unwrap_in_if_arm.almd", class = "fixture" },
  { path = "spec/wasm_cross/unwrap_in_recursion.almd", class = "fixture" },
  { path = "spec/wasm_cross/unwrap_or_operand.almd", class = "fixture" },
  { path = "spec/wasm_cross/let_unwrap_propagation.almd", class = "fixture" },
  { path = "spec/wasm_cross/destructure_let_unwrap.almd", class = "fixture" },
]

[[contract]]
id        = "C-109"
spec      = "ALS-I3"
title     = "Self-hosted base64 encode byte-matches canonical / native on the v1 wasm path"
statement = "The self-hosted almide/base64 encode path, compiled to v1 wasm32, prints stdout byte-identical to native and to canonical base64 vectors. This composes a const module-global alphabet string (Init::Str), a tail-recursive String-accumulator encode loop with chained multi-concat (acc + c0 + c1 + c2 + c3), let-bound heap-result `if` expressions for the pad characters, and `string.get ?? \"\"` Option fallbacks. Encoding \"a\", \"ab\", \"hello\", and \"Many hands make light work.\" yields the standard padded base64 output on both backends."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/base64_encode.almd", class = "fixture" },
]

[[contract]]
id        = "C-110"
spec      = "ALS-M13"
title     = "In-place bytes.push mutation accumulator byte-matches native on v1 wasm"
statement = "An in-place `bytes.push(mut buf, val) -> Unit` mutation accumulator inside a `var buf; while { bytes.push(buf, x) }` loop (the bigint.from_int shape) produces stdout byte-identical to native on the v1 wasm path. Because v1 has value semantics, the MIR lowering rewrites each `bytes.push(buf, x)` to the functional rebind `buf = bytes.append(buf, x)` using bytes_core's self-hosted append, preserving observable results. For input 0x010203 it reports length 3 and first byte 3 on both backends."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/bytes_push_inplace.almd", class = "fixture" },
]

[[contract]]
id        = "C-111"
spec      = "ALS-T7"
title     = "Module-level const heap globals initialize and read identically on v1 wasm and native"
statement = "Const module-level heap globals materialize as static DATA with no runtime initializer call, so functions referencing them lower and read byte-identical to native on the v1 wasm path. This covers a string literal global (Init::Str), an int-list literal, and a `bytes.from_list([int literals])` global (Init::Bytes), as in the AES S-box / base64 alphabet shape. Slicing the const string and reading `bytes.read_u8` from the const byte table print the same stdout on both backends."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/module_global_const.almd", class = "fixture" },
]

[[contract]]
id        = "C-112"
spec      = "ALS-R5"
title     = "random.int draws stay in-range identically under the WASI entropy floor on v1 wasm"
statement = "The self-hosted `random.int(lo, hi)` reaches the WASI entropy capability (Capability::Entropy) via prim.random_get on the v1 wasm path, the first effectful capability beyond Stdout, and is callable only from an effect fn that declares Entropy. Because the drawn value is nondeterministic, the certified observable is the deterministic RANGE invariant: every draw satisfies lo <= r <= hi. Drawing random.int(10, 20) fifty times, all 50 draws fall in range and both backends print \"50\"."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/random_int_entropy.almd", class = "fixture" },
]

[[contract]]
id        = "C-113"
spec      = "ALS-M1"
title     = "Let-bound ADT/Result variant matched by tag byte-matches native on v1 wasm"
statement = "A let-bound user-function Result/ADT value that is subsequently matched by tag (the num_signed_base shape) executes the match by reading the variant tag instead of walling, producing stdout byte-identical to native on the v1 wasm path. Seeding the let-binding's variant param gives the bound var the same read-shape a direct call-argument position already has, including the harder case of a let-bound heap-result `if` whose tail-duplication desugar distributes the match into each arm. Matching ok/err over both directly-bound and if-distributed Results prints the same output on both backends."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/letbound_variant_match.almd", class = "fixture" },
]

[[contract]]
id        = "C-114"
spec      = "ALS-M1"
title     = "Matching an Option with a heap payload byte-matches native on v1 wasm"
statement = "Matching an Option whose Some-payload is a heap value (String/Value), as in `match list.first(xs) { none => A, some(key) => B }` from toml set_nested/append_aot, returns the correct branch byte-identical to native on the v1 wasm path. The Some-payload binds the heap handle as a borrow via heap_or_scalar_bind, gated on the Option[heap] subject tracked as a nested-owner, extending the prior lowering that only handled a Tuple Some-payload. picking from [\"hello\", \"world\"] yields \"hello\" and from [] yields the fallback \"empty\" on both backends."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/option_match_heap_payload.almd", class = "fixture" },
]

[[contract]]
id        = "C-115"
spec      = "ALS-M10"
title     = "Pipe into a block-bodied lambda producing a value byte-matches native on v1 wasm"
statement = "A pipe into an inline lambda (`x |> (n) => body`) is inlined by the frontend to a block bind `{ let n = x; body }` and lowered to the correct value on the v1 wasm path, byte-identical to native, fixing a prior silent miscompile that produced 0. This also exercises block-valued scalar binds (`let a = { let n = ...; ... }`), which v1 MIR previously mis-lowered to 0 and now lowers by running the block's statements then binding to the block tail. Single, chained, explicit-block, and block-bodied-lambda forms print 25, 26, 48, and 36 on both backends; a pipe-lambda in a call-argument position safely walls rather than mis-lowering."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/pipe_lambda_block_value.almd", class = "fixture" },
]

[[contract]]
id        = "C-116"
spec      = "ALS-I3"
title     = "v1 scalar-value lowering edges byte-match native (tail Bool literal, float.parse inf/nan)"
statement = "Two previously-diverging v1 scalar-value cases now produce stdout byte-identical to native. (1) A function whose body IS a bare Bool literal (`fn t() = true`, or a constant/param-ignoring predicate `(x) => true` for list.all/any/count) RETURNS its 0/1 — not the deferred-Const ZERO it used to fall into in tail position, which silently made every constant-true predicate FALSE (list.all((x)=>true) wrongly false). (2) float.parse accepts the IEEE 754 special values inf / infinity / nan (case-insensitive, optional sign) like Rust's strtod, building them by runtime division (1/0, 0/0) so the bit patterns match on both targets; junk still errs and normal floats are unaffected. Native is the oracle."
since     = "0.27.7"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/bool_literal_tail.almd", class = "fixture" },
  { path = "spec/wasm_cross/float_parse_special.almd", class = "fixture" },
]

[[contract]]
id        = "C-117"
spec      = "ALS-M6"
title     = "In-loop let-bound heap if/match is lifted to a tail helper and renders on v1"
statement = "A heap-result `if`/`match` bound to a `let`/`var` INSIDE a for-in/while body produces stdout byte-identical to native on the v1 trust-spine wasm path. The branch_lift pass (almide-optimize, the shared frontend pipeline) hoists the inline branch into a synthesized tail helper fn `__branch_lift_N(free_vars) -> T = <branch>` and rebinds the local to its call result — a proven shape (tail heap-result branch + let-bound call result), so v1 renders it where the inline branch walled (the flat cert could not attribute one scope-end drop to one-of-two exclusive arm-allocs). The existing MIR tail-duplication desugar handles the non-loop cases; this closes the loop-body residual (the #1 real-app v1 coverage lever — binary-search and kin). Native is the oracle; the lift is semantics-preserving (0 backend-split across the 3-way oracle)."
since     = "0.27.7"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/branch_lift_in_loop.almd", class = "fixture" },
]

[[contract]]
id        = "C-118"
spec      = "ALS-R5"
title     = "env.args works on WASM and matches native (argv[0] skipped)"
statement = "env.args() runs on the WASM target (previously a stub that always returned the empty list) and agrees with native. It maps to almide_rt_env_args = std::env::args() with argv[0] (the binary name) SKIPPED — only the real program args. On WASM it is built from the WASI args_sizes_get / args_get imports by the SAME builder process.args uses (crates/almide-mir/src/render_wasm_p3.rs), parameterized by a leading-argv skip count: env.args passes skip=1 (drop argv[0]); process.args passes skip=0 (full argv). The result List and each inner String are framed with the canonical 8-byte [len][cap][data] header. Verified byte-identical native == wasm on the argument COUNT: with no extra program args the underlying argv is [program] on both legs, so after the skip env.args() is the empty list (length 0) on both — an argv[0]-content-independent invariant, since argv[0] itself differs between a temp binary path and the wasm module path."
since     = "0.27.8"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/env_args.almd", class = "fixture" },
]

[[contract]]
id        = "C-119"
spec      = "ALS-M4"
title     = "effect-`!` inside a `for` loop body propagates Err and byte-matches native"
statement = "An effect-`!` (error propagation) inside a `for` loop body, in a function returning `Result[T, String]`, runs on the WASM target and agrees with native. Previously such a `!` WALLed at lowering (a mid-loop early-return makes the loop's owned iterable + iteration transients conditionally dropped — a double-free the flat ownership certificate cannot model). It is now a PURE IR→IR desugar (desugar_loop_unwrap) to a loop-carried error-flag + post-loop dispatch: `for x in xs { … e! … }; <post>` becomes `var __ef=false; var __ev=\"\"; for x in xs { if not __ef then { match e { ok(v)=>…, err($x)=>{__ef=true; __ev=$x ++ \"\"} } } else () }; if __ef then err(__ev) else <post>`. BYTE-IDENTICAL to early-return: once __ef is set the per-iteration guard skips all remaining effects, the loop terminates by exhausting xs (bounded — `for` only, never `while`), and the post-loop dispatch yields the propagated Err. The accumulator stores an OWNED copy (`$x ++ \"\"`, a fresh String) so the loop-carried slot certifies as the PROVEN i(id)m shape — the whole desugar reuses the existing loop-slot + heap-result-`if` machinery with NO new MIR op and NO certificate/Coq change (corpus-wall: ownership ACCEPT over all 4517 functions). Verified byte-identical native == wasm on both the all-ok path (the loop completes) and the err path (a mid-loop `!` short-circuits)."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/effect_unwrap_in_loop.almd", class = "fixture" },
]

[[contract]]
id        = "C-120"
spec      = "ALS-I3"
title     = "capturing filter_map with a conditional keep/skip arm body byte-matches native"
statement = "A CAPTURING `list.filter_map` whose inline lambda body is a variant `match` with a CONDITIONAL arm — an arm whose body is a Block and/or an `if c then some(rec) else none` (the porta load_porta_config `secrets` shape: `match val { some(s) => some(rec), none => { let b = …; if b then some(rec2) else none } }`) — runs on the WASM target and agrees with native. Previously the defunc `emit_filter_map_arm` handled only a bare `some(elem)` / `none` / empty-list arm body and WALLed any other shape (`list.filter_map with an unliftable/closure-list higher-order argument`). It now recurses into a Block arm body (lowering the leading `let`s as per-arm effects, captures resolving via value_of, heap temps freed at the arm frame end) and an `if` arm body (a UNIT keep/skip control structure — only the taken arm runs, its record built+stored into the SAME write-cursor result list, the cursor's increment in-place under unit_arm_depth), mirroring the proven `append_body_to_str_acc` Block/If handling for the str-accumulator path. NO merged heap value is bound (the record is materialized inside the taken arm), so no unsound flat-cert shape arises; NO new MIR op and NO certificate/Coq change (corpus-wall: ownership ACCEPT over all 4520 functions). Verified byte-identical native == wasm: the some-arm (key present), the none-arm's `if … then some` (conditional keep), and the none-arm's `else none` (skip) all produce identical output."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/filter_map_conditional_arm.almd", class = "fixture" },
]

[[contract]]
id        = "C-121"
spec      = "ALS-I1"
title     = "String pass-through fast paths hand back an owned (+1) reference"
statement = "String runtime fns whose fast path returns the INPUT string unchanged — `string.replace` with no occurrence, `string.replace_first` on a miss, `string.pad_start`/`string.pad_end` when the width is already satisfied, and `string.capitalize` of the empty string — hand the caller its OWN reference (SHARE +1), so a pipe chain of no-match links byte-matches native. The WASM emitter previously returned the input borrowed: every no-match link added an extra Dec of the same buffer, and a 3-link no-match `string.replace` chain (the svg `escape_attr` shape) under-flowed the refcount and trapped `__rc_dec` while native printed the correct output."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/string_passthrough_share.almd", class = "fixture" },
  { path = "spec/wasm_cross/zip_view_shape.almd", class = "fixture" },
]

[[contract]]
id        = "C-122"
spec      = "ALS-I1"
title     = "Value object ops allocate full list layout and share pairs with +1"
statement = "`value.merge`, `value.pick`, `value.omit`, `json.keys`, and the ok path of `value.get`/`value.field` build their result from an existing object's pairs: the result list uses the FULL `[len][cap][data]` layout and owns a +1 on every shared pair pointer, key string, and ok payload. The WASM emitter previously allocated the pair list 4 bytes short (no cap word — the last element write clobbered the adjacent heap block), left cap uninitialized, and copied the pointers borrowed: `value.merge(a, b)` read back `{\"\":null}`, `list.get(json.keys(v), 0) ?? d` freed the object's live key, and the toml array-of-tables parse silently dropped fields on wasm while native was correct."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/value_object_ops_share.almd", class = "fixture" },
]

[[contract]]
id        = "C-123"
spec      = "ALS-M2"
title     = "Record spread shares copied heap fields and alias overrides with +1"
statement = "`{ ...base, field: v }` byte-copies the base record, so every duplicated heap-field pointer is reachable from two records: the copy owns a +1 per non-overridden heap field, and a borrowed-alias override (a Var) is retained under the same `emit_stored_field` +1 rule as a plain `Record` field. The WASM emitter previously copied both borrowed: the svg `doc` shape (`{ ...piped_base, children }`) rendered `<svg/>` with its attrs Map freed once the piped base temp was dropped, then trapped `__rc_dec` at the scope-end drops, while native printed the full element."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/spread_record_share.almd", class = "fixture" },
]

[[contract]]
id        = "C-124"
spec      = "ALS-C3"
title     = "Value equality is deep structural, mirroring the native PartialEq"
statement = "`Value == Value` (and `!=`) compares deep structure exactly like the native derived `PartialEq` on `enum Value`: strict tag match (no Int/Float widening), string payloads by content, arrays elementwise, and objects as IN-ORDER pair lists. The WASM emitter previously fell through to the childless-record equality arm and compared POINTERS — two separately-built `json.null()`s were unequal, so `(json.get(f, \"import\") ?? json.null()) != json.null()` classified every function as a JS import on wasm (almide-wasm-bindgen generate_esm) while native said none."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/value_deep_eq.almd", class = "fixture" },
]

[[contract]]
id        = "C-125"
spec      = "ALS-C5"
title     = "bytes.set has value semantics — never observable through the input"
statement = "`bytes.set(b, i, v)` mirrors the native oracle `almide_rt_bytes_set` exactly: it CLONES and returns a new Bytes, so the mutation is never observable through `b` or any binding sharing its buffer — including a fn param (the caller's value stays intact) and a local alias. The wasm emitter previously stored in place unconditionally, so `encrypt_block(iv, …)` inside the aes cfb8 loop clobbered the caller's iv and produced wrong NIST vectors while native was correct. The in-place fast path survives only for the provably-unaliased `x = bytes.set(x, …)` self-update (AliasCowPass marks copy-aliased and param-reachable targets, vetoing the peephole), which is unobservable — byte-identical to native."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/bytes_set_value_semantics.almd", class = "fixture" },
]

[[contract]]
id        = "C-126"
spec      = "ALS-M5"
title     = "Nested-lambda HOF params keep their inference link (no literal sig-generic pin)"
statement = "An unannotated lambda param inside another lambda resolves through the union-find like any inference var: the #653 expected-type pin never writes the CALLEE's own unbound generic (`A` when the collection arg is itself an unresolved var) into the param, while a pin that denotes an IN-SCOPE rigid generic (`names[T: Labelled]` calling `list.map(xs, (e) => e.name())`) still applies. Previously the literal `A` disconnected the param from the solver and the inner `let sq = list.map(row, (x) => x*x)` defaulted its element type to Int — native rendered `Vec<i64>`/`Fn(f64) -> i64` (E0308/E0271) and wasm emitted an invalid module (i64 vs f64), the nn variance_rows shape."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/nested_hof_lambda_inference.almd", class = "fixture" },
  { path = "spec/wasm_cross/protocol_ufcs_inferred_lambda.almd", class = "fixture" },
]

[[contract]]
id        = "C-127"
spec      = "ALS-M5"
title     = "unwrap_or sizes its payload from the default when the chain type is unresolved"
statement = "`option.unwrap_or` and `result.unwrap_or` derive their payload width from the DEFAULT argument whenever the Option/Result inner type arrives unresolved from a generic chain (`list.find |> option.map |> option.unwrap_or`, the nn gguf get_alignment shape). The wasm emit previously picked the pointer-width arm from the unresolved inner while the default arm emitted its real width — an `if (result i32)` block with an `i64.const` arm that failed structural validation; native was correct. `unwrap_or(o: Option[T], d: T)` makes the default's type authoritative."
since     = "0.27.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/generic_chain_unwrap_or.almd", class = "fixture" },
]

[[contract]]
id        = "C-128"
spec      = "ALS-T17"
title     = "datetime.format substitutes strftime specifiers identically on every backend"
statement = "`datetime.format(ts, pattern)` substitutes the strftime specifiers %Y %m %d %H %M %S with the zero-padded civil fields (year 4-wide, others 2-wide) via the SAME sequential `string.replace` order on all three backends — native Rust, v0 wasm emit, and the v1 self-hosted module — so the output is byte-identical. Previously the default v0 wasm arm was a stub returning `int.to_string(ts)` (pattern ignored) while native/self-host substituted a different `YYYY/MM/DD` token set that the documented `%Y-%m-%d` never matched (#747). `%` is special only immediately before a recognized specifier; there is no `%%` escape. SCOPE: year 0..9999, the same documented edge as to_iso."
since     = "0.28.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/datetime_format.almd", class = "fixture" },
]

[[contract]]
id        = "C-129"
spec      = "ALS-T4"
title     = "list.chunk / list.windows non-positive sizes: negative keeps the promoted norm, zero aborts in the T6 form"
statement = "A NEGATIVE size keeps the historical-`as usize`-wrap behavior promoted to the ALS-T4 norm — `list.chunk(xs, n<0)` returns the whole list as ONE chunk (an empty list stays empty) and `list.windows(xs, n<0)` returns the empty list — byte-identical on both targets. A ZERO size is a domain error: `chunk` aborts with `Error: chunk size must be positive`, `windows` with `Error: window size must be positive`, one stderr line + exit 1 on BOTH targets per the ALS-T6 termination convention. Previously `n == 0` leaked Rust's raw `chunks(0)`/`windows(0)` panic natively (exit 101) while wasm trapped on chunk and SILENTLY returned len+1 empty windows for `windows(xs, 0)` — a value-vs-abort divergence."
since     = "0.28.4"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/list_chunk_windows.almd", class = "fixture" },
  { path = "spec/wasm_cross/list_chunk_zero.almd", class = "fixture" },
  { path = "spec/wasm_cross/list_windows_zero.almd", class = "fixture" },
  { path = "spec/wasm_cross/list_window_zero.almd", class = "fixture" },
]

[[contract]]
id        = "C-130"
spec      = "ALS-I1"
title     = "option/map combinators hand back OWNED heap results (no bare pass-through handles)"
statement = "A combinator whose result can be one of its inputs' heap values returns that value CO-OWNED (+1), never as a bare aliased handle: `option.unwrap_or_else`'s Some path acquires the payload before the Option temp's recursive drop releases it, and `map.fold` acquires a heap init when storing the accumulator and releases the previous accumulator after every closure call — so the zero-iteration fold hands back an owned handle exactly like the N-iteration one. Previously a some(Map) payload was returned bare and read after free (garbage lengths — a 2-entry map printed as hundreds of `false`), and an empty-subject fold's caller double-freed the init against its own binding (rc sentinel trap; with a `map.get_or` subject the allocator reuse printed the init as `[:]`). These were fuzz #727's dominant clusters; all 13 campaign representatives replay CLEAN with these two acquisitions in place. Same share-+1 discipline as the #668 pass-through family."
since     = "0.28.5"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/option_unwrap_or_else_heap.almd", class = "fixture" },
  { path = "spec/wasm_cross/map_fold_heap_acc.almd", class = "fixture" },
]

[[contract]]
id        = "C-131"
spec      = "ALS-C5"
title     = "Loop-rebuilt buffers are O(n): COW guards only LIVE aliases, and LICM never hoists heap allocations"
statement = "A loop that rebuilds a heap container via in-place mutation (`list.push` per element, the buffer dropped or swap-carried each round) costs O(n) bytes and byte-matches native at 152k elements. Two static rules make the COW guard precise without weakening it: (1) LICM never hoists a heap-typed result out of a loop — hoisting the `[]` allocation had aliased every iteration to one shared `__licm` temp, making the guard load-bearing on every push; (2) a DIRECT whole-var copy (`cur = merged`) whose source has no read or mutation after the edge — same-loop sources are re-declared before any use — is a MOVE and adds no alias edge. A LIVE alias (the source read after the copy) still clones on mutation, and param-reachable targets remain unconditionally guarded, so value semantics are unchanged (alias_cow, bytes_set_value_semantics). Previously the unconditional per-push clone was O(n²) and drove the 32-bit heap arithmetic out of bounds past ~128 KiB per round — the merge-sort OOM at n≳8000 (#696)."
since     = "0.28.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/loop_buffer_churn.almd", class = "fixture" },
  { path = "spec/wasm_cross/loop_push_trailing_increment.almd", class = "fixture" },
]

[[contract]]
id        = "C-132"
spec      = "ALS-M13"
title     = "mut parameters of reallocating containers persist to the caller at every call position"
statement = "A `mut` parameter of a REALLOCATING container (List, String, Bytes) mutated in place inside the callee persists to the caller on wasm exactly as native, at EVERY call position — statement, Bind/Assign RHS, nested expression, loop bodies — and for both bare-var and record-FIELD arguments (`push9(b.items, 7)` writes the buffer back into the field). Value-returning callees return an (orig, buffer) tuple destructured at the call site; Unit callees return the buffer. Previously only Unit callees in statement position were lowered: `let i = push9(v, 7)` pushed into a discarded copy (wasm len=1 vs native len=3, silent len=0 from empty; a real MLP printed loss 0.0), the #705 class."
since     = "0.28.6"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/mut_heap_param.almd", class = "fixture" },
]

[[contract]]
id        = "C-133"
spec      = "ALS-R5"
title     = "env.get observes the host environment identically on native and wasm"
statement = "`env.get(name)` returns some(value) for a variable present in the host process environment and none for an absent one, with the SAME value bytes on native (std::env::var) and wasm (WASI environ_get; the runner passes the host environment through `wasmtime -S inherit-env=y`). Previously the wasm emitter had NO dispatch for env.get at all — it ICE'd (a panic, not a wall) on every program that called it, and even with a dispatch the guest saw an EMPTY environment because the runner never forwarded it."
since     = "0.29.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/env_get.almd", class = "fixture" },
]

[[contract]]
id        = "C-136"
spec      = "ALS-M13"
title     = "In-place place mutations persist to the subsequent read on both targets"
statement = "An in-place PLACE mutation — record field assign (`r.f = v`, scalar or heap field, on all-scalar and mixed records), map insert (`m[k] = v`), list element store (`xs[i] = v`, scalar or heap element), and a heap-var reassignment inside an executing branch arm (`if let v = x { out = f(v) }`) — persists to every subsequent read identically on native and wasm. Previously the v1 leg lowered these to ONLY the `MakeUnique` COW guard (or nothing at all) and silently ELIDED the write (a caps-counting-era deferral left executable on the STRICT output path): the assign was a no-op and reads returned the pre-assign value while native mutated — a silent wrong value on the verified default (shipped in 0.29.0; surfaced by the v1 test-runner flip, which ran the long-v0-only test-block corpus on v1 for the first time). Now the scalar-slot stores execute on v1 (`ListSetScalar` on the uniform aggregate block) and every other shape WALLS honestly (v0 emits the correct bytes) — an elided write is never executable."
since     = "0.30.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/place_mutation.almd", class = "fixture" },
]

[[contract]]
id        = "C-135"
spec      = "ALS-M4"
title     = "Declared-Unit effect fn ABI agrees between def and every call site"
statement = "A declared-Unit `effect fn` compiles to a module in which its definition and EVERY call site agree on the ABI, on both targets and across module boundaries: a never-err callee is void/raw (callers strip the lifted `Try`/Result type), a can-err or stmt-propagating callee carries the lifted `Result[Unit, String]` ABI, and any Result-ABI fn whose body tail is Unit-typed materializes the promised `ok(())` block as its return. Previously the v1 wasm leg classified CALLERS by the name-keyed ABI registries but emitted the DEF by body shape: a Unit-tailed Result-ABI fn came out VOID while callers `local.set` the promised Result handle — a structurally invalid module (an honest build-time refusal, never wrong bytes) — and the never-err rewrites ran with main-only sets, so cross-module and module-sibling callers never stripped at all (the #786 class)."
since     = "0.30.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/unit_effect_abi.almd", class = "fixture" },
  { path = "spec/integration/modules/cross_module_unit_effect_test.almd", class = "fixture" },
]

[[contract]]
id        = "C-134"
spec      = "ALS-T10"
title     = "Vendored-libm atan / tanh are byte-identical cross-target"
statement = "`math.atan` and `math.tanh` route through the SAME vendored musl-libm reference on both targets (native runtime/rs/src/libm_p4.rs — atan/expm1/tanh; wasm stdlib/math_atan.almd + stdlib/math_tanh.almd, self-hosted transcriptions of the same reference), so every result is byte-identical — printed via the (itself byte-identical) Dragon4 `float.to_string`. Native previously carried a PLATFORM `f64::atan` (dead code, never exposed in stdlib); it now runs the vendored fn. Every atan subrange (7/16, 11/16, 19/16, 39/16, 2^-27 tiny, 2^66 huge) and every tanh/expm1 range split (log(5/3)/2, log(3)/2, |x|>20, subnormal, k = 0/±1/>56 scaling) is exercised; identities atan(1) = pi/4, atan(inf) = pi/2 and the upstream expm1(1.1) sanity vector hold. Differential fuzz certifies the continuous input space."
since     = "0.30.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/math_atan_tanh.almd", class = "fixture" },
  { path = "tests/libm_atan_tanh_cross_target_test.rs", name = "atan_tanh_cross_target_random_fuzz", class = "fuzz", n = 3000 },
]

[[contract]]
id        = "C-137"
spec      = "ALS-R6"
title     = "Relative fs paths resolve against the host CWD on wasm"
statement = "Every fs operation given a RELATIVE path (`fs.read_text(\"Cargo.toml\")`, `fs.stat(\"spec/x.almd\")`, write/exists/list_dir/…) resolves it against the HOST current working directory on the wasm target, byte-identically to native std::fs. WASI has no cwd, so the wasm runtime normalizes the path first (`$path_norm`): an absolute path is already the guest path, a relative one is joined onto \"$PWD/\" (the PWD environment variable, inherited by every harness via `-S inherit-env=y`; ALMIDE_CWD wins over it), and the result then goes through the SAME preopen resolution both forms share (C-042). Previously a relative path resolved against the preopen ROOT and failed (NOENT / \"stat failed\") while native succeeded — a silent native⇄wasm divergence that the native test fallback masked. fs.stat additionally stats into an 8-ALIGNED scratch buffer (WASI path_filestat_get demands alignment the self-host's Bytes scratch cannot guarantee) and copies the 64-byte filestat back."
since     = "0.31.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/fs_relative_path.almd", class = "fixture" },
  { path = "spec/stdlib/fs_stat_test.almd", class = "fixture" },
]

[[contract]]
id        = "C-138"
spec      = "ALS-I3"
title     = "ok/err ctor with a stdlib-call payload materializes the real value"
statement = "A Result ctor whose payload is a PURE stdlib Module call (`ok(float.to_fixed(x, 4))`, `err(string.to_upper(s))`, `ok(int.to_string(n))`) bound or returned directly materializes a REAL Result block on the v1 leg — the payload call executes and its fresh owned String moves into the ctor slot, for all three Result layouts (heap-Ok ok, heap-Ok err, scalar-Ok len-as-tag err). Previously the un-admitted Module-call payload fell to the deferred Opaque and the ZEROED block reached observed output as `ok(\"\")` — even for err(), whose tag zeroed too (the silent-wrong-value class; differential-fuzz seed 1784352208133210990 index 323/768/904)."
since     = "0.31.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/result_ctor_call_payload.almd", class = "fixture" },
]

[[contract]]
id        = "C-154"
spec      = "ALS-T6"
title     = "clamp with an invalid range aborts in the T6 form"
statement = "`int.clamp(n, lo, hi)` with lo > hi, and `float.clamp` with lo > hi OR a NaN bound (`!(lo <= hi)` covers both in one IEEE comparison), abort with `Error: clamp requires min <= max` — one stderr line + exit 1, byte-identical on both targets. Previously native leaked the raw Rust clamp panic (exit 101, the \"min > max\" banner) while wasm's un-checked min/max chain SILENTLY returned a value (differential-fuzz seed 20260718 index 5). All four consumers carry the check: the v1 self-hosts via prim.die, the native runtime wrappers, the v0 emit arms via the interned-message div_trap, and the interp bridge via Flow::Abort."
since     = "0.31.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/int_clamp_inverted.almd", class = "fixture" },
  { path = "spec/wasm_cross/float_clamp_invalid.almd", class = "fixture" },
]

[[contract]]
id        = "C-155"
spec      = "ALS-T6"
title     = "to_fixed with out-of-domain decimals aborts in the T6 form"
statement = "`float.to_fixed(n, decimals)` with decimals outside 0..=4096 aborts with `Error: to_fixed requires decimals in 0..=4096` — one stderr line + exit 1, byte-identical on both targets. Previously a negative count reinterpreted as usize on native (the raw format! capacity panic, exit 101) while wasm printed the empty string for a small negative or trapped OOM for i64::MIN (differential-fuzz seed 20260718 index 49). The bound was then 0..=1000000, which was itself NOT total: Rust's format! caps runtime precision at u16::MAX, so `to_fixed(1.5, 1000000)` panicked raw on native (101) while the wasm dragon expansion trapped (134) — differential-fuzz seed 1784512190387680000. An f64's exact expansion never exceeds ~1074 fractional digits, so 0..=4096 is generous AND total. The guard's consumers: the v1 self-host via prim.die, the native runtime wrapper, and the interp bridge via Flow::Abort. In-domain decimals stay total and byte-identical."
since     = "0.31.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/to_fixed_domain_abort.almd", class = "fixture" },
  { path = "spec/wasm_cross/to_fixed_domain_abort_hi.almd", class = "fixture" },
  { path = "spec/wasm_cross/to_fixed_wide_precision.almd", class = "fixture" },
]

[[contract]]
id        = "C-153"
spec      = "ALS-T18"
title     = "Non-test assert failures abort in the T6 form on both targets"
statement = "A failing `assert` / `assert_eq` / `assert_ne` OUTSIDE a test block aborts with a STRUCTURED stderr block + exit code 1, byte-identical across targets: `Error: assertion failed` followed by one `  key: value` line each — `  at: line <N>`, `  expected: <r>` (`!= <l>` for ne), `  found: <l>`; `assert(c, msg)` puts the message on the header (`Error: assertion failed: <msg>`) and carries only `at:`. Operand display per ALS-R2, operands evaluated exactly once (the failure block re-references bound temps). FIELD ORDER IS PART OF THE PROMISE: `expected` precedes `found` so the one field with no terminator is LAST, which is what lets `almide test` parse a multi-line value back out and render a diff (#1313, FeedbackEval arXiv 2504.06939 — structured expected/found is the feedback shape that repairs). The `at:` line is dropped when the call has no span, a property of the shared frontend lowering and never of the target. Implemented as ONE frontend-lowering desugar (if + eprintln + process.exit(1)) that native, v0-wasm, v1-wasm, and the interp all inherit. It was previously ONE line (`left = <l>, right = <r>`) with no source site; before that native leaked the raw Rust panic (exit 101 + the panic banner) while wasm printed a value-less line with exit 1 for assert_eq and a BARE TRAP (exit 134) for assert/assert_ne (differential-fuzz seed 20260718 index 10). In passing: the bare `eprintln` builtin — which the desugar rides — was UNLINKED on the v1 leg and ICE'd the v0 emitter; it is now a registered self-host (fd-2 print_str twin) and a shared parametrized v0 runtime fn."
since     = "0.31.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/assert_abort_eq.almd", class = "fixture" },
  { path = "spec/wasm_cross/assert_abort_ne.almd", class = "fixture" },
  { path = "spec/wasm_cross/assert_abort_msg.almd", class = "fixture" },
  { path = "spec/wasm_cross/assert_abort_multiline.almd", class = "fixture" },
]

[[contract]]
id        = "C-147"
spec      = "ALS-M12"
title     = "list.unique_by routes by key repr — String keys dedupe by content"
statement = "`list.unique_by` is routed by the KEY closure's result repr (the CallIndirect table-type axis): a scalar key over scalar elements rides the base impl, a String key over scalar elements rides the `_sk` twin (content equality via string.eq, first-kept, O(n^2) recompute of the pure key), and any other repr routes to the UNLINKED `_x` wall. Previously every instantiation linked the Int-typed base impl whose scalar `(Int) -> Int` table type TRAPPED on a String-key closure (\"indirect call type mismatch\", differential-fuzz seed 20260718 index 9 — the unique_by edition of the zip_with class C-141)."
since     = "0.31.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/list_unique_by_str_key.almd", class = "fixture" },
]

[[contract]]
id        = "C-148"
spec      = "ALS-M12"
title     = "list.scan stores each intermediate at the accumulator's own width"
statement = "`list.scan` is routed by the ACC repr (the closure's result AND the output element): a scalar acc rides the base impl, a String acc over scalar elements rides the `_str` twin on the v1 leg (each intermediate is the closure's fresh owned String MOVED into its slot, borrowed back for the next step), and any other repr walls (`_x`). On the v0 leg the per-step acc store switches on the accumulator's valtype — the old fallback used i64_store for every non-F64 acc, so an i32 String handle failed validation (\"expected i64, found i32\", differential-fuzz seed 20260718 index 259) and would have smeared 8 bytes over a 4-byte-stride slot."
since     = "0.31.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/scan_heap_acc.almd", class = "fixture" },
]

[[contract]]
id        = "C-149"
spec      = "ALS-I1"
title     = "unwrap_or_else hands back a co-owned heap Ok payload"
statement = "The unwrap family with a HEAP payload hands the caller a CO-OWNED (+1) reference — on BOTH wasm legs. v0: `result.unwrap_or_else`'s input Result temp's drop releases its payload at scope end, so handing back the bare `payload@4` was a use-after-free — `unwrap_or_else(result.collect([...]), f)` read the freed list as `[]` (differential-fuzz seed 20260718 index 590). `option.unwrap_or` / `result.unwrap_or`: BOTH branches — the kept payload (still owned by the input temp) and a Var default (a borrow) — rc_inc before returning; either un-inc'd double-freed at scope end (__rc_dec trap after correct output, fuzz index 149's `ok(result.unwrap_or(..))` chain). `option.to_list`: the new list CO-OWNS the payload the Option still owns — the un-inc'd alias double-freed on a nested-Option payload (fuzz index 338). `option.to_result`: the ok() arm shares the kept payload the same way — un-inc'd it double-freed a nested-Result payload (fuzz index 937). v1: `option.or_else` with a heap payload routes to the `option.or_else_h` twin whose KEPT arm Dups the payload into the rebuilt some() — the scalar twin raw-copied the handle un-owned, the same double free (fuzz index 622). The option unwrap_or_else arm carried this share-+1 discipline first (#727 cluster 5). The CUSTOM-E `option.to_result` (#1114's typed-error route) rides the same discipline through the `_ve` twin: the err arm CO-OWNS the caller's error block (payload-type-independent handle physics), where the type-blind registry link previously served the (Int?, String) body to every scalar-A instantiation and its `__copy_str` read a VARIANT error block as string bytes — the match over the result dispatched on the corrupt copy and the err path printed NOTHING while native printed the payload (the shim-v1 failure mode selfhost-link-v2.md names). A HEAP-payload custom-E instantiation routes to an unregistered name and WALLS honestly instead of silently corrupting."
since     = "0.31.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/uoe_heap_ok_share.almd", class = "fixture" },
  { path = "spec/wasm_cross/nested_unwrap_or_share.almd", class = "fixture" },
  { path = "spec/wasm_cross/to_result_nested_share.almd", class = "fixture" },
  { path = "spec/wasm_cross/to_result_custom_e.almd", class = "fixture" },
]

[[contract]]
id        = "C-150"
spec      = "ALS-C5"
title     = "Ctors over a heap var are value copies — the var stays live"
statement = "A Result/Option ctor wrapping a heap VAR takes a CO-OWNED reference (Dup) on the v1 leg, never the var's own handle: `let a = ok(r0); let b = ok(r0)` yields two real values and r0 stays readable afterward — value semantics, byte-identical to native. Previously the first ctor MOVED the var's handle out of the live set, so a SECOND ctor over the same var found nothing and deferred to the zeroed Opaque, printing `ok(\"\")` (differential-fuzz seed 20260718 index 248). The borrow-then-Dup discipline the param-payload arm already used, extended to owned locals."
since     = "0.31.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/ctor_var_copy_semantics.almd", class = "fixture" },
]

[[contract]]
id        = "C-151"
spec      = "ALS-M5"
title     = "Result combinators with a heap-Ok RESULT never link the scalar impl"
statement = "`result.map`/`map_err`/`flat_map` whose RESULT type is heap-Ok while the INPUT is scalar-Ok (`result.map(r, (v) => some(v))` — `Result[Int, String]` → `Result[Option[Int], String]`) route to the UNLINKED `_x` name — a deterministic render wall. The scalar impl's i64-result closure table type mismatches the heap-result closure AND its len-as-tag rebuild is the wrong OUTPUT layout: the un-routed link printed `some(1791929075367945)` for `some(0)` (differential-fuzz seed 20260718 index 647)."
since     = "0.31.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/result_wall_escalations.almd", class = "fixture" },
]

[[contract]]
id        = "C-152"
spec      = "ALS-I3"
title     = "An un-admitted heap call payload in a ctor walls, never zeroes"
statement = "A Result/Option ctor whose payload is a HEAP-typed call the materializer did not admit (`ok(float.parse(s))` — a Module call returning a nested Result) WALLS the function on the v1 leg instead of deferring to the flat Opaque: the zeroed block read `ok(0)` while native printed `err(\"invalid float literal\")` (differential-fuzz seed 20260718 index 888). The C-138 admission family's honest-wall tail — admission with an exact nested drop is a follow-up; the bind-net List/Map guard already existed, this extends it to every heap call payload and to the err() ctor."
since     = "0.31.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/result_wall_escalations.almd", class = "fixture" },
]

[[contract]]
id        = "C-143"
spec      = "ALS-I3"
title     = "Ctor if-payloads materialize the taken arm"
statement = "A Result/Option ctor whose String payload is a heap-result IF/MATCH (`some((if b then r0 else r0))`, `ok((if c then a else b))`, err twin, len-as-tag err twin) EXECUTES the taken arm on the v1 leg and moves the one owned result into the ctor slot. Previously the un-admitted if-payload fell to the deferred Opaque: the zeroed option READ `none` while native printed `some(\"5\")` — a silent flip (differential-fuzz seed 1784352208133210990 index 858)."
since     = "0.31.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/ctor_if_payload.almd", class = "fixture" },
]

[[contract]]
id        = "C-144"
spec      = "ALS-I3"
title     = "A scalar-list literal never observes as a silent empty list"
statement = "A NON-EMPTY scalar-element list literal bound to a let either MATERIALIZES a real populated block on the v1 leg or WALLS the function (v0 emits it) — it never defers to the flat Opaque whose every observation reads `[]`. Previously an element outside the executable subset (a nested inadmissible-HOF chain) deferred the WHOLE list: `${r4}` printed `[]` while native printed the values (differential-fuzz index 198/659), while the same chain in BIND position walled honestly — the element position now matches the bind position's honesty. The heap-element twin gate (non-empty List[heap] literal) already existed; this is its scalar twin."
since     = "0.31.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/list_element_hof_chain.almd", class = "fixture" },
]

[[contract]]
id        = "C-145"
spec      = "ALS-M5"
title     = "Mono-suffixed stdlib combinator names route by their base name"
statement = "A stdlib Module call whose name the monomorphizer suffixed with its instantiation (`result.or_else__Int_String_String`) routes through the self-host registry by its BASE name — the suffix carries no information the router needs (the instantiation's types are already in the arg/result types, which the repr-keyed gates consult). Previously the suffixed form fell through every router arm to an UNLINKED dotted name and walled the using function at render — `result.or_else` walled at EVERY instantiation, capturing and non-capturing alike (differential-fuzz index 198's recovery chain)."
since     = "0.31.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/list_element_hof_chain.almd", class = "fixture" },
]

[[contract]]
id        = "C-146"
spec      = "ALS-I1"
title     = "A lifted closure returning a captured alias hands out a co-owned reference"
statement = "On the v0 leg, a lifted closure (`__closure_N`) whose body RETURNS a bare CAPTURED heap var (`(v) => s1` — the or_else recovery shape, chased through the conversion's nested bind blocks) rc_incs the env handle before returning: every consumer treats a closure result as OWNED, so handing out the borrowed alias un-inc'd double-freed the block at scope end with the still-owning binding — a __rc_dec trap AFTER correct output (differential-fuzz index 198: wasm run failed while native succeeded). The #666/#668 share discipline, closure-return edition; a param or computed tail keeps its existing balance."
since     = "0.31.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/list_element_hof_chain.almd", class = "fixture" },
]

[[contract]]
id        = "C-140"
spec      = "ALS-T15"
title     = "float.round preserves the sign of a zero result"
statement = "`float.round` matches native `f64::round` bit-for-bit on the sign of a zero result: round(-0.0) and round(-0.4) are -0.0 (printed `-0`), round(0.4) is +0.0; every nonzero result keeps half-away-from-zero. Previously the v1 self-host branched on `x >= 0.0` — TRUE for -0.0 under IEEE — so round(-0.0) took the positive branch and printed `0` while native kept `-0` (differential-fuzz seed 1784352208133210990 index 67/655). copysign now carries x's sign onto the magnitude."
since     = "0.31.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/float_round_negzero.almd", class = "fixture" },
]

[[contract]]
id        = "C-141"
spec      = "ALS-M12"
title     = "list.zip_with routes by element repr — String zips work, no wrong-typed link"
statement = "`list.zip_with` is routed by the RESULT element repr (the one axis of the closure's CallIndirect table type — params ride the widened i64 slots uniformly): a scalar result element rides the base impl (heap SOURCE elements pass as borrowed handles, never copied into the flat result), the (String, String → String) triple rides the `_str` twin (borrow the pair via load_str, MOVE the closure's fresh result in — the list_map_str discipline), and any other heap result element routes to the UNLINKED `_x` wall. Previously every instantiation linked the Int-typed base impl, whose scalar $closure_fn2 table type TRAPPED on a heap-result closure (\"indirect call type mismatch\", differential-fuzz index 65) — capturing and non-capturing alike — and whose raw slot copies would alias handles un-owned."
since     = "0.31.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/list_zip_with_typed.almd", class = "fixture" },
]

[[contract]]
id        = "C-142"
spec      = "ALS-M5"
title     = "result.unwrap_or_else is valid wasm at the Float instantiation on both legs"
statement = "`result.unwrap_or_else` over a `Result[Float, String]` compiles to structurally valid wasm on BOTH legs and returns the exact payload/closure result. The v0 emitter's inline expansion carried I64 and i32-fallback cases only: a Float Ok payload emitted an `if i32` block around an f64 closure result and an i32 payload load — a structurally invalid module that the validator refused at build time while native ran (differential-fuzz index 96; surfaced when a heap-accumulator fold walled main off the v1 leg). The expansion now carries the F64 case (f64 block type + f64 payload load), mirroring the option twin that already had it."
since     = "0.31.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/result_uoe_float.almd", class = "fixture" },
]

[[contract]]
id        = "C-139"
spec      = "ALS-I3"
title     = "Heap-Ok Result value combinators keep tag and payload"
statement = "The result VALUE combinators (`map`, `map_err`, `flat_map`, `unwrap_or_else`, `to_option`, `to_err_option`) applied to a heap-Ok `Result[String, String]` (the cap-as-tag block: payload handle @12, tag @16) preserve the tag and payload byte-identically to native: the `Result[String, String]` instantiation routes to `_h` twins that read tag@16 and rebuild through the typed ctors, and every OTHER heap-Ok instantiation (including `filter`/`or_else`/`flatten`/`to_list`/`zip`) routes to an UNLINKED `_x` name — a deterministic render wall, never a wrong-typed link. Previously the len-as-tag scalar impls read len@4 (always 1 for a heap-Ok block), took every ok() for an Err, and rebuilt the WRONG layout: map/map_err/flat_map swapped or replaced payloads (`ok(\"abc\")` became the closure's constant), to_option/to_err_option flipped some/none, and unwrap_or_else emitted invalid wasm (fuzz index 904 via map_err)."
since     = "0.31.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/result_combinators_heap_ok.almd", class = "fixture" },
]

[[contract]]
id        = "C-156"
spec      = "ALS-I3"
title     = "An if-merged some((String, String)) ctor is a real tracked Option"
statement = "A `some((s1, s2))` constructor over a `(String, String)` tuple payload — including the if-merged form `if c then some((a, b)) else some((c, d))` in bind, call-argument, and arm position — materializes a REAL 1-element Option block whose payload tuple owns both Strings, and `option.unwrap_or_else` over it takes the Some payload. The scope-end drop routes to the generated `$__drop_opt_str_str` (both tuple slots, the tuple block, then the wrapper). Previously the un-admitted payload left the bind a deferred Opaque (len 0) that the unwrap twin misread as None — the fallback closure ran on a Some (differential-fuzz seed 20260721 index 374, a silent wrong value)."
since     = "0.32.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/opt_str_str_ctor.almd", class = "fixture" },
]

[[contract]]
id        = "C-157"
spec      = "ALS-I3"
title     = "An unannotated generic-ctor top-let carries its solved payload type to every reader"
statement = "A top-level `let` whose initializer is a generic constructor over a nominal payload (`let MAYBE = some(Cfg { … })`, no annotation) presents the fully SOLVED type (`Option[Cfg]`) to every reader — a same-file `match`'s payload binding and a cross-module reader (`m.MAYBE`) alike — and the match executes identically on both targets. Previously the registration seed `Option[Unknown]` leaked: the pre-solve write to `env.top_lets` was never upgraded after constraint solving, the v1 wasm pipeline skipped the module top-let refresh pre-pass entirely, and re-registration clobbered an already-solved entry back to the seed — the payload binding stayed Unknown and the whole program walled off the v1 leg while native ran (the option_record_toplet crossmod matrix cell, the last KnownBroken cell in the matrix)."
since     = "0.32.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/unannotated_toplet_generic_ctor.almd", class = "fixture" },
]

[[contract]]
id        = "C-158"
spec      = "ALS-I3"
title     = "A some/ok ctor over a scalar call or tuple payload materializes the real value, never a zeroed ctor"
statement = "A `some(...)`/`ok(...)`/`err(...)` constructor whose payload is a SCALAR-typed call (`ok(<Float combinator chain>)`, `some(list.len(...))`) or a TUPLE literal (`ok((0.3, 1000000))` — the mixed-scalar (Float, Int) shape) either materializes the REAL computed payload — byte-identical to native — or WALLS honestly. Previously the ctor safety net required a HEAP CALL payload, so these fell to the deferred Opaque: an EMPTY block the Result/Option formatter read as `ok(0)` while native printed the real value (differential-fuzz seed 1784512190387680000 index 74, a silent wrong value), and `result.unwrap_or` over the deferred tuple-payload wrapper aborted \"index out of bounds\" on the wasm leg while native succeeded (RunFailureDivergence). These — and IF-merged payloads (`ok(if c then some(\"a\") else some(\"b\"))`, whose deferral made the downstream unwrap take the FALLBACK on a Some) — now take the same ANF route: materialize into a synth temp via the ordinary bind path, rebuild the ctor over the temp, retry the materializer, and wall if the bind itself deferred or is outside the subset."
since     = "0.32.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/ctor_scalar_call_payload.almd", class = "fixture" },
]

[[contract]]
id        = "C-159"
spec      = "ALS-I1"
title     = "list.binary_search returns the same index on both targets for duplicate keys"
statement = "`list.binary_search` over a sorted `List[Int]` returns the IDENTICAL index on native and wasm even when the key occurs more than once. The native oracle is Rust std's branchless (1.52+) `binary_search_by` loop, which does NOT early-return on equality — `while size > 1 { half = size/2; mid = base+half; base = if elem > target then base else mid; size -= half }` then one final equality check — so an all-equal `[2,2,2,2,2]` search lands on index 4. The wasm self-host previously replicated the CLASSIC early-exit loop and returned 2 (differential-fuzz seed 1784512190387680000: `some(4)` vs `some(2)`, a silent wrong value). Both indices are valid per the std contract, but the native == wasm byte-identity invariant demands one — the self-host now replicates the branchless loop instruction-for-instruction."
since     = "0.32.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/binary_search_duplicate_keys.almd", class = "fixture" },
]

[[contract]]
id        = "C-160"
spec      = "ALS-D4"
title     = "Pure-Almide bundled stdlib modules link and run byte-identically on wasm"
statement = "A BUNDLED stdlib module whose functions are all pure Almide (no @intrinsic / @inline_rust / @wasm_intrinsic / @extern, no hole bodies — today `path` and `args`) links on the wasm target exactly like a user sibling module and produces byte-identical observable output to native. Previously every stdlib-named module was excluded from call resolution, so `import path` walled as an unlinked stdlib call; additionally env.args's WASI out-pointers came from the tightly-packing bump allocator unaligned, trapping (`Pointer not aligned to 4`) on the second args_sizes_get call. Intrinsic-bearing bundled modules (json, ...) keep registry-backed dispatch."
since     = "0.34.4"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/bundled_pure_modules.almd", class = "fixture" },
]

[[contract]]
id        = "C-161"
spec      = "ALS-T6"
title     = "Matrix constructor dimensions clamp negatives and abort over a shared ceiling"
statement = "A dimension-taking matrix constructor (`matrix.zeros`/`ones` and their `_f32` aliases) normalizes `(rows, cols)` identically on both targets. A NEGATIVE dimension clamps to 0 — the empty matrix — matching the C-034 signed-clamp rule the `repeat` family already follows; an element count above the shared 2^28 (2 GiB of f64, inside wasm32's address space so both targets accept exactly the same shapes) ABORTS in the T6 form: one `Error: matrix dimensions too large` line on stderr and exit 1. Before, a transformer-shaped program built with `seq = -2147483648` crashed on BOTH targets in DIFFERENT forms — native a raw-vec `capacity overflow` panic (exit 101), wasm an out-of-bounds memory fault in `__mx_rms_rows` (exit 134) — and neither is a permitted observable: the T6 convention forbids a native panic and a wasm `unreachable` alike. The ROW COUNT is bounded ALONE by the same ceiling, not only the r*c product: `zeros(2^31, -7)` has 0 elements after the cols clamp yet still asks for 2^31 ROW slots — a 16 GiB row-pointer array that OOM-killed the wasm leg while native's UNUSED-value allocation was LLVM-elided and ran clean, two verdicts for one program (differential fuzz, 2026-08-10 night); rows over the element ceiling can never yield an in-ceiling matrix anyway, so both targets die in the T6 form, and the fixture keeps its bind deliberately unused so the guard's abort is what pins the call on both. The guard fires before either allocator is asked for anything, so the ceiling costs no memory to enforce; `ALMIDE_MATRIX_MAX_ELEMS` (runtime/rs/src/matrix.rs) and `__mx_max_elems` (stdlib/matrix_core.almd) are the two halves that must stay in sync."
since     = "0.35.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/matrix_dims_guard.almd", class = "fixture" },
  { path = "spec/wasm_cross/matrix_dims_guard_overflow.almd", class = "fixture" },
  { path = "spec/wasm_cross/matrix_dims_guard_rows.almd", class = "fixture" },
]

[[contract]]
id        = "C-162"
spec      = "ALS-T5"
title     = "io.write / io.write_bytes emit in program order, interleaved with println"
statement = "`io.write(Bytes)` and `io.write_bytes(List[Int])` write their payload to stdout at the point of the call, so a program interleaving them with `println` produces the SAME byte stream in the SAME order on native and wasm. Two defects met here. (1) Neither fn had a wasm self-host: `io.write` was not in the admitted-effectful set, so it walled as `effectful/impure stdlib Module call io.write needs a declared capability` and any byte-streaming program was native-only; both are now self-hosted in stdlib/io_write.almd over the single-iovec `prim.fd_write` floor, carrying the SAME Capability::Stdout as `io.print` (a stdout write is a stdout write). (2) Once linked, the ORDER diverged: native routed the byte writers through a 64 KiB `BufWriter<Stdout>` flushed only at exit, while `println!` wrote through Rust's own `Stdout` handle — two independent buffers over one fd, so every `io.write` landed AFTER every line write (wrong even native-only), where the wasm leg's direct `fd_write` kept program order. Native now flushes at the end of each write; the buffer still batches WITHIN one call, which is where the byte-streaming benches spend their time. A zero-length write emits nothing and disturbs no ordering."
since     = "0.35.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/io_write_ordering.almd", class = "fixture" },
]

[[contract]]
id        = "C-163"
spec      = "ALS-M10"
title     = "A heap-result if/match bound to a let/var executes the taken arm on both targets"
statement = "`let x = if cond then <heap> else <heap>` (and the `match` form) computes the taken arm and binds ONE owned value on both targets — the same result the call-ARGUMENT and TAIL positions of the shape already produced. Only the BIND position walled on the v1 leg, so a whole enclosing function fell outside the MIR-lowering subset: native computed the value while a wasm-only build errored. The missing case was a String LITERAL payload under `some(...)` — the heap `some` arm admitted a Var, a call result and a list concat, but not a `LitStr` — which is exactly the archived cafe differential-fuzz finding (`let v = ok(if c then some(\"ΑΒΓ\") else some(\"café\"))`, two literal Some arms). The literal now allocates a fresh rc-1 block and moves into the Option with no Dup, the same `im` per-arm balance a bare literal arm already had."
since     = "0.35.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/heap_result_if_bind.almd", class = "fixture" },
]

[[contract]]
id        = "C-164"
spec      = "ALS-M12"
title     = "List modifiers and suffix copies co-own tuple / record / nested-list elements"
statement = "`list.set`/`insert`/`remove_at`/`swap`/`update` and `list.take_end`/`tail`/`drop_end` over a list whose element is a HEAP block that is neither String nor Value — a tuple, a record, a nested list — produce a result that CO-OWNS each copied element, and leave the source fully readable. The generic i64 implementation copied slots without `rc_inc`, so source and result co-referenced every element UN-OWNED and both released it at scope end: `list.remove_at` over a `List[(Float, Bool)]` trapped `unreachable` in `rc_dec` (#808's sibling). Those instantiations then routed to an unregistered `list.<fn>_heapelem` name and WALLED honestly rather than double-freeing, which kept the wasm leg sound but declined a shape any program may write. The `_heapelem` twins are now self-hosted (stdlib/list_heapelem.almd), mirroring the `_value` family one-for-one: the copy is element-type-BLIND (a slot holds a block handle; `rc_inc` on a handle is one instruction whatever the block contains), and the ONE place the `_value` twins are not blind — `set`/`update` freeing the replaced element with the Value-specific recursive `__drop_value` — is a plain `rc_dec` here, which is exact: the copy had just taken one reference and the source list holds its own for the whole call, so the count cannot reach 0 and no recursive free is owed. The RESULT list's recursive drop is routed by the CALL SITE's static element type, never by the twins. `__lsh_copy`/`__lsh_insert_fill`/`__lsh_set` are registered in the co-own trust anchor under the same CoownLoop.v grounding as their `_value` counterparts."
since     = "0.35.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/list_heapelem_rc.almd", class = "fixture" },
]

[[contract]]
id        = "C-165"
spec      = "ALS-M10"
title     = "fold over a String-keyed map, a String list or a String set threads a heap accumulator on both targets"
statement = "`map.fold` / `list.fold` / `set.fold` with a HEAP accumulator — a String, List or Map built up across the fold — computes the same value on both targets for every String-keyed subject layout. The v1 leg walled most of them: the heap-accumulator variants were self-hosted only for the all-String map layout with a String accumulator, so `map.fold([\"a\": 1], \"\", …)` over the skv layout (String keys, i64 values), the keys-collect shape `map.fold(m, [], (acc, k, v) => acc + [k])`, and every `set.fold` routed to the unregistered `map.fold_hacc` / `set.fold_hacc` and declined the whole program. The restriction was not one the layouts require: a heap accumulator is ONE i32 handle whatever block it points at, and the fold loop never inspects it — the accumulator MOVES into `f` each iteration and the result comes back — so exactly one variant per SUBJECT layout serves String, List and Map accumulators alike. Two walks now cover all of it: the interleaved 16-byte pair walk (`Map[String, String]`) and the split keys-then-values 8-byte walk (`Map[String, <scalar>]`), plus one type-erased `List[String]` walk that `Set[String]` shares because a Set[String] IS a DynListStr block of unique, insertion-ordered Strings. Keys and elements stay BORROWED (the subject keeps ownership and is readable after the fold); the final accumulator is the owned result."
since     = "0.36.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/map_fold_heap_acc_skv.almd", class = "fixture" },
]

[[contract]]
id        = "C-166"
spec      = "ALS-M10"
title     = "Map interpolation renders every self-hosted key/value pairing on both targets"
statement = "`${map}` produces the same `[\"k\": v, …]` text on both targets for every pairing that has a self-hosted display, and `[:]` when empty. Two of the most common pairings walled the whole program instead. The interp leaf table synthesizes an ALREADY-SUFFIXED display name — `map.to_string_ss` for an all-String map, `to_string_sb` for Bool values, `to_string_x` for a pairing with no variant — and the repr router then appended ANOTHER suffix to it, fabricating `map.to_string_ss_str_wall` and `map.to_string_x_skv_wall`: names nothing defines, so the render walled. Only the bare `to_string` and the one literal `to_string_sb` were spelled out as pass-through arms; every other member of the family fell through to the wall arm, which is why `${Map[String, String]}` — the single most common map interpolation an LLM writes — declined. The pass-through is now keyed on the whole `to_string*` family. Separately, `Map[String, Float]` had no variant at all and now has one, rendering values with the same shortest-round-trip `float.to_string` a bare `${float}` uses and quoting keys through the shared `__mts_fill_key` escaping path rather than re-deriving it."
since     = "0.36.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/map_interp_string_value.almd", class = "fixture" },
]

[[contract]]
id        = "C-167"
spec      = "ALS-M8"
title     = "float.clamp returns its input unchanged when in range, sign bit included"
statement = "`float.clamp(n, lo, hi)` returns `n` itself — sign bit included — whenever `lo <= n <= hi`, so `float.clamp(-0.0, 0.0, hi)` is `-0.0` on both targets. The self-hosted implementation was `fmin(fmax(n, lo), hi)`, which is NOT what `f64::clamp` does: wasm's `f64.max` is specified to prefer the POSITIVE zero when both operands are zero, so the wasm leg returned `+0.0` where native returned `-0.0`. A sign flip is an observable divergence under the equivalence claim, and it was reachable from ordinary arithmetic (differential fuzz). The implementation is now the comparison chain `f64::clamp` actually is — `if n < lo then lo else if hi < n then hi else n` — so the sign rides through untouched. NaN behaviour is unchanged: a NaN fails both comparisons and lands on the `hi` arm, exactly as `fmin(fmax(NaN, lo), hi)` did. `float.min` / `float.max` keep their own wasm-spec zero behaviour; this contract is about `clamp` alone."
since     = "0.36.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/float_clamp_negative_zero.almd", class = "fixture" },
]

[[contract]]
id        = "C-168"
spec      = "ALS-M12"
title     = "list.flatten borrows its argument, so it composes with another borrow of the same binding"
statement = "`list.flatten` reads its argument without consuming it: `list.get_or(xs, 0, list.flatten(xs))` compiles and runs on both targets, and `xs` is fully readable afterwards. It was declared `@consume(xss)` over a runtime taking an owned `Vec`, so that expression moved `xs` into `flatten` while `get_or` borrowed it — `check` accepted and native rustc rejected with E0505, the acceptance gap the differential fuzz hunts. Consuming bought nothing: the element type is already `Clone`, so the runtime cloned out of the moved `Vec` anyway. It now takes a slice like every other read-only list fn, and an owned `Vec` still deref-coerces into it. Flatten stays non-destructive on both legs, empty outer and empty inner lists contribute nothing, and String elements take the same path."
since     = "0.36.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/list_flatten_borrow.almd", class = "fixture" },
]

[[contract]]
id        = "C-169"
spec      = "ALS-T6"
title     = "list.repeat over the size ceiling aborts in the T6 form on both targets"
statement = "A `list.repeat` whose RESULT would exceed the shared 2^31-byte ceiling ABORTS in the T6 form on both targets — one stderr line (`Error: repeat result too large`) and exit 1 — the same rule `string.repeat` already followed (C-161). Without it the two legs failed differently for a count native can satisfy but wasm cannot: `list.repeat(0.0, i32::MAX)` allocated 16 GiB natively and printed a length, while the wasm leg — capped at a 4 GiB address space — trapped out-of-bounds with exit 134 (differential fuzz). Neither is a permitted observable, and a machine-dependent success on one leg is not one the equivalence claim can carry. The ceiling counts SLOTS at the wasm element width (8 bytes), not `size_of::<T>()`, so the limit is the same NUMBER on both legs whatever the native element happens to be; the boundary itself succeeds and only counts ABOVE it abort. A negative count still clamps to the empty list (C-054) rather than tripping the guard. `ALMIDE_LIST_REPEAT_MAX_ELEMS` (runtime/rs/src/list.rs) and the `list_make.almd` guard are the two halves that must stay in sync."
since     = "0.36.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/list_repeat_size_ceiling.almd", class = "fixture" },
]

[[contract]]
id        = "C-170"
spec      = "ALS-M8"
title     = "Integer arithmetic wraps in every position, including a module-level let"
statement = "Integer `+` / `-` / `*` wrap on two's complement wherever they appear — in a local binding, in a module-level `let`, and under any build profile — and the position never changes the result. They were emitted as bare Rust `+`/`-`/`*`, which only delivers that law in a release profile: under `-C overflow-checks` (debug, `cargo test`) they panic, and in a CONST-EVALUABLE position rustc rejects the program outright with `this arithmetic operation will overflow`. So `let OVERFLOW = MAX_COUNT + 9223372036854775807` at module scope failed to BUILD after `check` accepted it, while the identical expression as a local binding wrapped correctly (differential fuzz) — an acceptance gap AND a position-dependent semantics. Native now emits `wrapping_add` / `wrapping_sub` / `wrapping_mul`, matching wasm's `i64.add` / `i64.sub` / `i64.mul` in every position and profile. `list.sum` / `list.product` already folded with the explicit wrapping ops for the same reason (C-056); this extends it to the operators themselves."
since     = "0.36.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/toplevel_const_wrapping.almd", class = "fixture" },
]

[[contract]]
id        = "C-171"
spec      = "ALS-T4"
title     = "Byte-offset bound checks do not overflow at the i64 boundary"
statement = "A byte-offset bound check is written subtractively, so it cannot wrap: `bytes.read_f16_le(b, i64::MAX)` returns the 0.0 out-of-range default on both targets. The guard was `pos + 2 > len`, and at `i64::MAX` that addition WRAPS to a negative number — the comparison passed, the load read a wrapped address, and the wasm leg returned the value there while native's `pos as usize` was out of range and gave 0.0 (differential fuzz). The bound is now `pos > len - 2`, which cannot overflow: `len` is a non-negative i32-ranged length, so `len - 2` is representable for every value it can hold. This is the self-host twin of the `checked_add` guards the native runtime already uses (C-034). The same additive shape was fixed in `read_f16_le_array`'s per-element guard and in the JSON parser's `\\uXXXX` hex-quad bound, where a truncated escape must stay an error rather than an out-of-range read."
since     = "0.36.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/bytes_f16_offset_overflow.almd", class = "fixture" },
]

[[contract]]
id        = "C-172"
spec      = "ALS-M12"
title     = "unwrap_or over any heap payload yields the same value on both targets"
statement = "`??` / `unwrap_or` over an `Option[T]` or `Result[T, E]` whose payload is heap-backed produces the same value on both targets, for the carried value and for the fallback alike, and leaves the source readable. The catch-all names `option.unwrap_or_hx` / `result.unwrap_or_hx` were UNREGISTERED, so every payload with no shape-specific variant WALLED — `Option[Map[String, Int]]`, `Option[(String, Int)]`, `Option[Option[String]]` and their Result twins, the top remaining subset debt in the fuzz wall histogram (#812). The restriction was not one the layout requires: a heap payload is ONE i32 handle whatever block it points at, and neither the tag read nor the handle load inspects it, so exactly one type-erased implementation per CONTAINER covers all of them — Option reads its len-as-tag at @4, Result its cap-as-tag at @16, and both load the payload handle at @12. The shape-specific variants above them stay and are still tried first: they exist for the DROP discipline of their particular payload, not for the unwrap itself. Ownership is `option_listint_unwrap_or`'s: the kept handle is returned as a bare-Var tail, which auto-acquires, while the container holds its own reference for the rest of the call, so the count cannot reach 0 in between."
since     = "0.36.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/unwrap_or_heap_payload.almd", class = "fixture" },
]

# ── INTEGER LITERAL DOMAIN: MAGNITUDE, SIGN, AND THE i64 CARRIER ──────
[[contract]]
id        = "C-173"
spec      = "ALS-M14"
title     = "An integer literal outside what its context can represent is a checker error on both targets, never a silent value"
statement = "An integer literal must be a value its context can represent, and one that cannot is an E024 on BOTH targets — never folded, never carried as something else. Three deviations exist and each needed its own treatment. (1) MAGNITUDE, the original #626 rule: too large for the width, would fold to 0. (2) SIGN: a negated literal in an UNSIGNED context. `int_literal_fits_type` ignored the `negated` flag on its unsigned branch and only bounded the magnitude, so `let k: UInt64 = -5` passed check — 5 is in range for UInt64 — and native rustc then rejected the emitted `-5u64` with `error[E0600]: cannot apply unary operator '-' to type 'u64'` (differential fuzz). That is the same check-passes/build-fails class #626 closed for magnitudes, reopened for the sign, and it is not a magnitude question at all: an unsigned type has no negative values at ANY magnitude. `-0` stays legal — it is `0`. (3) CARRIER: a magnitude inside `UInt64`'s DECLARED domain but above the `i64` the compiler carries every integer in. E024's own hint used to recommend `UInt64` as the fix for case (1), and that path was broken — the IR's carrier is one i64 for every width and MIR, the default path for both targets, knows only `NTy::I64`, so `let big: UInt64 = 18446744073709551615; big / 2` printed `0` where the program meant `9223372036854775807`, a SIGNED divide, identically wrong on both legs, which is exactly why the cross-target fuzzer never surfaced it. A wall is never a miscompile, so the literal is rejected until the unsigned lane lands (#872), and the hint says so instead of recommending a type that cannot hold it. The three cases carry three distinct hints — a smaller literal, a different type, and neither-of-those — because the wrong hint sends the reader down a path that does not exist, and the magnitude hint now STATES the range (`its range is 0..=4294967295`) rather than referring to it, since few readers hold `u32::MAX` in mind. The classifier's own escape hatch was a fourth silent fold: a magnitude past the `u128` the comparison is done in answered `fits`, so a 44-digit literal printed `0` with no diagnostic — the check that exists to stop silent folds, folding silently at its own edge. `-0` stays legal — it is `0`, and the rule this enforces is `can the type represent this value`, which `0` passes; rejecting it (as Rust does, following from ITS choice to frame the failure as a missing `Neg` impl) would contradict the framing that lets the other two hints name a fix. Everything the carrier DOES reach is byte-identical native == wasm: both bounds of every unsigned width, `-0` in each, and the negated minimum of every signed width. The cross-language survey behind these choices — every case run on a real toolchain — is docs/roadmap/active/integer-literal-domain.md. SUPERSEDED IN PART by C-179 (0.37.0): deviation (3), the CARRIER rejection, was explicitly interim — 'until the unsigned lane lands' — and the lane landed, so `UInt64`'s bound is again its DECLARED `u64::MAX` and its fixture (tests/diagnostics/e024-carrier-overflow) is retired with the wall it pinned. Deviations (1) MAGNITUDE and (2) SIGN are unchanged and still carried by the evidence below."
since     = "0.36.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/unsigned_literal_domain.almd", class = "fixture" },
  { path = "tests/diagnostics/e024-unsigned-negative-literal/broken.almd", class = "fixture" },
]

# ── TCO HEAP-ACCUMULATOR SEED: THE UPFRONT COPY IS A REAL SHARE ───────
[[contract]]
id        = "C-174"
spec      = "ALS-M6"
title     = "A tail-recursive Map/Set accumulator keeps its seed on both targets, non-empty seeds included"
statement = "A tail-recursive accumulator of Map/Set type produces the same value on both targets whatever the INITIAL accumulator holds — the loop the TCO rewrites it into starts from the seed, not from empty. The TCO synthesizes an upfront copy of the borrowed accumulator param (`let slot = acc + []`, span-less); a List executes that copy via __list_concat, but a Map/Set is not a list, so the bind fell to the terminal deferred Alloc{Opaque} — an EMPTY block standing in for a copy of the seed. Every in-tree caller seeded the accumulator with an empty map (map.from_list allocates `map.new()` and folds into it), so the empty block COINCIDED with the correct value and the whole map/set self-host suite ran on top of the misshape without a visible defect; a program seeding `[(99, 7)]` then watched wasm insert into an empty map (`len=2 seed99=-1`) while native kept the seed (`len=3 seed99=7`) — a silent cross-target miscompile of exactly the F2 accept-but-wrong class #810 predicted, found by its producer census (41/41 terminal-Opaque producers were this one synthesized shape, plus one Try). The copy now lowers as an Op::Dup SHARE of the seed's block, the same CowSafety discipline a closure capture uses: sound because map/set growth is functional (map.set/set.insert allocate fresh — nothing mutates a block in place) and every in-place elision in the compiler is rc==1-gated (MakeUnique), which a Dup'd rc>=2 block defeats by construction. The zero-iteration case is the share discipline itself: the result aliases the seed (rc=2) and both handles drop independently — no double-free, no leak, and both read the same correct value. Covered per repr family: the Int-KV core map, the String-element set, and the interleaved _str String-KV map whose nested handles ride the rc-gated recursive drop."
since     = "0.36.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/tco_map_acc_seed.almd", class = "fixture" },
]

# ── LIST LITERALS OF VARIANT TYPES: BIND POSITION AND IF-ARM POSITION ─
[[contract]]
id        = "C-175"
spec      = "ALS-M7"
title     = "A List literal of a variant type builds and drops identically on both targets, in bind and heap-result-if-arm position"
statement = "A `List` literal whose elements are VARIANT values — `[shapes.circle(1.0)]` over a cross-module variant, `[Circle(1.0), Rect(2.0, 3.0)]` over a local one — materializes on the wasm target instead of walling, and behaves byte-identically to native through construction, iteration, match-based consumption and scope-end teardown (#875). A FLAT variant element (every constructor payload scalar — `Circle(Float)`) frees with a per-element rc_dec (the CtorFlat class): each element block owns no nested heap, so the flat free is exact. A RICH variant element routes to the generated per-element-recursive `$__drop_list_<V>` — the classifier asks `is_rich_variant_ty`, the SAME question the drop generator answers, so admission stays a subset of generation and a misclassification cannot dangle a drop call. The same literal as a heap-result `if` ARM (`if n <= 0 then [] else [leaf(n)]` — the ceangal kids_of shape, #881) routes through the bind-position record-list builder, with the EMPTY arm materialized as the zero-length block of the same layout: every list drop at len 0 is exactly the block free, so the moved-out object is uniform across arms. Separately hardened in the same change: a generated-drop target (`$__drop_…`) rendered from a DropVariant/DropWrapperRec op is now gated like a CallFn — an unresolved one WALLS instead of emitting a dangling call (an INVALID module, observed for a bare record name whose generated drop keys the module-qualified twin), and a bare-vs-qualified name mismatch resolves to its unique same-family twin rather than walling spuriously."
since     = "0.37.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/list_cross_module_variant.almd", class = "fixture" },
]

# ── OPTION/RESULT CTOR OVER AN INLINE CALL PAYLOAD ─
[[contract]]
id        = "C-176"
spec      = "ALS-M5"
title     = "some/ok around an inline tuple-returning call materializes the real payload on both targets"
statement = "An Option/Result constructor whose payload is an INLINE call — `some(option.unwrap_or(some((2, 3)), (2, 4)))`, `ok(<same>)` — wraps the call's REAL result on the wasm target exactly as native does (#871). The payload reaches the bind as a desugared MATCH (`option.unwrap_or` inlines before lowering), which the ctor's ANF safety net previously missed (its guard admitted Call/Tuple/If only), so the ctor fell to the #810 terminal: before that terminal existed the bind deferred to an EMPTY Opaque block that READ `none` (native printed `some((2, 3))` — the silent-wrong-value class, differential-fuzz seed 1785015406589852000), and one level out an `ok(...)` misread as an empty block made the downstream unwrap trap out of bounds where native returned `(2, 3)`. Two changes close it: the net's guard admits MATCH payloads (the ANF bind then routes them through the proven heap-result branch machinery, C-106), and the ctor's heap-general strategy gains a scalar-slot-TUPLE Module-call arm (the callee's fresh owned flat block moves into the ctor slot; an all-scalar tuple's rc_dec is its full free, the same drop class as a scalar list). A payload neither path claims still WALLS — never an empty block the program can observe."
since     = "0.37.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/opt_ctor_over_call_payload.almd", class = "fixture" },
]

# ── MUTABLE-GLOBAL PROJECTION ARGS: LOOPS ARE HOIST BOUNDARIES ─
[[contract]]
id        = "C-177"
spec      = "ALS-M6"
title     = "A mutable-global projection read in a loop-body call argument reads the CURRENT slot every iteration"
statement = "A call argument that projects out of a heap-typed MUTABLE global (`bump(items[i], d)` inside a `for` body; `float.to_string(items[0].elapsed)` — a SCALAR tail over a heap mg projection) evaluates against the slot's CURRENT contents at that iteration on both targets. The projection-arg ANF (#881's `desugar_mutable_global_projection_args`) hoists such reads to let-binds at STATEMENT position — and a LOOP is a hoist BOUNDARY: the walker previously descended through `ForIn`/`While` bodies and emitted the binds BEFORE the loop, freezing one stale handle (and one index) for every iteration — the wasm_cross_pkg two-phase tick then read pre-reassign values (native `110.0 4.0`, wasm `100.0` + a garbage denormal: the silent-wrong-value class). Loop bodies now hoist WITHIN their own statement list, the While cond is never touched (no per-iteration slot exists before it), and the same boundary is applied to the heap-if call-arg hoist (the committed sibling with the identical latent walker). The hoist also decomposes the DEEPEST qualifying prefix under a scalar tail (the `.elapsed` read above), and binds land in the enclosing STATEMENT list — an earlier in-place Block wrapper turned concat operands into Blocks the arg machinery declined (the wall that masked this class). The wasm_cross_pkg fixture is the package-form consumer; this fixture is the single-file distillation."
since     = "0.37.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/mg_rebuild_two_phase.almd", class = "fixture" },
]

# ── MUTUAL TAIL RECURSION: CONSTANT-STACK VIA return_call ─
[[contract]]
id        = "C-178"
spec      = "ALS-M5"
title     = "A mutual tail-recursion chain runs at unbounded depth on the wasm target"
statement = "A cross-function tail-recursion chain (`is_even`/`is_odd`, an accumulator `ping`/`pong`) computes the same value on both targets at a depth (1,000,000) that overflows a plain-call chain on wasm (#864 — the legacy almide-codegen emitted return_call, but the port was missed when almide-mir became the default renderer: the chain compiled to plain `call`s and trapped `call stack exhausted`). The renderer now classifies a FUNCTION-TAIL `CallFn` — one whose result flows UNMODIFIED through only Else/EndIf value merges (plus its own Consume move-out marker) to the function's `ret`, with nothing else (no drops, no stores) on the path from the call to the exit — and emits `return_call`, transferring the frame; the dead merge plumbing after it is unreachable-but-valid wasm. The classification is render-side only (MIR ops, certificate, and caps counts are unchanged — the call op is the same op, differently encoded), and anything breaking the pattern keeps today's plain `call`. SELF tail recursion still takes the upstream TCO loop rewrite and is not affected. Native Rust emit is byte-identical (emit-diffs 0 across the 1001-file corpus); the wasm re-encode was suite-verified (all three spec legs plus flagship outputs byte-identical). The INDIRECT twin (2026-08-03): the same classification admits a function-tail `CallIndirect`, so a closure call in the reaches-ret-unmodified shape renders as `return_call_indirect` (the closure_tail_recursion fixture's `step`; the instruction encoding is machine-pinned by crates/almide-mir/tests/tail_call_indirect_test.rs). The depth claim stays honest: a cycle that ALLOCATES a closure per hop keeps one creator frame per round on BOTH targets — the creator's post-call drop correctly declines the transfer, a transferred frame would leak the closure — so the indirect fixture pins parity at a survivable depth plus the instruction gate, not an overflow-proving depth; an unbounded pure-indirect cycle needs a creator-free closure chain no spine-lowerable construction produces today. THE ALL-SCALAR SUBSET (#1043): a tail-call SCC whose members' params and shared return type are all Int/Float/Bool is collapsed BEFORE either renderer — in the shared IR at the monomorphize exit (almide-optimize's mutual_tco, a running-flag dispatcher loop plus thin per-member wrappers) — so this fixture's is_even/is_odd and ping/pong execute as the SAME loop on native, wasm AND almide-interp, and native termination no longer depends on the cargo profile's opt-level (the dependence #1043 objects to; pinned at true rustc opt-0 by native_v1_differential_test). `return_call`/`return_call_indirect` remain the wasm mechanism for every shape outside that gate — heap-typed params/returns and closure cycles, the closure_tail_recursion fixture among them — and the render-side classification is unchanged."
since     = "0.37.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/mutual_tail_recursion.almd", class = "fixture" },
  { path = "spec/wasm_cross/closure_tail_recursion.almd", class = "fixture" },
]

# ── THE UNSIGNED 64-BIT LANE ─
[[contract]]
id        = "C-179"
spec      = "ALS-M2"
title     = "UInt64 reaches its full declared domain, with every observer reading the slot unsigned"
statement = "A `UInt64` value in the UPPER HALF of its declared domain (`i64::MAX+1 ..= u64::MAX`) computes and prints the same on both targets (#872). The IR slot stays ONE i64 and now carries that band as a 64-bit PATTERN: the literal decoder parses it as `u64` and reinterprets, and every observer reads it unsigned — `IntOp::DivU`/`ModU`/`LtU`/`LeU`/`GtU`/`GeU` (new) render `i64.div_u`/`rem_u`/`lt_u`… on wasm and `as u64` casts (plus `rt_chk_div_u`/`rt_chk_mod_u`, same divide-by-zero message bytes) on native; the legacy Rust emitter reinterprets at the declared width so `-1` is written `18446744073709551615u64` rather than the invalid `-1u64`; and `uint64.to_string`/`to_float32`/`to_float64` split ONE step with the unsigned `/`,`%` instead of pivoting through the signed `Int` (their quotients land back in the signed-safe band by construction, so one split suffices). The CONSTANT lane matters too: propagation is what first puts two literals under a `UInt64` `/`, after the fold pass has run, and the native leg's own constant handling then folded it SIGNED (`u64::MAX / 2 = 0`) while wasm kept the runtime unsigned op — a cross-target divergence in exactly the band the lane exists for, settled by an unsigned re-fold scoped to `UInt64`. The interim E024 carrier rejection (C-173's third cause) is RETIRED with the lane that replaced it: the diagnostic's magnitude bound for `UInt64` is again its declared `u64::MAX`, and the docs recommend it once more. Unsigned ordering is unsigned; add/sub/mul keep two's-complement wrapping (identical either reading)."
since     = "0.37.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/uint64_upper_half.almd", class = "fixture" },
  { path = "spec/wasm_cross/unsigned_literal_domain.almd", class = "fixture" },
]

# ── SIZED-INTEGER ARITHMETIC WRAPS AT ITS DECLARED WIDTH ─
[[contract]]
id        = "C-180"
spec      = "ALS-M2"
title     = "Sized-integer +, -, * and ^ wrap at the declared width on both targets"
statement = "Arithmetic on a narrow sized integer (`Int8`/`Int16`/`Int32`, `UInt8`/`UInt16`/`UInt32`) produces the SAME value on both targets, wrapping at the declared width — the documented semantics (stdlib/int8.almd: narrowing wraps rather than traps). The MIR carries every integer in one i64, so `Int8 127 + 1` computed `128` in the lane; native emits real `i8` arithmetic and wrapped to `-128`, so the wasm leg printed a value OUTSIDE the type's range entirely and the two legs disagreed (#889 — `UInt8 200 + 100` was `44` natively and `300` on wasm; anything that later narrowed, compared or serialized the wasm value was off). Each `+`/`-`/`*` whose operand type is a narrow width now re-wraps its result: signed via shift-left then ARITHMETIC shift-right (sign-extending the truncation), unsigned via a mask. `Int`/`Int64`/`UInt64` are the carrier's own width and are untouched; comparisons cannot leave the range; `/` and `%` cannot either once their operands are in it, and their one exception (`MIN / -1`) already aborts on both legs (C-002). In-range arithmetic is bit-identical to before. The CONSTANT FOLDER obeys the same rule: it computes at i64 width but emits the result as a LITERAL of the declared type, so a folded `let a: Int8 = 127; let b: Int8 = 1; a + b` rendered `128i8` — which rustc rejects outright, meaning the native leg would not BUILD a program the wasm leg ran correctly (#901). Both fold sites (the propagation fold in almide-optimize and the peephole fold in almide-codegen) now narrow to the declared width, so fold-time and runtime agree. `^` obeys the same rule and reached it last: on wasm the operator lowers to a `math.pow` CALL, which never passed through the `IntBinOp` path that re-wraps, so `Int32 999997 ^ 2` printed the un-narrowed `999994000009` against native's `-733379959` — while `a * a` at the very same type agreed on both legs, which is what named the operator rather than the type as the divergence (differential fuzz, seed 1785159097100061000 index 1049, a mutation of the C-184 fixture). The integer operator now wraps its call result to the declared width; wrapping once at the end is wrapping at every step, since two's-complement multiplication is congruent mod 2^bits. `math.fpow` returns a float and has no declared-width notion, so only the integer operator wraps."
since     = "0.37.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/sized_int_overflow_wrap.almd", class = "fixture" },
]

# ── COMMAND-LINE ARGUMENT SURFACE ─
[[contract]]
id        = "C-181"
spec      = "ALS-M8"
title     = "args.positional returns every non-flag argument, and the args surface agrees across targets"
statement = "`args.positional()` returns EVERY argument that is not a flag — it used to drop the first one (#891): `env.args()` already excludes argv[0], and the body dropped one more, so `-- input.csv out.csv` yielded only `out.csv` on BOTH targets (a stdlib logic bug, not a codegen one). The rest of the surface (`raw`, `flag`, `option`, `option_or`) is pinned here too, cross-target. `flag` and `option` are written as index recursions rather than `list.any`/`list.find` over CAPTURING lambdas: a capture inside a BUNDLED module mislowers on the wasm leg (#892 — the closure env held garbage, so `\"${args.flag(\\\"quiet\\\")}\"` printed a truncated `f` instead of `false`), and the recursion carries its comparands as parameters so nothing is captured. #892 stays OPEN for the underlying capture defect, whose remaining reproducer is a bundled-module call bound to a `let` AND called again inline in the same function."
since     = "0.37.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/args_surface.almd", class = "fixture" },
]

# ── NEGATED FLOAT LITERAL TAKES ITS CONTEXT TYPE ─
[[contract]]
id        = "C-182"
spec      = "ALS-M2"
title     = "A negated float literal takes its context's float type on both targets"
statement = "`let f: Float32 = -2.5` (and the same literal in argument position) carries the CONTEXT's float type, exactly as the positive literal already did (#890). Lowered as a `UnOp::NegFloat` over an operand still typed bare `Float` while the enclosing node claimed the annotated `Float32`, native emitted `-(2.5f64)` into an `f32` slot — `error[E0308]`, a hard build failure — and the wasm leg read the mismatched slot as `0.0`, a silent wrong value that a program doing `let scale: Float32 = -1.5` would compute with. The negation is now FOLDED into the literal at the context type, the same rule the Int case has had since #626 (which is why `let a: Int8 = -128` always worked). A negated EXPRESSION (not a literal) still negates at runtime, and the sized-float `NegFloat` selection now recognises `Float32`/`Float64` operands rather than only bare `Float`."
since     = "0.37.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/negative_float_literal_sized.almd", class = "fixture" },
  { path = "spec/wasm_cross/float32_literal_excess_precision.almd", class = "fixture" },
]

# ── NESTED SCALAR-RECORD LIST APPEND ─
[[contract]]
id        = "C-183"
spec      = "ALS-M4"
title     = "A loop-carried append of a list-of-scalar-records executes on both targets"
statement = "`gs = gs + [inner]` where the element is `List[P]` and `P` has only scalar fields lowers and runs identically on both targets (#888 — snaidhm's ttf `contours = contours + [pts]`). The concat admitted `List[List[String]]` and `List[List[scalar]]` but not `List[List[<scalar-only record>]]`, so the append fell out of the scalar-loop subset, the enclosing `for` rolled back to its model-one-iteration form, and the function walled on the loop variable. The physics are the same as the admitted cases: a scalar-only record's block is FLAT, so its `rc_dec` IS its full free (which the one-level `scalar_aggregate_elem` case already relies on), and the two-level `DropListListStr` sweep frees each inner element, each inner block, then the outer. Both levels are read back in the fixture so a wrong element handle would surface."
since     = "0.37.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/nested_scalar_record_append.almd", class = "fixture" },
]

# ── INTEGER `^` OPERATOR == math.pow ─
[[contract]]
id        = "C-184"
spec      = "ALS-T6"
title     = "The integer `^` operator is the same total exponentiation as math.pow on both targets"
statement = "`a ^ b` on integers evaluates identically to `math.pow(a, b)` on BOTH targets, including a NEGATIVE base and a negative exponent (#895). The wasm leg has always routed `^` through `math.pow`; native rendered it as the OPERATOR form `{left}.pow({right} as u32)`, which carried two divergences. (1) PRECEDENCE: Rust binds a method call tighter than unary minus, so a const-folded negative base emitted `-3i64.pow(2)` == `-(3.pow(2))` == -9 where wasm said 9 — odd exponents agreed by coincidence, which is how it survived to 0.36.0. (2) NEGATIVE EXPONENT: `as u32` wrapped `-1` into 4294967295 and returned garbage instead of the C-047 abort. Native now emits the `almide_pow!` prelude macro — exponentiation by squaring with wrapping multiply, at the BASE's own width, aborting `Error: negative exponent` + exit 1 — so the operator, the stdlib function, and the interpreter's `int_pow` are one implementation in three places. A literal-only sized-int expression (`let b: Int32 = 5 - 3`) is also retyped whole rather than only when it is a bare literal, since nothing in such a tree chose a width and the folded value otherwise reached an `i32` slot as `2i64`."
since     = "0.37.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/int_pow_operator_negative_base.almd", class = "fixture" },
]

# ── fan.any FIRST-OK REGARDLESS OF POSITION ─
[[contract]]
id        = "C-185"
spec      = "ALS-C3"
title     = "fan.any returns the first Ok in list order on both targets, whatever position it is in"
statement = "`fan.any([t0, t1, …])` yields the FIRST thunk whose result is Ok, in list order, identically on native and wasm — and the fixed `fan.any: all candidates failed` Err when none is (#900). On wasm the combinator is INLINED into a match-over-call chain rather than materializing a `List[funcref]`, and the chain reused one CLONED Ok arm at every level, so every level bound the SAME VarId. The renderer resolves a VarId to one local at one binding site — the innermost it saw — so an outer level extracted its payload into a local nothing read, and the duplicated body read the inner level's local, which on that path was never stored. `fan.any([() => ok(11), () => ok(22)])` returned 0: a plausible value, not a trap and not a wall, in the combinator whose documented purpose is redundancy. Each level now binds its OWN fresh var with the body substituted accordingly, and the desugar seeds from and publishes back the chain's shared var counter so no later rewrite in the same loop can reissue those ids."
since     = "0.37.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/fan_any_early_winner.almd", class = "fixture" },
]

# ── INLINE INDEX READ AS A LIST-ACCUMULATOR ELEMENT ─
[[contract]]
id        = "C-186"
spec      = "ALS-M4"
title     = "Appending an inline index read to a list accumulator lowers and runs on both targets"
statement = "`acc = acc + [xs[i]]` — an INLINE index read as the appended element — executes identically on native and wasm (#888, snaidhm's ttf `expand_implied` rotate; the same wall blocked ceangal consumers). The element TYPE was already admitted: a scalar-only record's block is FLAT, so per-element `rc_dec` is its full free (C-183's physics). What had no lowering arm was the element EXPRESSION. The literal builder knew Block / ctor / Var / call elements and not an index read, so the append declined, which dropped the enclosing `for` to its model-one-iteration fallback, which walled the loop variable under STRICT value mode, which collapsed the enclosing heap-result `if` — three layers of fallback turning one missing arm into `heap-result 'if' outside the executable subset`, and a CONSUMER of the walled function saw only an unlinked `almide_rt_*` call with no hint of the cause. Writing the identical thing as `let c = xs[i]; acc + [c]` always worked, and that is exactly the ownership the arm reproduces: the container keeps owning the element, the slot load is a BORROW, and the `Dup` (rc_inc) makes the handle the literal stores an owned one, released when that literal is dropped. A scalar-only record's list literal is now classified (Flat class) so its operands reach the per-element arms at all; a recursive-drop record still REFUSES to fall back to the generic path, because that materializes a structural literal in source field order which the declared-order `$__drop_<R>` would free wrongly — a flat record has no such drop and keeps the generic path as a fallback, which makes the widening strictly additive."
since     = "0.37.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/list_index_element_append.almd", class = "fixture" },
]

# ── IN-PLACE MUTATION OF A MODULE-LEVEL `var` BUFFER ─
[[contract]]
id        = "C-187"
spec      = "ALS-M4"
title     = "An in-place mutator writing through a module-level `var` buffer behaves identically on both targets"
statement = "`var arena = bytes.new(N)` written with `bytes.set_f32_le` / `bytes.set_at` / `list.pop` — from `main`, from a callee, from a loop, from an `@export(wasm, …)` fn — produces byte-identical output on native and wasm instead of walling the renderer (#906; it blocked almide-graphics/nendo's VRM viewer, which keeps its whole scratch arena in one module-level buffer). A mutable global is deliberately never cached in `value_of` — it is read fresh from its storage slot on every reference so an intervening write is seen — so the in-place-mutator receiver check asked `value_for` and got `use of unbound var`: the receiver was never a local to begin with. The block is owned by the SLOT, so the copy-on-write is the slot's, the two-level record+field COW with the record level replaced by real storage: borrow the slot handle, `MakeUnique` it (at rc > 1 the render's `rc_dec` releases the slot's reference and a fresh uniquely-owned copy takes its place; at rc == 1 it is a no-op), store the unique handle back into the slot. The mutator's own receiver arg then re-reads the slot and writes the uniquely-owned block, so every later read of the global — in any function — sees the write. The aliasing half is what makes the COW load-bearing rather than decoration: native models the global as an `RcCow` whose `make_mut` copies a SHARED block, so `let snap = arena; bytes.set_f32_le(arena, 0, 99.5)` must leave `snap` at the pre-write bytes, which eliding the COW would silently break and copying unconditionally would pay for on every unshared write. A write inside a MODELED (non-executable) frame still walls, the same fence a mutable-global assignment carries: the write is an effect the model would elide. An IMMUTABLE module-level `let` receiver stays refused — it has no storage slot, each reference is a fresh per-use copy, and native renders it as a non-writable Rust `static` — but now says so and names `var` as the fix, instead of reporting `a non-var receiver … the two-level record+field COW`, which was true of neither the shape nor the remedy. The modeled-frame fence no longer misfires on a REAL scalar-`if` arm — a guard-shaped write (`if bytes.len(d) < 12 then 0 else { bytes.set_i32_le(g, …); … }`) executes there, since exactly one arm runs under the real markers (C-188, #907); it keeps walling the genuine both-arms linearization, where the write would run the untaken arm's effect."
since     = "0.37.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/mutable_global_bytes_arena.almd", class = "fixture" },
  { path = "spec/wasm_cross/scalar_if_arm_effects.almd", class = "fixture" },
  { path = "spec/wasm_cross/mutable_global_repeat_writes.almd", class = "fixture" },
]

# ── STATEMENT EFFECTS INSIDE A REAL SCALAR-`if` ARM ─
[[contract]]
id        = "C-188"
spec      = "ALS-M4"
title     = "A scalar `if` arm executes its statement effects — global writes land and outer-var reassignments hit the stable local"
statement = "The scalar-if machinery (`try_lower_scalar_if`, the variant/refinement chains, the desugared match chains) emits each arm under REAL `IfThen`/`Else`/`EndIf` markers — exactly one arm runs at runtime — but `lower_scalar_arm` raised only the frame counter and not `unit_arm_depth`, so the arm was indistinguishable from a MODELED (both-arms-linearized) frame. Two divergences flowed from that one gap. (1) The modeled-frame fence (`in_frame > 0 && unit_arm_depth == 0`, C-187's fence) walled a mutable-global in-place write inside the ordinary early-return guard `if bytes.len(d) < 12 then 0 else { bytes.set_i32_le(g, …); … }`: the arm's statement erred, the scalar-if rolled back, and the whole fn fell to the both-arms linearization wall naming the CONDITION as unresolvable — the cond had lowered fine; the arm's fence was the decliner (#907: nendo's `load_vrm_data` and `set_skeleton`, the VRM/GLB entry points — the practical effect was that an exported parser could not START with a cheap validity check). (2) A scalar reassignment of an outer `var` inside the arm rebound `value_of` to a fresh frame-local instead of `SetLocal`-ing the var's stable local — frame-local rebinds are last-writer-wins, so a read after the branch saw only the LAST-LOWERED arm's local and `{ var r = 0; let x = if n > 5 then { r = 100; 1 } else { r = 7; 2 }; r * 10 + x }` printed 1 on wasm against native's 1001: the silent-wrong-value class, strictly worse than the wall. `lower_scalar_arm` now raises `unit_arm_depth` alongside `in_frame` (the same discipline `try_lower_unit_if` and the variant unit arms already carry), so both effects execute faithfully; the fence still walls the true both-arms linearization, whose `lower_branch_arm` raises only `in_frame` — a write there would run the untaken arm's effect."
since     = "0.37.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/scalar_if_arm_effects.almd", class = "fixture" },
]

# ── PLATFORM-REPORTING EXEMPTION (the equivalence law's ONLY carve-out) ─
[[contract]]
id        = "C-189"
spec      = "ALS-R5"
title     = "env.os and the temp-dir surface report the HOST — the equivalence law's only exemption, and it is closed"
statement = "`env.os()` and the temp-dir surface (`env.temp_dir()` / `fs.temp_dir()` — one observable, two spellings, agreeing byte-for-byte on every leg) are the ONLY stdlib observables whose bytes may differ between native and wasm, and they differ BY DESIGN: all report the host they are running on. Native answers the real OS name and the real temp directory (std::env::temp_dir — $TMPDIR on unix, GetTempPath on Windows); the wasm leg answers \"wasi\" for os and, for the temp dir, the HOST's $TMPDIR claim first with the \"/tmp\" preopen convention as the fallback — the Go/Python WASI rule (Go wasip1: $TMPDIR else /tmp; Rust std punts with a panic), so the guest converges on the native answer wherever the runner passes the host env through. Agreeing across targets unconditionally would be the defect — a program asking which platform it is on must be told the truth. The exemption is bounded, not open: the certified observable is the deterministic INVARIANT that holds identically on both legs (os is a member of the closed platform set {macos, linux, windows, wasi}; the temp dir is non-empty, absolute on every posix host, and identical across its two spellings), the same technique C-112 uses for random.int's nondeterministic draw. Every other fn in the process-environment surface stays byte-identical — env.args (C-096), env.get (C-118), random.int's range (C-112) — and `env.cwd` is not an exemption but a diagnosed wall on wasm. Adding a FOURTH exempt fn REQUIRES amending this statement and its fixture, which is what keeps the carve-out from widening silently."
since     = "0.37.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/env_platform_reporting.almd", class = "fixture" },
]

[[contract]]
id        = "C-190"
spec      = "ALS-M4"
title     = "A closure captures any scalar, not just Int and Bool — Float and every sized int width included"
statement = "A lifted lambda's closure env holds its non-heap captures in the SCALAR region: raw 64-bit slots that `$__drop_closure` never touches, written by `Op::ListLit` and read back by `Op::ListGetScalar`. The class test for that region restated a SUBSET of `is_heap_ty`'s negation (`Int | Bool`) instead of reading it, so every other scalar was refused for no physical reason and the whole lift walled — `(s) => d` over a `let d: Float`, and equally over `Int8`/`Int16`/`Int32`/`Int64`/`UInt8`/`UInt16`/`UInt32`/`UInt64`. `Float` carried a specific misreading: that its slot needed an f64↔i64 reinterpret the prim vocabulary lacked. It needs none — a MIR float local ALREADY holds its bits in an i64 and every float op reinterprets around ITSELF (`PrimKind::FloatBin` and the F32 family), so the bits round-trip through a scalar slot untouched, exactly as an `Int` does. The refusal was not merely a missing feature: a lambda outside the liftable subset in a call-argument position walls its enclosing function, so `apply((s) => d)`, `result.unwrap_or_else(v, (s) => d)` and `list.map((x) => x * scale)` over a Float local all failed to render while their Int twins rendered — an acceptance split with no runtime difference behind it. Captures whose slot is genuinely NOT one raw i64 keep their honest wall: a `List[List[Int]]` capture has owned heap in every element, which neither the flat `rc_dec` nor the nested `__drop_list_str` walk frees."
since     = "0.37.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/closure_scalar_captures.almd", class = "fixture" },
]

[[contract]]
id        = "C-191"
spec      = "ALS-M4"
title     = "A module-level global reads its true value on every branch path — the materialization memo is scoped to straight-line context"
statement = "A module-level `let`'s reference is materialized per function and MEMOIZED so repeated references share one copy. The memo was sound under both-arms linearization — every op executed whatever the condition — and became silently unsound as real branching spread: a global first referenced inside a REAL branch arm cached a ValueId whose defining op sits in that arm's block, and a reader in the SIBLING arm (or after a zero-iteration loop) referenced a local that never initialized on its path, which wasm reads as 0. `let CAP = 2097152` compared in both arms of an `if` made the else arm compare against 0 and take the wrong side with no diagnostic (#945 — aivarium's glTF loader rejected every primitive against a capacity of 0, loading a 52849-vertex model as zero geometry); the heap edition read `s == TAG` as false for TAG's own bytes. Two fixes, one discipline: (1) all nine global-materialization memo sites route through ONE chokepoint (`memo_global`) that caches only in straight-line context (`in_frame == 0 && unit_arm_depth == 0 && scalar_loop_depth == 0`) — inside any arm, loop, or frame each reference re-materializes locally, which costs a ConstInt or a fresh arm-local alloc and can never dangle; (2) `lower_heap_result_arm` now raises `in_frame`+`unit_arm_depth` for its extent, the same C-188 discipline `lower_scalar_arm` carries, since every caller emits it under real `IfThen`/`Else`/`EndIf` markers — without that the chokepoint read heap-result arms as straight-line code and the heap twin stayed wrong."
since     = "0.37.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/global_in_both_arms.almd", class = "fixture" },
]

[[contract]]
id        = "C-192"
spec      = "ALS-M4"
title     = "An auto-unwrapped effect call inside a string interpolation lifts and propagates like a call argument"
statement = "The effect-`!`/auto-`?` desugar handles an unwrap in statement, bind, tail and call-argument position by ANF-lifting it into a fresh `let tmp = e!` whose early-return machinery already lowers. The lift's extractor walked Call arguments, operators, tuples, ctors, conditions and access objects — but not STRING-INTERPOLATION parts, so `println(\"a=${fetch()}\")` (an effect call returning Result, auto-unwrapped, interpolated, in a call-argument position) reached the interp lowering with the Unwrap intact and the whole wasm render walled with \"non-lowerable string interpolation in a call-argument position\" while native ran (#950). The hoisted twin (`let v = fetch(); println(\"a=${v}\")`) building on both targets was the tell that only the LIFT was missing, not the propagation machinery. Interpolation parts are evaluated unconditionally, left to right, which is exactly the soundness condition the extractor's other arms rely on — an arm for `StringInterp` parts closes the gap, multiple unwraps in one interpolation lift one per fixpoint round, and the err path early-returns before any later part or the enclosing call runs."
since     = "0.37.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/interp_effect_unwrap_arg.almd", class = "fixture" },
]

[[contract]]
id        = "C-193"
spec      = "ALS-M4"
title     = "Repeated in-place writes through a mutable global stay in place — the receiver borrows the post-COW handle"
statement = "An in-place mutator over a mutable module-level buffer COWs the STORAGE SLOT (C-187) and then passes the receiver argument. The receiver used to be read back through the generic global-read path, which `Dup`s the slot's handle as an owned value-semantics reference released only at scope end — so within one body the SECOND write's `MakeUnique` always saw rc == 2 and paid a full O(|buffer|) copy, EVERY iteration of a loop. One `bytes.copy_from` per iteration ran at 0.1 µs; two ran at ~5 ms each against a 16 MiB destination (#946 — nendo's three-copies-per-vertex VRM loop took 23 s where three single-copy loops took 2 ms; the cost scaled with the DESTINATION size, the tell that the copy was the COW's, not the write's). The fix: `mutable_global_cow` stages its own post-MakeUnique handle and the receiver argument consumes it as a BORROW — the slot owns the block, nothing between the COW and the call can release it, and the callee's &mut protocol writes through it — so the block stays at rc == 1 across every write in the body. Consumed once per statement and cleared at every statement boundary, so a rolled-back attempt can never leak an un-Dup'd alias into a later read; a second read of the same var in the same statement (`bytes.copy_from(dst, dst, …)`'s source position) takes the normal owned-Dup path. The aliasing semantics are unchanged and pinned observably: a snapshot taken between writes keeps its pre-write bytes on both targets."
since     = "0.37.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/mutable_global_repeat_writes.almd", class = "fixture" },
]

[[contract]]
id        = "C-194"
spec      = "ALS-M4"
title     = "bytes.copy_from with a mutable-global destination writes through the storage slot"
statement = "`bytes.copy_from` with a mutable module-level `var Bytes` as DESTINATION lands the copy in the module's storage slot on both targets, whether the source is a distinct global, the same global (non-overlapping ranges), or a local. Native's in-place-mutator table was missing `copy_from` — the one two-buffer entry in the runtime's &mut surface — so the destination rendered through the generic global-read path as `&mut G.with(|c| (**c.borrow()).clone())`: a mutation of a discarded temporary clone. A distinct-global copy COMPILED and silently read back the pre-copy bytes on native while wasm wrote through (the silent-wrong-value class); the self-copy spelling was the loud tell, emitting invalid Rust because the aliasing hoist bound the whole `&src` borrow (`let __hoist: RcCow<Vec<u8>> = &G.with(…)`) where the Bind path expects an owned value. Two changes close it: `copy_from` joins the native mutator table (mirroring almide-mir's lower/calls.rs list, which always had it), so the destination takes the same slot-COW receiver discipline as `set_at`/`fill`; and the hoist binds the borrow's INNER expr and re-borrows `&__hoist` at the call, so the snapshot gets the Bind renderer's owned-value glue and the by-ref calling convention is preserved. OVERLAPPING self-copy ranges carry SNAPSHOT semantics on both targets (#963): the copy reads as if src were snapshotted before the first write. Native gets this from the aliasing hoist's clone (safe Rust cannot alias `&mut` and `&`); the wasm self-host detects the aliased block (dh == sh — the only way two Bytes overlap, every block being its own allocation) and routes through copy_within's direction-switching memmove, which is as-if-through-a-temp. Before #963 wasm's forward byte loop observed its own writes and propagated src[soff] down the range — newly reachable only once this contract's fix made self-copies compile at all. Both directions are pinned in the fixture."
since     = "0.38.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/mutable_global_bytes_copy_from.almd", class = "fixture" },
]

[[contract]]
id        = "C-195"
spec      = "ALS-M4"
title     = "Value-position variant match over the checked-conversion family executes"
statement = "A `match` over any cell of the checked numeric conversion matrix (#956: `to_*_checked` / `from_*_checked` on int, float, and every sized carrier module) whose scrutinee is a RUNTIME value executes as a real tag-read on both targets — let-bound, call-argument, and return position, both arms — and produces byte-identical output, as does the `!` unwrap over the same cells. Before this brick the admission list for materialized-Option subjects was maintained point-wise and drifted when #956 completed the matrix: `int.from_uint64_checked` and every sized-module cell (`uint8.to_int8_checked`, …) walled the v1 renderer with `variant (Option/Result) match bound to a let/var outside the executable subset` the moment the scrutinee stopped folding (#958 — the walled-real ratchet caught exactly this in #956's matrix test, which had to ship with a `?? sentinel` workaround). The family is now admitted by SHAPE — numeric carrier module × `to_/from_*_checked` name — in `is_self_host_option_module_fn`, sound because every cell is self-hosted through the ordinary some()/none ctor rails and returns a materialized Option over a scalar lane. The derived matrix gate `checked_conversion_family_is_admitted` reads the cell list from the stdlib sources themselves, so a new cell cannot drift out of the admission again — the point-wise failure mode is structurally closed, not just fixed."
since     = "0.38.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/checked_conversion_value_match.almd", class = "fixture" },
]

[[contract]]
id        = "C-196"
spec      = "ALS-T6"
title     = "Call-stack exhaustion is a resource limit, not an observable-behavior promise"
statement = "Termination that depends on recursion depth beyond a target's call-stack resources is TARGET-DEPENDENT, and the divergence is contracted rather than silent: when one leg's optimizer legally transforms an unbounded self-recursion into iteration (LLVM's accumulator tail-recursion elimination on native — Wave 4 finding 57, a mutated C-044 fixture whose `factorial(n - -2147483648)` recursion is ~4.29e9 frames deep) while the other leg recurses faithfully and traps (`call stack exhausted` on wasmtime; `stack overflow` on the native guard page), neither leg is wrong — no finite stack absorbs the recursion, and termination-by-transformation is not a semantic promise. The convergent boundary IS promised: recursion within both targets' limits stays byte-identical (the fixture). The differential oracle classifies the one-sided stack-exhaustion case as this contracted class (ladder.rs `one_sided_stack_exhaustion`, unit-tested, mirroring the pre-existing both-legs-exhausted skip). A depth-guard normalization — a defined T6 abort at the same depth on both targets — remains the strengthening follow-up that would retire this exception."
since     = "0.41.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/recursion_depth_within_limits.almd", class = "fixture" },
]

[[contract]]
id        = "C-197"
spec      = "ALS-T6"
title     = "Linear-memory exhaustion is a resource limit with a defined abort"
statement = "Allocation that exceeds a target's memory resources is TARGET-DEPENDENT, and the divergence is contracted rather than silent: wasm32's linear memory is architecturally smaller than native's 64-bit address space, so a program whose completion requires more memory than wasm32 can grow (Wave 4 L5: ~4.29e9 pushes ≈ 34 GB — native completed, wasm32 could not) diverges by RESOURCE, not semantics. What IS promised: (1) the failure shape — exhaustion is the DEFINED abort `Error: out of memory` + exit 1 on BOTH targets: on wasm never an out-of-bounds fault, and on native never a raw Rust allocator SIGABRT (the bytes.new/bytes.repeat constructors reserve fallibly — fuzz seed 500705518626 index 711 was bytes.new(i64::MAX) killing native by signal while wasm exited 1) (the $oom primitive fires on a refused memory.grow, on an i32 frontier overflow in $alloc/$alloc8, on a cap whose byte size would wrap the $list_new multiply, and on a full-i64 length that would wrap the `i32.wrap_i64` block-size math in the DynStr/DynList alloc renders — the `bytes.new(4294967295)` forged-tiny-block case, fuzz seed 424245 index 104, whose zerofill previously ran off the memory end exactly like the L5 layer-2 wrap class that read as a memory-safety bug); (2) the convergent boundary — allocation within both targets' limits stays byte-identical (the fixture). The differential oracle classifies the one-sided memory-exhaustion case as this contracted class, mirroring C-196's stack rule."
since     = "0.41.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/allocation_within_limits.almd", class = "fixture" },
  { path = "spec/wasm_cross/bytes_new_exhaustion.almd", class = "fixture" },
]

# ── MATRIX HEAD-COUNT DOMAIN ─
[[contract]]
id        = "C-198"
spec      = "ALS-T6"
title     = "A head count below 1 is a defined abort, identically on both targets"
statement = "Every `matrix` entry point taking a head count (`multi_head_attention`, `masked_multi_head_attention`, `rope_rotate`/`_at`/`_neox_at`, `rms_norm_heads`, and the GQA forms taking `n_q_heads`/`n_kv_heads`) treats a head count below 1 as OUT OF DOMAIN and aborts with `Error: head count must be positive` + exit 1 on BOTH targets — the same unified abort `list.chunk` / `list.windows` / `int.rotate_*` already use, never a native hang, a wasm divide-by-zero trap, or a silent clamp. Within the domain the two targets compute byte-identically, including a head count that does not divide the model dim (the head width truncates the same way on both). This replaced THREE point-wise answers to one question, which is what Wave 5 R2 exposed: native `rope_rotate_at` clamped a negative count to 0, native `rms_norm_heads` clamped it to 1, and native `mha_core` did not guard at all — `n_heads = i32::MIN` cast to `usize` as 18446744071562067968 and the head loop HUNG, while the same program on wasm exited 0 and printed a shape. The self-hosted wasm bodies agreed with none of the three: they divided by the raw count, so 0 trapped and a negative value silently returned a zero-filled matrix of the input shape. Totality is now held by one helper per leg (`almide_rt_matrix_head_count` / `__mx_head_count`) and by a matrix gate that fails when a family member spells its own answer."
since     = "0.42.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/matrix_head_count_domain.almd", class = "fixture" },
  { path = "tests/matrix_head_count_domain_test.rs", class = "by-construction", name = "no_native_site_totalizes_a_head_count_on_its_own" },
]

# ── FAN BLOCK ERR IS LIST-ORDER, AND NOTHING IS CANCELLED ─
[[contract]]
id        = "C-199"
spec      = "ALS-R3"
title     = "A fan block joins every sibling and reports the first Err in list order"
statement = "`fan { a b c }` JOINS every sibling — there is no cancellation — and the block's failure value is the FIRST `Err` in LIST ORDER, surfaced as `Error: <msg>` + exit 1 identically on both targets via the effect-main termination. Not the first to fail in wall-clock time: with two failing siblings the two rules give different answers, and the fixture pins the list-order one. Cancellation is excluded BY DESIGN under the deterministic-data-parallelism stance (#1000): how far a cancelled sibling got before it stopped is a function of scheduling, which is the dependence the model exists to remove — so a `fan` whose sibling does not terminate does not terminate, exactly as sequential evaluation would. SPEC.md §9.9 and §13.3 claimed fail-fast cancellation in two places and #1023 reported that neither was observable; the implementation was correct and the prose was corrected to match, rather than the reverse."
since     = "0.42.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/fan_block_err_list_order.almd", class = "fixture" },
]

# ── FAN SIBLING TRAP ─
[[contract]]
id        = "C-200"
spec      = "ALS-T6"
title     = "A trap in a fan sibling exits through the unified main-error abort, convergently"
statement = "A runtime trap inside a `fan` sibling (division by zero, integer overflow, index out of bounds) terminates the program through the SAME unified `Error: <msg>` + exit 1 the effect-main path uses — never a native panic (101) nor a wasm trap (134) — and the two targets agree, including on what reached stdout first. In-flight siblings are NOT waited for: measured with a 1.5s-sleeping second sibling, both targets abort in ~0s with an empty stdout. This closes #1026's third complaint (the block's documented outcomes — a tuple, or the first Err in list order per C-199 — were not the only exits, and the trap exit was uncontracted). It does NOT require the per-arm output buffering the Unit's design note proposed: that was written on the assumption that the trap case diverged, and the measurement says it converges. Buffering remains the answer to a DIFFERENT question — C-004's EXCEPTION clause, where two siblings both PRINT and native interleaves by wall clock while wasm is sequential."
since     = "0.42.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/fan_sibling_trap.almd", class = "fixture" },
]

# ── OPTION TUPLE-PAYLOAD MATRIX ─
[[contract]]
id        = "C-201"
spec      = "ALS-M1"
title     = "An Option combinator's tuple result is materializable as an owned element for every element-type combination"
statement = "`some(option.unwrap_or(o, d))` — an Option combinator RESULT, not a tuple literal, sitting in an owned-aggregate element position — compiles and agrees native == wasm for ALL FOUR element-type combinations of a 2-tuple over {Int, String}: all-scalar, both mixed orders, and all-heap. The claim is the MATRIX, not a sample: the family was previously keyed four different ways (one cell type-keyed, three shape-keyed), so an inline call result divided cells that a tuple literal or a `let`-bound result did not, and the differential fuzzer found the seam (R3, seed 1785492509375906000 index 17) as `OptionSome argument cannot be faithfully materialized`. The last cell fell because the owned-heap-element lowering's `Match` arm recognised only LITERAL arm chains, so the `some`/`none` pair the combinator desugars to was declined outright — every OTHER match-lowering site already tried the variant value-match ladder first. KNOWN REMAINDER, unchanged by this contract: the NATIVE verified render still walls this family on ownership verification and falls back to the standard codegen, which is why the promise here is stdout+exit equality rather than a v1-verified native render. That wall predates the fix and reproduces without it, on a program that never reaches the arm above (#1037)."
since     = "0.44.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/option_tuple_payload_matrix.almd", class = "fixture" },
]

# ── DETERMINISTIC TIME: CONSTRUCTOR GUARDS ─
[[contract]]
id        = "C-202"
spec      = "ALS-D1"
title     = "Time constructors guard their domain: negative aborts, overflow saturates"
statement = "A NEGATIVE argument to any `compute.*` / `duration.*` constructor, and a negative scale factor in `T * Int`, terminate through the §13 convention (`Error: negative time: compute.us(-5)` / `Error: negative time scale: -2` on stderr + exit 1) identically on both targets; an overflowing construction or `T + T` sum SATURATES to the maximum representable time instead of wrapping (a wrapped-negative budget would exhaust instantly — saturation admits the work). Non-negative literal arguments fold at compile time to the same erased value the runtime guard produces."
since     = "0.47.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/time_negative_trap.almd", class = "fixture" },
  { path = "spec/wasm_cross/time_negative_scale.almd", class = "fixture" },
  { path = "spec/wasm_cross/time_saturate.almd", class = "fixture" },
]

# ── DETERMINISTIC TIME: OPERATOR ALGEBRA ─
[[contract]]
id        = "C-203"
spec      = "ALS-D1"
title     = "The time-type operator algebra is unit-exact and saturating on both targets"
statement = "`T + T` (saturating), `T - T` (0-saturating), `T * Int` in either operand order (saturating), and same-clock comparisons agree native == wasm at CHARGE-UNIT precision: the fixture pins every cell by driving a `fan.bounded` verdict across an exact unit boundary, so a one-unit drift in any operator's erasure flips an observable verdict. `T * T`, any Compute⊕Duration mix, bare-Int joins, and `/` are compile-time type errors (checker matrix, not runtime behaviour)."
since     = "0.47.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/time_ops_algebra.almd", class = "fixture" },
]

# ── DETERMINISTIC BUDGET VERDICT (fan.bounded) ─
[[contract]]
id        = "C-204"
spec      = "ALS-D2"
title     = "A fan.bounded verdict is a function of the program and its inputs alone"
statement = "`fan.bounded(c) { body }` yields Err (the ledger-constant message) exactly when the region's metered spend in charge units exceeds `floor(ns / CM-1)`, and the Ok ⇄ Err flip happens at the SAME declared nanosecond on both targets — the boundary fixtures pin the flip at unit exactness (heavy(1000) = 1002 units flips at 3006ns under CM-1 v0.3). Bind statements inside a block body add zero charge; the bare (no-`??`) form produces the same verdict as a real `Result[T, String]` value; the region body is pure by typing, so no wall-clock input can reach the verdict. The budget expression may reach the head through variables (a let-bound sum, a nested-block binding, an effect-fn boundary) — the nominal time tag erases at MIR, so the variable spelling meters identically to the inline one."
since     = "0.47.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/fuel_bounded_boundary.almd", class = "fixture" },
  { path = "spec/wasm_cross/fuel_block_body.almd", class = "fixture" },
  { path = "spec/wasm_cross/fuel_bare_result.almd", class = "fixture" },
  { path = "spec/wasm_cross/fuel_divergence_cut.almd", class = "fixture" },
  { path = "spec/wasm_cross/fuel_dyn_charge.almd", class = "fixture" },
  { path = "spec/wasm_cross/fuel_var_budget.almd", class = "fixture" },
]

# ── DETERMINISTIC RACE WINNER (fan.race) ─
[[contract]]
id        = "C-205"
spec      = "ALS-D3"
title     = "The fan.race winner is the (spend, index) lexicographic minimum among admitted arms"
statement = "Every arm is metered on the deterministic clock; an arm is ADMITTED iff it completes within the (optional) budget AND — when it returns a `Result` — it returned Ok (an arm's Err self-disqualifies it, never propagates). The winner is the admitted arm with the least `(spend, index)`: the cheapest arm, ties to source order; no admitted arm yields the ledger-constant Err. The winner-appearance boundary is unit-exact and identical on both targets (heavy(500) = 502 units appears at 1506ns under CM-1 v0.3). The MAPPER form `fan.race(budget?, xs, f)` obeys the SAME law over a dynamic list — arm i is `f(xs[i])` (a pure 1-param lambda returning `Result[T, String]`), index order is list order, the budget is per-element — so an equal-spend tie takes the earlier element and an empty list is the no-admitted-arm Err."
since     = "0.47.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/fuel_race_boundary.almd", class = "fixture" },
  { path = "spec/wasm_cross/fuel_race_err_skip.almd", class = "fixture" },
  { path = "spec/wasm_cross/fuel_bare_result.almd", class = "fixture" },
  { path = "spec/wasm_cross/fuel_trap_cut.almd", class = "fixture" },
  { path = "spec/wasm_cross/fuel_trap_window.almd", class = "fixture" },
  { path = "spec/wasm_cross/fan_race_mapper.almd", class = "fixture" },
]

# ── SETTLE TUPLE ─
[[contract]]
id        = "C-206"
spec      = "ALS-D4"
title     = "A settle block settles every arm into its own tuple slot, in arm order"
statement = "`fan.settle { a; b; … }` evaluates its arms in arm order and yields the TUPLE `(Result[A, String], Result[B, String], …)`: heterogeneous arm types are allowed, a plain arm's value wraps in Ok, and an EFFECTFUL arm's Err is CAPTURED into its slot — never propagated out of the block. Both targets agree byte-for-byte, including the captured-Err case and a tuple-pattern match over the settled slots."
since     = "0.47.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/fan_settle_tuple.almd", class = "fixture" },
]

# ── CM-1: THE VERSIONED COST MODEL ─
[[contract]]
id        = "C-207"
spec      = "ALS-D2"
title     = "CM-1 is a versioned constant with a ratio-only wall-clock claim"
statement = "Deterministic time is `charge units × CM-1` where CM-1 v0.3 = 3ns/unit, defined ONCE (`almide_types::time_units::CM1_NS_PER_CHARGE`) and consumed by both renderers, the interpreter, and `--time-report` from that single definition. The wall-clock claim is RATIO-ONLY: a standing calibration gate measures the declared/measured ratio on a 100M-unit reference workload and asserts it inside the declared 5x band (ADR-0001 D5) — the gate falsified both prior draft constants (21x and 18x out). Changing CM-1 is a versioned cost-model revision (CM-2, …), never a silent retune: every unit-exact fixture in ALS-D2/D3 pins the current value."
since     = "0.47.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/fuel_bounded_boundary.almd", class = "fixture" },
  { path = "tests/charge_probe_test.rs", class = "by-construction", name = "cm1_calibration_within_declared_band" },
]

# ── WALL-CLOCK DEADLINE (fan.timeout) — the R_OMEGA contract ─
[[contract]]
id        = "C-208"
spec      = "ALS-D5"
title     = "fan.timeout is omega-relative: fixed ends everywhere, fixed omega replays byte-identically"
statement = "`fan.timeout(duration.ms(n)) { body }` checks its wall deadline COOPERATIVELY at charge sites (never mid-operation) and nests by min-cap on the absolute deadline. The contract is RELATIVE TO omega (which check the deadline fires at — a host input, ADR-0001 S8): a recorded omega REPLAYED (the ordinal baked at compile time, the clock never read) reproduces the observations byte-identically on EVERY target — record on native, replay on wasm included. Only the two ends are absolute: a deadline larger than the work always completes Ok, and a diverging body under any finite deadline is always cut at a charge site (Err, the fallback runs, the program continues). The v1 omega encoding is the single ordinal of the FIRST hit."
since     = "0.47.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/fuel_timeout_ends.almd", class = "fixture" },
  { path = "tests/charge_probe_test.rs", class = "by-construction", name = "timeout_deterministic_ends_and_replay" },
]

# ── CODEC NONE/NULL LAW + Value PASSTHROUGH — the #1062 design decision ─
[[contract]]
id        = "C-209"
spec      = "ALS-D6"
title     = "Codec encode omits none; decode folds missing/null to none; Value passes through verbatim"
statement = "Derived Codec encode OMITS a none Option field (proto3-style unset; every peer that started null-by-default — serde, Go v1, aeson — grew an opt-out afterward, so Almide omits from day one). Decode keeps folding missing and explicit null to none, giving the retraction law decode(encode(x)) == x while encode(decode(doc)) normalizes the document. Fields with declared defaults stay emitted — the only omission rule is none, monotone and value-independent. `Value` fields never interpret the wire (identity both directions, explicit nulls preserved), so `Option[Value]` is the 3-state escape hatch: missing → none, explicit null → some(null), value → some(v) — the ONE Option cell where absent and null differ, and the sanctioned spelling for RFC-7386-style explicit null. List ELEMENTS keep null in both directions (an array position cannot be omitted). Containers nest arbitrarily (List[Option[T]] carries meaningful element nulls, List[List[T]] and Option[List[...]] compose — #1065); the rejected cells are SEMANTIC: Option[Option] at any depth, Option[Value] anywhere but field root, and non-wire leaves (Map/Set, sized numerics, Bytes, tuples, fn types, Result) — all E023 at declaration; passing check and dying in codegen is the forbidden outcome (#1061). Verified byte-identical native == wasm."
since     = "0.52.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/codec_none_omission.almd", class = "fixture" },
  { path = "spec/stdlib/codec_field_matrix_test.almd", class = "fixture" },
]

# ── NaN OBSERVATION IS CANONICAL — Wasm 3.0 deterministic-profile conformance ─
[[contract]]
id        = "C-210"
spec      = "ALS-T2"
title     = "NaN observation is canonical — the deterministic-profile conformance law"
statement = "A NaN's raw sign/payload bits are the one place the byte-identical law would leak host state: x86 arithmetic produces sign-set NaNs where aarch64 does not, engines may or may not propagate payloads, and `int.bits_to_float` can smuggle arbitrary payloads in. Every OBSERVATION boundary therefore collapses NaN to a single canonical pattern — `float.to_bits` answers 0x7FF8000000000000 for every NaN however produced (arithmetic, negation, from_bits round-trip), and the bytes float writes (`bytes.append_f64_le/be`, `append_f32_le/be`) write the same canonical f64 pattern / the canonical f32 pattern 0x7FC00000. Non-NaN bits stay raw (to_bits(1.5) == 0x3FF8000000000000 — normalization never over-reaches), and a non-canonical NaN still EXISTS as a value (from_bits -> arithmetic keeps IEEE semantics); only observers are blind to its representation. This is the Wasm 3.0 deterministic-profile stance applied at the language layer, so it holds on BOTH targets and on every host architecture — stronger than the profile itself, which constrains only the wasm leg. The emitted module also stays inside the profile's instruction subset: no relaxed SIMD, no threads/atomics/shared memory (machine-checked by crates/almide-mir/tests/deterministic_profile_test.rs)."
since     = "0.53.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/nan_canonical_observation.almd", class = "fixture" },
  { path = "spec/wasm_cross/nan_canonical_bytes_write.almd", class = "fixture" },
]

# ── PURE-FN ! PROPAGATION — the #1067 E022 lift ─
[[contract]]
id        = "C-211"
spec      = "ALS-D6"
title     = "! propagates in pure Result/Option-returning fns, byte-identical across targets"
statement = "`!` (unwrap-with-propagation) is legal in a PURE fn whose declared return type is Result[_, E] or Option[_] — not only in effect fn bodies and test blocks (the pre-0.54 E022 rule). Semantics match the effect-fn body exactly: a Result operand unwraps ok and early-returns its err (the operand's error type must unify with the fn's E — a mismatch is a check-time error, never generated-Rust E0308); an Option operand in a Result fn turns none into err(\"none\") (the established unwrap-of-none message); an Option operand in an OPTION fn propagates the none ITSELF — the cell a Result-assuming lowering gets wrong (native E0277 via ok_or in an Option fn; the wasm leg read the mistyped block back as some(\"none\"), a silent wrong value caught while landing this). The closure boundary is unchanged: `!` never propagates out of a lambda (#489), and a fn returning any other type keeps E022. Verified byte-identical native == wasm across all three operand/return cells."
since     = "0.53.5"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/pure_bang_propagation.almd", class = "fixture" },
  { path = "spec/lang/pure_result_bang_test.almd", class = "fixture" },
]

[[contract]]
id        = "C-212"
spec      = "ALS-I3"
title     = "Two self-host modules with same-named private helpers link cleanly or wall — never an invalid module"
statement = "A program that pulls TWO self-host stdlib sources whose `__`-private helpers share a name links to a VALID wasm module, or fails with an honest compile error — never renders `Ok` and then fails wasm validation. The link-time dedup merges by FULL `MirFunction` equality (one source reached through two registry paths), not by name: two DIFFERENT functions sharing a name are a render wall (`self-host link collision: two different functions are both named ...`, naming both arities). Merging by name alone rebound the loser's call sites to a wrong signature, so `hex.encode` + `int.to_hex` in one program (their `__hex_fill` helpers were 5-param and 4-param) rendered successfully and wasmtime rejected the module with `type mismatch: expected i64 but nothing on stack` — the invalid-wasm-as-Ok class the ledger audits as zero, which stayed at zero only because no corpus program linked both modules (#1068). The instance is also fixed at the source (int_hex's helper is `__int_hex_fill`), and both legs are byte-identical native == wasm in one program."
since     = "0.52.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/hex_two_module_link.almd", class = "fixture" },
  { path = "crates/almide-mir/src/pipeline_link.rs", name = "dedup_linked_by_name", class = "by-construction" },
]

[[contract]]
id        = "C-213"
spec      = "ALS-C5"
title     = "The bytes byte-level writers encode the same buffer on both targets, out-of-range writes included"
statement = "Every `bytes` byte-level writer produces a byte-identical buffer native == wasm: `append_*` encodes width and order with a TRUNCATING cast (`append_u8(b, 511)` stores 255, `append_u32_le(b, 258)` stores `[2,1,0,0]`), a negative value and its unsigned twin write the SAME bytes (two's complement — `append_i16_le(b, -2)` and `append_u16_le(b, -2)` are both `[254,255]`), `append_f32_*` rounds f64 to f32 on the way in while `append_f64_*` is exact, and `set_*` splices at an offset where a window that does not fit — past the end, or a NEGATIVE offset, which is enormous as the native `pos as usize` — is a SILENT no-op that neither aborts nor grows the buffer. `fill` overwrites every existing byte and never changes the length (an empty buffer stays empty); `copy_from` no-ops when either offset is at-or-past its buffer's end and otherwise clamps `len` to the shorter remaining tail; `copy_within` clamps `src_end` to the length and moves only when the source range is non-empty AND the destination window fits. `runtime/rs/src/bytes.rs` is the oracle for each. The `write_*` serialization cursor holds too (#1099 closed its wasm self-host gap): `write_u8` is the 1-byte push, `write_u32_be`/`write_i64_be`/`write_f64_be` are the `append_*_be` layouts under the cursor's name, `write_bool` appends 1/0, and `write_string_be` appends a u32 BE BYTE-length prefix then the UTF-8 bytes. The `Endian`-parameterized sized-type surface holds as well (#1098 closed its native leg): the runtime owns the `Endian` enum and the twelve dispatching fns (the FileStat pattern — a bundled fn body never lowers natively, and the old @inline_rust templates matched a type nothing declared), the wasm leg links the bytes_typed self-host twins, and reads/writes/sets round-trip byte-identically in both orders. The interpreter models the WHOLE family from the name (`almide-interp` `inplace.rs`, gated total over `stdlib/bytes.almd` by `every_stdlib_bytes_writer_is_modeled`), so it becomes a third vote on the excluded parts the day their target leg lands."
since     = "0.53.5"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/bytes_writer_family.almd", class = "fixture" },
  { path = "crates/almide-interp/src/inplace.rs", name = "bytes_write_op", class = "by-construction" },
]

[[contract]]
id        = "C-214"
spec      = "ALS-R5"
title     = "process.exec_status_timeout: the fire-path error is pinned; whether it fires is the host's"
statement = "`process.exec_status_timeout(cmd, args, timeout_ms)` is the ONE admissible timeout: spawning a process is already outside the byte-identity contract, so bounding it adds no nondeterminism the spawn did not — while bounding a PURE computation would make its result a function of the machine, which is why the wall-clock `fan.timeout` of 0.29.0 stayed removed and its successor is the deterministic omega-replay form (T5-1). The contract has two halves. GUARANTEED: if the deadline fires, the child is KILLED and the error value is exactly `exec timed out after <ms>ms` — same message, same err path, on every host and (when a target grows process spawning) every target; a fast child completes with its real ProcessStatus, a chatty child cannot deadlock the drain (stdout/stderr ride reader threads), and a missing binary is the `exec failed:` error, never a timeout. NOT GUARANTEED: whether the deadline fires — that is a function of the host, which is why the fixture's firing case uses a wide margin (a 5s sleeper against a 100ms bound — the C-200 fan-trap discipline). The admissibility RULE is the family: a timeout twin may exist exactly for operations already outside byte-identity (process spawn now; an http.get deadline is the recorded intended next cell) and never for a pure combinator. The process surface is native-only today (wasip1 has no spawn; the render wall rejects it honestly on wasm), so the fixture runs on the native leg; the pinned error shape binds any future leg on arrival."
since     = "0.53.6"
status    = "active"
evidence  = [
  { path = "spec/stdlib/process_timeout_test.almd", class = "fixture" },
]

[[contract]]
id        = "C-215"
spec      = "ALS-R5"
title     = "fs content readers: absence is ok(none) via the _if_exists family, classified by the runtime"
statement = "Each fs content reader (`read_text` / `read_bytes` / `read_lines` / `read_bytes_raw`) has an `_if_exists` twin returning `Result[Option[T], String]` with a three-way behavior matrix: a path that does not exist — a missing PARENT directory included — is `ok(none)` (absence is a value, ADR-0004 D4, the map.get principle); a readable file is `ok(some(content))` with the same content the plain reader returns; every other failure (permission, a directory at the path, IO) is the SAME `err(message)` the plain reader produces. The classification happens in the native runtime on `ErrorKind::NotFound` — it cannot be spelled in Almide, because the String error has already erased the kind (and branching on its text is the E035-linted anti-pattern) and an `fs.exists` pre-check races (TOCTOU). The family is exactly the four content readers, machine-enforced by tests/fs_if_exists_family_gate_test.rs; metadata and directory ops keep their predicate spellings as intentional omissions. The surface is native-only today — the wasm render walls it honestly (the prim floor exposes no errno to classify); the pinned matrix binds the wasm leg on arrival, C-214-style."
since     = "0.53.7"
status    = "active"
evidence  = [
  { path = "spec/stdlib/fs_if_exists_test.almd", class = "fixture" },
]

[[contract]]
id        = "C-216"
spec      = "ALS-D6"
title     = "explicit ! on a declared-Option effect call is the implicit strip's identical twin"
statement = "On a DECLARED-OPTION effect fn (`effect fn f(..) -> T?`, ABI-lifted to Result[Option[T], String]), the spelled propagation `f(..)!` behaves byte-identically to the implicit auto-? form on both targets: the effect-Result layer strips, leaving Option[T] for the consumer (`??`, match, a later Option-`!`). This holds in every payload class — never-err callee (the v1 wasm ABI returns the raw Option; the marker is the identity), can-err callee (a real Result block; the err/ok match propagates), scalar and heap Some payloads, and the pure `-> T?!` composition. Before 0.54.1 the wasm leg read a raw Option block as a Result for the spelled form only — a wrong value (`hit=999`) followed by an rc_dec trap (#1125), the silent-wrong class that blocked the ADR-0008 N+1 switch: the switch REQUIRES the explicit spelling to be a drop-in for the implicit one everywhere."
since     = "0.54.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/effect_option_explicit_bang.almd", class = "fixture" },
]

[[contract]]
id        = "C-217"
spec      = "ALS-D6"
title     = "let _ = f() discards the Result — the err does not propagate"
statement = "With implicit propagation removed (ADR-0008, the 0.55 switch), `let _ = <Result-typed expr>` is the sanctioned intentional-discard spelling: the Result binds dead, its err does NOT return through the enclosing failure channel, and the fn completes normally — on both targets, byte-identically. Pre-switch the same line auto-?'d and PROPAGATED the err (a fn body's completion depended on a binding whose value was never read); the E041/E042 error pair plus this discard rule replace that: a statement-position Result must be consumed (`!`, `let _ =`, `??`, `?`, or an ok/err match), and only the `let _ =` spelling throws the error away — visibly."
since     = "0.55.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/let_wildcard_discard.almd", class = "fixture" },
]

# ── THE RETURN-POSITION HEAP `??` (the polymorphic-HOF consumer) ─────
[[contract]]
id        = "C-218"
spec      = "ALS-M12"
title     = "a heap-payload ?? returned as the fn tail yields the same value on both targets"
statement = "`fn f(..) -> T = <heap Result expr> ?? fallback` — the RETURN position — produces the same value on both targets, for the carried payload and for the fallback alike. This is the consumer half of the fallibility-polymorphic HOF idiom (ADR-0006): `list.map(xs, (x) => g(x)!)` is first-err short-circuiting, so its value is a `Result[List[T], E]` and the idiomatic way to spend it is `?? fallback` as the tail. C-172 type-erased `??` over heap payloads but only in BIND position (`lower_call_args` → `result.unwrap_or_hx`); the tail has its own lowering, where `UnwrapOr` reached nothing but `Init::Opaque` — moving out an EMPTY heap value — so the brick correctly refused and the whole module walled with 'heap-result UnwrapOr cannot be faithfully returned in this brick'. The idiom's most idiomatic consumer therefore could not be compiled to wasm at all (#1134). The rewrite is scoped twice over: only this POSITION (a whole-body desugar took the C-149 share shapes off their own proven paths) and only the RESULT polarity (a tail Option `??` already reaches `option.unwrap_or_str`, a proven executable path, and routing it through the match would take a working shape off that path for nothing — pinned by `heap_unwrap_or_tail_position_executes`). Within those bounds it is `match r { ok(p) => p, err(_) => fb }`, whose tail lowering is already proven."
since     = "0.56.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/unwrap_or_tail_result.almd", class = "fixture" },
]

# ── A DIVERGING CALL IN A BRANCH ARM RUNS, AND ITS EXIT CODE IS THE PROCESS'S ──
[[contract]]
id        = "C-219"
spec      = "ALS-T6"
title     = "a Never-typed call in a branch arm runs on both targets and sets the exit code"
statement = "A call whose return type is `Never` — `process.exit(code)`, the stdlib's only one — EXECUTES when it is the tail of a branch arm, and the process exits with that code on BOTH targets. The arm-tail dispatch in `lower_branch_arm` ran a `Call` only when its type was `Ty::Unit`; a `Never` call fell through to the deferred-value path, which records the call as a capability marker and emits NO call. The arm lowered to zero ops and returned Ok, so `guard n > 99 else process.exit(3)` built clean and exited 0 (#1124) — a silently wrong exit code with no wall and no diagnostic, reached through a TYPE the match forgot to name rather than through an unsupported shape, which is why strict-value mode never fired. NOT a wasm-only defect, though #1124 reported it as one: with a runtime condition the exit is lost on the NATIVE leg too; the issue\'s repro read `process.args()`, whose list length the native path resolved differently, which is what made it look cross-target. `Never` now rides with `Unit` in that dispatch — both produce no value by construction, the property the arm needs. The CONSTANT-condition form (`guard false else process.exit(3)`) is folded earlier (#1117) into a scalar tail outside the value subset, and STRICT value mode walls it honestly rather than deferring it to Const 0 — a wall, not a wrong answer, tracked separately."
since     = "0.56.0"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/guard_else_exit_code.almd", class = "fixture" },
]

# ── STREAMING LINE WALKERS: O(LONGEST LINE) MEMORY, READ_LINES SEMANTICS ──
[[contract]]
id        = "C-220"
spec      = "ALS-R6"
title     = "fs streaming line walkers fold/each with read_lines line semantics"
statement = "The streaming family {`fs.fold_lines`, `fs.for_each_line`} walks a file line-by-line through a buffered reader without materializing a `List[String]` — peak memory is O(longest line), not O(file) (measured: 2.2 GB eager vs 1 MB streaming on a 632 MB input, research/benchmark/perf/README.md). Line semantics byte-match `fs.read_lines`: split on \\n, one trailing \\r stripped per line, no phantom empty line after a trailing newline, a final newline-less line still yielded, and a missing file is the same err(message) the eager reader produces. An empty file folds to `init` with zero callback invocations. One deliberate divergence, inherent to streaming: a mid-file read error surfaces AFTER the callbacks for earlier lines have run — the eager reader fails before yielding anything. The family is exactly {fold_lines, for_each_line, fold_lines_range, fold_lines_chunked} (tests/fs_streaming_family_gate_test.rs). The range cell is the chunk worker's walker — a line is owned by the chunk containing the byte BEFORE its first byte (line 0 by the chunk containing byte 0), so folding a partition of [0, file_size) visits every line exactly once. The chunked cell runs one range worker per chunk on scoped threads INSIDE the runtime (the fan machinery's purity gate keeps effectful bodies sequential, so file parallelism is the fs module's own intrinsic) and returns partials in CHUNK ORDER — merging stays with the caller, so the observable result is deterministic whatever the thread schedule, and the emitted program's Send+Sync bounds make a state-capturing callback a compile error rather than a race. Transforming pipelines belong on fold_lines, the eager List belongs to read_lines, and chunk workers accumulate (for_each has no range/chunked cell). The ADR-0006 fallible callback forms are NO LONGER a tracked omission: the two sequential, callback-driven cells {fold_lines, for_each_line} carry them (C-269, #1144) and the two partitioned cells {fold_lines_range, fold_lines_chunked} decline them for a stated reason — a partitioned walk has no defined first err — with tests/fs_streaming_family_gate_test.rs pinning BOTH columns, so the family's ADR-0006 shape is now machine-enforced rather than narrated. The PLAIN surface's wasm reach is still partial — the callback crosses the runtime boundary, and generic HOF closures are the wasm defunctionalization frontier (#1134) — so only the accumulator shapes with self-host twins (the `Map[String, Int]` fold_lines/chunked pair, the `List[String]` range cell, the Int chunked cell) run on wasm; `for_each_line` has no twin at all and walls on both its plain and fallible forms. The wasm render walls honestly, and the pinned fixtures bind the wasm leg on arrival, C-215-style."
since     = "0.56.1"
status    = "active"
evidence  = [
  { path = "spec/stdlib/fs_streaming_test.almd", class = "fixture" },
  { path = "spec/stdlib/fs_fold_lines_range_test.almd", class = "fixture" },
  { path = "spec/stdlib/fs_fold_lines_chunked_test.almd", class = "fixture" },
]

[[contract]]
id        = "C-221"
spec      = "ALS-M15"
title     = "An effect fn-typed slot admits pure and fallible lambdas with one carrier semantics"
statement = "An `effect (A) -> B` fn-typed slot (#1055) is one runtime shape on every leg: the checker types any lambda checked against the slot as the effect carrier `(A) -> Result[B, String]` (a pure value tail is ok-wrapped, a `!`-using body propagates into the channel), the IR normalizes the slot type itself to that carrier at lower exit, and both renderers plus the interpreter then run the SAME fallible-closure machinery the ADR-0009 D1 `(A) -> B!` slots landed. NAMED fn VALUES ride the same rule (#1148): a named effect fn referenced as a value IS a carrier-returning closure (the checker bakes `Result[B, String]` into the value's ret — `fn_value_ty`), whether it reaches a call directly, through a pipe, through an unannotated let, or through an effect param slot; a PURE named fn filling an effect slot is ok-wrapped at the slot (the let-annotation and call-arg adaptation sites). Promised observables: a fallible handler's success value, a pure handler's value, and a failing handler's err message are byte-identical native <-> wasm (the lambda fixture pins all three, err via `?? fallback`; the fn-value fixture pins direct/pipe/unannotated/param-slot values and err via `!`-propagation into main's unified abort); calling an effect fn VALUE from a pure context is E006; a fallible lambda into a PLAIN `(A) -> B` slot routes to a GENERATED `__fallible__` twin when the HOF is in the 2b-iii Cell 1 subset (same-program, one bare fn slot, callback only a direct callee outside loops — the user-HOF fixture pins ok/err/total/double-call/named-fn on both legs) and stays the honest E005 outside it (forwarding and loop-called callbacks, pinned by the plain-slot-* diagnostics fixtures). `http.serve`'s handler slot is the first stdlib user: a handler `Err` becomes the listener's 500 response."
since     = "0.56.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/effect_slot_carrier.almd", class = "fixture" },
  { path = "spec/wasm_cross/effect_fn_value.almd", class = "fixture" },
  { path = "spec/lang/fallible_user_hof_test.almd", class = "fixture" },
]

[[contract]]
id        = "C-222"
spec      = "ALS-R2"
title     = "An expression-nested scalar unwrap propagates the err identically on both legs"
statement = "A scalar `!` NESTED inside a Bind/Assign value expression — a concat-list element (`out = out + [conv(s)!]`), a binop operand (`total = total + conv(x)!`), straight-line or inside a `for` body — propagates the err with the same observable behavior native <-> wasm: the Ok path yields the payload, the Err path takes the enclosing fn's err channel, stdout/stderr/exit byte-identical. Mechanism: the shared branch-desugar fixpoint ANF-hoists the innermost nested unwrap to its own bind-position statement (`let $t = conv(s)!; out = out + [$t]`), which the proven bind-position machinery — the effect-unwrap continuation match in straight-line code, the loop flag rewrite in a loop body — then handles; the hoist moves the call subtree intact, so the classify call-count is unchanged on both sides (desugar-before-both). Before this contract the nested unwrap survived every desugar and fell to a tag-blind scalar-operand payload read: the wasm leg read the err block's slot 0 (the err-message handle) AS the scalar value — released 0.56.0 printed `ok2: 8457` for the loop sum — while native (`?`) propagated. The residual plain-unwrap world (L9 test blocks, genuine die-on-err spellings) now carries a REAL tag dispatch at the payload read: len-as-tag != 0 dies with the err message the block carries, never a garbage read."
since     = "0.56.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/nested_unwrap_propagation.almd", class = "fixture" },
]

[[contract]]
id        = "C-223"
spec      = "ALS-T6"
title     = "Matrix transcendentals compute through the vendored musl-libm, not the platform one"
statement = "A `matrix` entry point whose math reaches a transcendental computes it through the VENDORED musl-libm (`runtime/rs/src/libm.rs`, the StrictMath/fdlibm decision) on BOTH legs — never the host platform's libm. Promised observable: `matrix.pow` over varied values (positive, fractional and negative exponents), the rope inverse-frequency table, and the non-kernel sigmoid print byte-identically native <-> wasm. Before this contract native called `f64::powf`/`f64::exp` directly while the wasm self-host used the vendored algorithm, so `matrix.pow(m, 1.7)` printed `1.5620815356808382` on native and `1.5620815356808384` on wasm (#1197) — live on released 0.56.0 and invisible to every gate: the stdlib matrix tests assert through a 1e-5 `approx_eq` tolerance, and the corpus's earlier matrix fixtures fed `matrix.ones`, whose uniform rows cancel the difference. VARIED input values are therefore part of the promise, not incidental. The platform libm is also non-deterministic ACROSS platforms (glibc / macOS / musl differ in the last ULP), so this contract is equally a native-to-native promise: the same program prints the same bytes wherever it was built. The SIMD activations join this promise (#1197 step 2): `matrix.softmax_rows`, `matrix.gelu` and `matrix.swiglu_gate` compute through the CANONICAL fast-exp — one algorithm, spelled identically by the native SIMD kernel (AVX / NEON / wasm-SIMD lanes and the scalar fallback) and by the wasm self-host (`stdlib/matrix_activations.almd`). Four independent spellings had to be unified for that, each worth a byte and each algebraically invisible: (1) FUSION — a fused multiply-add rounds once where mul+add rounds twice, so the algorithm is deliberately UNFUSED throughout, because wasm has no FMA instruction and relaxed-simd's fma is spec'd as fused-or-unfused; (2) REDUCTION ORDER — one left-to-right sum over the stored exps, never a lane-wise accumulator plus a horizontal combine; (3) SCALING — reciprocal-multiply (`inv = 1/sum`; `v * inv`), never `v / sum`; (4) gelu's two spellings — the identity `1 - 2/(exp(2y)+1)` and the cube association `c * ((x*x)*x)`. A lane-equality test (`fast_exp_determinism`) pins each SIMD lane to the scalar reference bitwise over ~60k inputs; its first version used a coarse grid and passed with a re-introduced fmadd, so the dense sweep is part of the promise, not decoration."
since     = "0.56.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/matrix_pow_libm.almd", class = "fixture" },
  { path = "spec/wasm_cross/matrix_softmax_fastexp.almd", class = "fixture" },
]

[[contract]]
id        = "C-224"
spec      = "ALS-C7"
title     = "if let / guard let bind and release heap payloads identically on both targets"
statement = "`if let name = scrutinee { … } else { … }` and `guard let name = scrutinee else { … }` produce byte-identical stdout/stderr/exit on native and wasm for HEAP payloads, not only the scalar ones the unit tests cover: a String bound and concatenated in the then-branch, a record payload projected (`c.name`, `c.port`), a `Result` whose Err carries a heap message and whose block must be released on the path not taken, and an early exit whose else-branch itself builds a heap value. Both constructs desugar to the same `match` the pattern machinery already proves — `Ok`/`Some` arm binds the payload and carries the rest of the block, wildcard arm takes the else — so the promise is that the DESUGAR, not just the match, is target-independent. NOT covered here and native-only by an honest wall: the loop form (`for … { guard let … else { continue } }`), because break/continue in a for body is a declared wasm frontier; it is regression-tested natively in spec/lang/guard_let_test.almd, where it PANICKED the compiler (\"internal error: entered unreachable code\") from the construct's introduction until #1204 — the loop-body statement list never ran the guard rewrite that blocks run, and no test had ever put a `guard let` in a loop even though early exit is what the construct is for."
since     = "0.56.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/if_let_guard_let.almd", class = "fixture" },
]

[[contract]]
id        = "C-225"
spec      = "ALS-R6"
title     = "fs.read_lines materializes a file's lines identically on both targets"
statement = "`fs.read_lines(path)` returns the file's lines with `str::lines()` semantics byte-identically native <-> wasm: split on \\n, one trailing \\r stripped per line (CRLF), no phantom empty line after a trailing newline, a final newline-less line still yielded, the empty file `Ok([])`, and a missing file the err channel on both legs. The wasm self-host (stdlib/fs_read_lines.almd) is the native oracle's exact composition — `read_to_string(path).map(|s| s.lines()...)` becomes prim.read_text_file + string.lines — so the line-splitting semantics are shared with C-220's streaming family by construction, and the result rides fs.list_dir's proven `Result[List[String], String]` cap-as-tag rails (tag @16, recursive DropResultListStr). Before this contract fs.read_lines had NO wasm body at all: every program touching it fell off the wasm leg (a COMPILER_FRONTIER row in proofs/wasm-reachability-baseline.txt, burned down with this contract). `fs.read_lines_if_exists` stays native-only debt on that ledger — its `Result[List[String]?, String]` shape has no tracking rails yet, and the reachability ratchet keeps it honest."
since     = "0.56.2"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/fs_read_lines.almd", class = "fixture" },
]

[[contract]]
id        = "C-226"
spec      = "ALS-C7"
title     = "A mut parameter crossing a call boundary mutates the caller's data on both targets"
statement = "Passing a `mut` parameter to another function mutates the caller's data byte-identically native <-> wasm, in every #1207 shape: a Unit effect fn forwarding its own mut param to another effect fn (`g(mut a) = { h(a)! }`), tail recursion threading a mut Bytes buffer through an `if` arm (the Barnsley-fern chaos-game shape), a loop-heavy call chain (10k writebacks — an ownership imbalance would trap or leak only under repetition), and a growth writeback where the callee REALLOCATES (list.push through a guarded chain) so the caller must observe the successor handle. Mechanism, two halves: (1) the C-132 move-mode rewriter rotates the effect wrapper onto the bound call (`Unwrap{Block{let buf = call; wb}}` -> `Block{let buf = Unwrap{call}; wb}`), so the tree carries only proven shapes — the C-222 bind-position unwrap and a statement Block — instead of a wrapper-over-Block no lowering accepts; (2) the unit-arm assign machinery admits the write-back into the fn's OWN borrowed mut-param slot (drop-old skipped: the callee consumed the old buffer, which flowed in as the mut arg and returned as the same handle or its realloc successor), recognized structurally by VarId — the assigned temp must be call-bound with the assign target among that call's Var arguments. Before this contract every mut-param call chain fell off the wasm leg (walls naming the condition instead of the arm — the #1207 report). Excluded and honestly walled, as before: a VALUE-returning effect fn with a mut param (tuple-inside-Result plumbing, the documented C-132 exclusion)."
since     = "0.56.2"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/mut_param_call_chain.almd", class = "fixture" },
]

[[contract]]
id        = "C-227"
spec      = "ALS-R6"
title     = "The fs metadata and composition family answers identically on both targets"
statement = "Ten formerly native-only fs/env/io functions are self-hosted on wasm and answer byte-identically to native: `fs.file_size` and `fs.modified_at` (the path_filestat floor, size@32 / mtim@48 nanoseconds to Unix seconds), `fs.is_dir`/`fs.is_file` (filetype@16; a missing path is `false` on both — the Path::is_dir/is_file error-swallow), `fs.append` (read-concat-write composition: append to an existing file and fresh-create both match native O_APPEND observables — the single-threaded WASI runtime has no interleaving writer to distinguish them), `fs.copy` (content round-trip; the missing-src err channel), `fs.create_temp_file` (exists-and-empty; the path itself carries an entropy suffix and is never a printed observable), `env.cwd` (the PWD read — the same C-137 variable relative fs paths resolve against, so cwd and path resolution tell one story), `env.get` under a `match` (the one admitted-effectful env fn whose Option result was untracked, walling every match over it), and `io.read_byte` (the stdin byte floor, -1 on EOF). One trap this family pinned: a `Result[Unit, String]` built by the ok(())/err(m) CTORS is the flat LEN-AS-TAG block (`materialize_result_ok`, tag @4) — NOT the cap-as-tag @16 layout the prim pass-throughs (fs.write) carry. Tracking copy/append on the @16 rails made every err read back as ok (dst never written, result ok — a silent wrong-branch caught by the missing-src probe); they ride the int.parse-family scalar-Result rails instead, and the fixture pins both err channels."
since     = "0.56.2"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/fs_metadata_family.almd", class = "fixture" },
]

[[contract]]
id        = "C-228"
spec      = "ALS-R6"
title     = "The fs composition family and the matrix row selectors answer identically on both targets"
statement = "The final fifteen formerly native-only functions reach wasm parity, emptying the COMPILER_FRONTIER burn-down list (26 rows at its creation → 0): `fs.remove` (single-entry semantics — a NON-EMPTY directory errs like remove_dir, a file/empty dir removes, a missing path errs — composed as an emptiness check over read_dir before the recursive floor), `fs.write_bytes`/`fs.write_bytes_raw` (byte-list / Bytes materialized into the write floor), `fs.walk` (recursive read_dir, full paths, dirs included, flat-sorted like native's walk_recursive+sort), `fs.glob` (the same simple `*`-matcher native implements, over the C-137 cwd), `fs.rename` (a NEW WASI floor — path_rename imported, normalized two-path call, the write_text_file-class cap-as-tag Result with native errno text; the NOENT/ACCES-only half it shipped with was folded into the shared four-errno mapping by C-273), `fs.is_symlink` (a NEW no-follow stat floor — lookupflags 0 exposes filetype 7; the follow-mode is_file through a symlink stays true, both pinned), all four `read_*_if_exists` twins (missing → Ok(none), present → Ok(some(payload)) through the Result-Option ctor rails — String, List[String], List[Int] and Bytes payloads all covered), and the three `matrix.select_rows_*` selectors (f32 LE, Q1_0 sign-bit blocks, Q8_0 fp16-scale int8 blocks — the shared decoders the full-matrix loaders use, on a row-subset schedule; the q8 fixture's expectations are hand-derived: 0.5·10=5, 0.5·(-10 as i8)=-5, 2.0·3=6). `env.set` is the one row NOT closed: WASI preview1 has no environ-write syscall and an in-module overlay cannot reach the real environ the C-137 path resolution reads — reclassified HOST_CAPABILITY with that reasoning in the ledger row itself."
since     = "0.56.2"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/fs_composition_family.almd", class = "fixture" },
  { path = "spec/wasm_cross/matrix_select_rows.almd", class = "fixture" },
]

[[contract]]
id        = "C-229"
spec      = "ALS-R6"
title     = "A selected row past the byte buffer is the all-zero row on both targets"
statement = "The three `matrix.select_rows_*` byte-buffer selectors (f32 LE, Q1_0, Q8_0) answer an out-of-range row id with the ALL-ZERO row — the same defined OOB→zeros edge the full-matrix `from_bytes_*` loaders take, applied per selected row — and clamp a negative row id to row 0 and a negative offset to 0, identically on both targets. Found by the generative fuzzer (run 31515916427, a mutation of the C-228 fixture): native panicked on an unchecked slice (`range end index 33 out of range for slice of length 32`) while wasm read undefined heap bytes past the block — a raw-abort/silent-wrong divergence, closed by per-row bounds checks against the buffer length in both implementations. `ALL-ZERO` is a claim about BIT PATTERNS: the zeros are observed through `float.to_string`, which spells the sign that `==` cannot see, and it holds for the IN-RANGE zeros too — the DEQUANTIZATION-ZERO RULING (#1358, fuzz run 31667874852) is that a Q1_0/Q8_0 element of zero magnitude is `+0.0` on both targets. A quantized element is magnitude (the block's fp16 scale) × direction (the sign bit / int8 quant); at zero magnitude the bytes encode no direction, so the sign IEEE-754 returns is an artifact of how the arithmetic was spelled — native's `-scale` produced -0.0 on an all-zero block exactly where wasm's `0.0 - scale` produced +0.0, and every `-0.0 == 0.0` downstream agreed with both. Both entry points are pinned (`select_rows_q1_0` and its full-matrix twin `from_q1_0_bytes`), for Q1_0 per block (every element is ±scale) and for Q8_0 per element (the product is zero from EITHER factor, and `a·b` signs by xor). Non-zero weights keep their exact sign; the float DECODERS `from_bytes_f16_le`/`_f32_le`/`_f64_le` are deliberately excluded, since there a stored -0.0 is the datum — see C-270."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/matrix_select_rows_oob.almd", class = "fixture" },
]

[[contract]]
id        = "C-230"
spec      = "ALS-R6"
title     = "The flight reference PID kernel runs identically on both targets"
statement = "The G-F4 reference application (#776, docs/roadmap/active/flight-reference-app.md): a single-axis PID control law in Q16.16 fixed point — saturation via `int.clamp`, integral anti-windup, pure-arithmetic `pid_step` — driven through a 16-step counted simulation loop against a first-order plant, printing the actuator command per step. The 16-line stdout trace is byte-identical native ⇄ wasm. The app is deliberately flight-subset-shaped: all-scalar Int state in driver `var`s, no allocation in the hot path beyond the per-step `int.to_string`, a literal loop bound, and exactly one declared {Stdout} capability exit — the artifact the qualification pitch runs end to end (`make verify-trust && make receipt`)."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/flight_pid_control.almd", class = "fixture" },
]

[[contract]]
id        = "C-231"
spec      = "ALS-E1"
title     = "Integer and boolean literal forms evaluate identically on both targets"
statement = "The integer-literal surface of ALS-E1 — decimal with underscore separators, hex/octal/binary radix prefixes, the full i64 range including the negated-MIN literal (the sign folds into the E024 range check, so -9223372036854775808 is writable directly) — and the ALS-E2 boolean literals produce byte-identical observables native <-> wasm, including the i64::MIN decimal round-trip through int.to_string (the $itoa edge pinned by execution in the WAT prelude audit). Out-of-range literals are a check-time E024 on both targets, never a silent wrap. First section pair of the syntax-element direction (proofs/als-element-coverage.toml): the four-part section form (grammar / value norm / rulings / fixture) is the template the remaining 71 rows follow."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/literal_int_forms.almd", class = "fixture" },
]

[[contract]]
id        = "C-232"
spec      = "ALS-E2"
title     = "Boolean literals and their display form are identical on both targets"
statement = "ALS-E2: `true` / `false` are reserved-word literals of type Bool whose display form at every observable point (string interpolation, comparison operands) is the lowercase spelling — byte-identical native <-> wasm. Shares the literal-forms fixture with C-231 (the ALS-E1/E2 section pair landed together as the syntax-element direction's template)."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/literal_int_forms.almd", class = "fixture" },
]

[[contract]]
id        = "C-233"
spec      = "ALS-E4"
title     = "The unit literal, its positions, and its reflexive equality are identical on both targets"
statement = "ALS-E4: `()` is the sole value of type Unit — accepted in expression position, let-binding position, and as the body of a Unit-returning function, with byte-identical observables native <-> wasm. Equality over Unit is reflexively constant (`() == ()` is true, `!=` false): one inhabitant means there is no runtime read, so the v1 lowering folds call-free Unit comparisons to the constant Bool (a call-bearing operand like `f() == ()` is NOT folded — eliding the call's effects would be a miscompile — and falls to the existing walls). This fold retired the Eq-over-Unit wall that previously kept unit equality native-only."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/literal_unit.almd", class = "fixture" },
]

[[contract]]
id        = "C-234"
spec      = "ALS-E6"
title     = "Parenthesized grouping and the 1-tuple disambiguation behave identically on both targets"
statement = "ALS-E6: parens override precedence and are otherwise semantically transparent ((1+2)*3 = 9 vs 1+2*3 = 7, byte-identical native <-> wasm), and fmt preserves redundant parens. The disambiguation is normative: (e) is grouping, (e,) is a 1-tuple whose trailing comma fmt round-trips (#1265 fixed in the same PR; regression test fmt_one_tuple_keeps_trailing_comma). The 1-tuple fixture is kept free of string interpolation while #1267 (native scalarization of the 1-tuple repr under interpolation, loud refusal) stands."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/grouping_unary.almd", class = "fixture" },
  { path = "spec/wasm_cross/tuple_single.almd", class = "fixture" },
]

[[contract]]
id        = "C-235"
spec      = "ALS-E7"
title     = "Unary operators: `not` negation and prefix minus evaluate identically on both targets"
statement = "ALS-E7: boolean negation is the keyword `not` (not true -> false, not not b == b); prefix `!` is a compile-time rejection that steers to `not` in BOTH lexer paths (expression statement and interpolation segment — tests/unary_not_diag_test.rs pins the steer, since postfix `!` is unwrap per ADR-0008). Prefix minus applies to literals (folding into the ALS-E1 range check), variables, parenthesized expressions, and floats: -(3+4) = -7, -x, -(1.5) = -1.5 — byte-identical native <-> wasm."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/grouping_unary.almd", class = "fixture" },
]

[[contract]]
id        = "C-236"
spec      = "ALS-E8"
title     = "Tuple construction, destructuring, indexing, and structural equality are identical on both targets"
statement = "ALS-E8: heterogeneous tuple literals, let-destructure, positional `.k` reads, tuple-typed parameters and returns, nesting, and structural `==` produce byte-identical observables native <-> wasm. The chained-index ruling is normative: `n.0.1` is REJECTED (the lexer reads `0.1` as a float literal — 'Expected a name'), nested reads are written `(n.0).1`. Known loud limitations recorded in the section: `.k` bounds/subject-type not validated at check time (#1266), native 1-tuple x interpolation refusal (#1267) — neither produces a wrong value."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/tuple_ops.almd", class = "fixture" },
]

[[contract]]
id        = "C-237"
spec      = "ALS-E9"
title     = "Option/Result constructor literals build and eliminate identically on both targets"
statement = "ALS-E9: `some(e)` / bare `none` / `ok(e)` / `err(e)` under a Result-typed expectation construct the same values on both targets, and `??` eliminates them identically (ok(3) ?? 0 -> 3, err ?? -9 -> -9, some(5) ?? 0 -> 5, none ?? -1 -> -1 — byte-identical native <-> wasm). The negative half is check-time and pinned by tests/ctor_diag_test.rs: `none()` is E001 (none is a value, not a function), and an un-annotated `let o = ok(3)` in an effect fn is the ADR-0008 explicit-propagation rejection. Nested elimination is IN contract since #1270 closed: some(none) ?? none -> inner none -> 9, some(some(5)) ?? none -> some(5) -> 5, byte-identical (the bind rewrites to `match e { some(p) => p, none => d }`, the Option-polarity mirror of the Result rewrite). RECORD-payload elimination is in contract since #1287 closed: `list.get(xs, i) ?? { record }` executes on both targets with String and Int fields read faithfully, and the merge's ownership witness BALANCES (the DropVariant release joins the merge-credit scans, so the kernel-proven checker accepts — the first landing was reverted in #1294 for exactly that bare-Dec rejection)."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/option_result_ctors.almd", class = "fixture" },
]

[[contract]]
id        = "C-238"
spec      = "ALS-E10"
title     = "First-class range values iterate their true bounds on both targets"
statement = "ALS-E10: a range is a first-class value — `let r = 0..<3` then `for i in r` iterates the same sequence as the inline head, for exclusive `..<`, inclusive `...`, non-zero starts, and call-typed bounds (fixture pins sums 3/15/9/6 byte-identical native <-> wasm). The bind materializes the real list via `list.range`; before this contract the deferred-empty bind made wasm silently iterate ZERO times while native printed the true sum (#1272 — the accept-and-wrong class this ledger exists to extinguish). The retirement half is check-time: `..` and `..=` are E031 with a mechanical `almide fix` steer (tests/range_spelling_diag_test.rs) — a silent re-reading would be an invisible off-by-one in either direction."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/range_first_class.almd", class = "fixture" },
]

[[contract]]
id        = "C-239"
spec      = "ALS-E11"
title     = "Collection literals, indexing, and the empty spellings behave identically on both targets"
statement = "ALS-E11/E12: list literals (with nesting and `+` concat), direct `xs[i]` element reads (Int and String elements), the square-bracket-colon map literal `[\"k\": v]`, the empty spellings `[]` / `[:]`, and Option-returning `m[k]` reads eliminated by `??` (missing key -> none) produce byte-identical observables native <-> wasm (fixture pins 40/y/2/3/0/3/0/-1). The asymmetry is normative: a list read returns the element DIRECTLY and aborts out-of-bounds with the unified message + exit 1 (C-067's ruling, read side), while a map read returns Option. The curly `{\"k\": v}` spelling is a parse error — a JSON transcription mistake stops at check time."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/collection_literals.almd", class = "fixture" },
]

[[contract]]
id        = "C-240"
spec      = "ALS-E12"
title     = "Map literals and empty-map reads behave identically on both targets"
statement = "ALS-E12: the square-bracket-colon map literal, `[:]` empty map, `m[k]` Option reads with `??` fallback, and deterministic insertion-order preservation (the fixture iterates [\"z\": 1, \"a\": 2, \"m\": 3] and pins z=1 a=2 m=3 — insertion order, not key order) — shares the collection fixture with C-239 (the E11/E12 pair landed together, like the E1/E2 template pair)."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/collection_literals.almd", class = "fixture" },
]

[[contract]]
id        = "C-241"
spec      = "ALS-S1"
title     = "Binding statements: let shadowing, var reassignment, and their check-time guards are identical on both targets"
statement = "ALS-S1 (the first statement-direction section): immutable `let` with SHADOWING (the new binding's initializer sees the old one — let x = 1 then let x = x + 10 gives 11), mutable `var` with sequential and in-loop reassignment (10, 6), and a conditional-expression initializer, byte-identical native <-> wasm. The negative half is check-time and pinned by tests/binding_diag_test.rs: assigning a `let` is E009 whose hint steers to `var`; reassigning a `var` at a different type is E001 (the declared type never silently widens)."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/binding_stmts.almd", class = "fixture" },
]

[[contract]]
id        = "C-242"
spec      = "ALS-E13"
title     = "Conditional expressions evaluate the taken arm only, identically on both targets"
statement = "ALS-E13: `if cond then arm [else arm]` — statement position without else (Unit), brace arms after `then`, the else-if chain, and the value form all produce byte-identical observables native <-> wasm (fixture pins stmt-then/brace-arm/else-arm/mid/9). `then` is mandatory: the brace-language transcription `if c { }` is a parse error steering to `then` (tests/if_then_diag_test.rs) — the most likely LLM slip stops at check time with the fix named. Only the taken arm's effects fire; the v1 both-arms-linearization wall defends exactly this norm."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/if_block_forms.almd", class = "fixture" },
]

[[contract]]
id        = "C-243"
spec      = "ALS-E14"
title     = "Block expressions yield their tail value identically on both targets"
statement = "ALS-E14: `{ stmt* tail? }` is an expression whose value is the tail (Unit when absent), usable as a let initializer, an if arm, or a function body, with block-scoped bindings — fixture pins 11 (let-initializer block) and 9 (if-arm blocks) byte-identical native <-> wasm. Shares the fixture with C-242 (the conditional's brace arms ARE block expressions)."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/if_block_forms.almd", class = "fixture" },
]

[[contract]]
id        = "C-244"
spec      = "ALS-E15"
title     = "While loops iterate their condition-tested body identically on both targets"
statement = "ALS-E15: `while cond { body }` — pre-tested, condition re-evaluated each iteration, Unit-valued, mutable state accumulating across iterations. Fixture pins a counter accumulation (10, terminal 5) and a DATA-DEPENDENT iteration count (Collatz from 16 -> 4 steps) byte-identical native <-> wasm. break/continue are outside this contract: the v1 renderer walls them loudly (#1277) and the Break/Continue coverage rows stay UNWRITTEN until that brick lands — no native-only semantics are normalized."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/while_loops.almd", class = "fixture" },
]

[[contract]]
id        = "C-245"
spec      = "ALS-E16"
title     = "String interpolation embeds each segment's canonical display identically on both targets"
statement = "ALS-E16: `${expr}` segments (variable, arithmetic, multiple per literal), the `\\${` literal escape, and per-type display — String verbatim, Int decimal, Bool lowercase, Float canonical (1.5 pinned; the exhaustive float display norm waits on the ALS-E3/#1261 ruling) — byte-identical native <-> wasm (fixture pins hello world / sum=3 / n*7=42 / mix / escaped ${not_interp} / float=1.5 bool=false)."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/string_interpolation.almd", class = "fixture" },
]

[[contract]]
id        = "C-246"
spec      = "ALS-E17"
title     = "Identifiers resolve to their nearest binding; unresolved names are check-time E003 naming the identifier"
statement = "ALS-E17: an identifier expression resolves to the nearest binding (shadowing per ALS-S1) with identical observables on both targets; an unresolved name is check-time E003 whose diagnostic CONTAINS the failed name (tests/ident_diag_test.rs) — no runtime name-resolution failure exists (a checked name always resolves; NameTotality is the v1 proof obligation for this)."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/string_interpolation.almd", class = "fixture" },
]

[[contract]]
id        = "C-247"
spec      = "ALS-E18"
title     = "Match expressions select the first matching arm identically on both targets; non-exhaustive is check-time E010"
statement = "ALS-E18: string-literal arms with wildcard, Int binder patterns with guards (first-match-wins pinned: 7 falls through both guards to `_`), Option some(x)/none, and Result ok(x)/err(e) with interpolated arm bodies — byte-identical native <-> wasm (fixture pins f64/odd/50/err:bad). Only the selected arm evaluates. The negative half: a non-exhaustive match is check-time E010 that NAMES the missing case (`missing none`) with an add-arms hint (tests/match_diag_test.rs) — no runtime match failure exists for a checked match."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/match_forms.almd", class = "fixture" },
]

[[contract]]
id        = "C-248"
spec      = "ALS-E19"
title     = "for-in iterates list-family heads with optional tuple destructuring identically on both targets"
statement = "ALS-E19: `for pat in iterable { body }` over List[Int] (accumulation 8), List[String] (per-element effect), and List[(Int, Int)] with HEAD DESTRUCTURING `for (i, v) in …` (53 including the S2 components) — byte-identical native <-> wasm. Elements bind in order exactly once; the iterable is evaluated once before the loop; body assignments accumulate across iterations. Range heads are C-238's evidence, map (k, v) iteration is C-240's; break/continue stay outside per #1277 (loud wall)."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/for_in_forms.almd", class = "fixture" },
]

[[contract]]
id        = "C-249"
spec      = "ALS-S2"
title     = "Tuple let-destructuring binds each component positionally, identically on both targets"
statement = "ALS-S2: `let (a, b) = e` binds each component to the corresponding positional value with the tuple's positional types — pinned in the for-in fixture (p + q participating in the 53 sum) and in tuple_ops.almd (C-236). One norm shared by three positions: let-destructure, fn-parameter tuple destructure, and the for-in head (ALS-E19)."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/for_in_forms.almd", class = "fixture" },
  { path = "spec/wasm_cross/tuple_ops.almd", class = "fixture" },
]

[[contract]]
id        = "C-250"
spec      = "ALS-E20"
title     = "Pipe and composition evaluate as plain application in order, identically on both targets"
statement = "ALS-E20: `x |> f` is observationally `f(x)` — single (10), chained (11), and through a HOF with a lambda (3); `f >> g` builds a callable applying left-to-right ((double >> inc)(4) = 9) — byte-identical native <-> wasm."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/pipe_compose_forms.almd", class = "fixture" },
]

[[contract]]
id        = "C-251"
spec      = "ALS-E21"
title     = "if let implicitly unwraps an Option scrutinee with a bare binder, identically on both targets"
statement = "ALS-E21: `if let x = o { .. } else { .. }` — the some path binds the INNER value (some(5) -> 10), the none path takes else (\"empty\"), byte-identical native <-> wasm. The binder is BARE by design: `if let some(x) = o` is a parse error (Swift-style implicit unwrap, not pattern matching — patterns belong to `match`, ALS-E18)."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/if_let_forms.almd", class = "fixture" },
]

[[contract]]
id        = "C-252"
spec      = "ALS-S3"
title     = "Expression statements and comments: Unit calls execute, discards are explicit, comments are invisible"
statement = "ALS-S3/S4: a Unit-returning call in statement position executes in order; a pure value is discarded only through `let _ = e`; and every comment form (leading line, trailing, multi-line block, before a declaration) is semantically invisible — the fixture threads all forms between two prints and both targets emit exactly first/second. The negative half is check-time E042 (ADR-0008 must-use): a bare statement-position Result is rejected with BOTH escapes spelled in the hint (`expr!` propagates, `let _ = expr` discards without propagating — C-217's pinned semantics), tested by tests/expr_stmt_diag_test.rs."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/expr_stmt_comment.almd", class = "fixture" },
]

[[contract]]
id        = "C-253"
spec      = "ALS-S5"
title     = "Place assignments and type ascription behave identically on both targets"
statement = "ALS-S5/E22: list-element `xs[i] = v`, record-field `p.x = v`, and map-key `m[k] = v` (upsert: insert-if-absent, overwrite-if-present — 6 then 100 pinned) all observe the new value on the next read, byte-identical native <-> wasm (fixture pins 21/12/6/100/7); the OOB-write abort stays C-067's ruling. The type-ascription expression `(e: T)` supplies an expected type without changing the value or its runtime representation."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/place_assign_ascription.almd", class = "fixture" },
]

[[contract]]
id        = "C-254"
spec      = "ALS-E22"
title     = "Type ascription supplies an expected type without changing the value on either target"
statement = "ALS-E22: `(e: T)` — the value is `e` unchanged ((7: Int) -> 7 pinned byte-identical), the ascription only feeds the checker's expected type and leaves the runtime representation untouched. Shares the place-assignment fixture with C-253 (the S5/E22 pair landed together)."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/place_assign_ascription.almd", class = "fixture" },
]

[[contract]]
id        = "C-255"
spec      = "ALS-E23"
title     = "Record literals, member reads, and spread updates preserve value semantics identically on both targets"
statement = "ALS-E23: the record literal, Int and String member reads, and the spread update `{ ...p, y: 20 }` (unlisted fields inherited) — with VALUE SEMANTICS pinned: the spread source is unchanged afterward (p.y still 2). Byte-identical native <-> wasm (fixture pins 3/a/21/a/2)."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/record_forms.almd", class = "fixture" },
]

[[contract]]
id        = "C-256"
spec      = "ALS-E24"
title     = "break and continue take effect at their statement position, identically on both targets and the interp"
statement = "ALS-E24 (#1277/#1306): guarded `continue` skips the rest of the iteration (the for-range STEP still runs — the lowering restructures into the guarded rest, structurally excluding the skipped-step infinite loop), guarded `break` exits immediately AT ITS POSITION — the mid-body shape pins 3, not the deferred-flag 4 that BOTH v1 legs previously agreed on while v0 and the interp said 3. That divergence class is invisible to the 2-way native==wasm gate by construction, which is why this fixture lives in the 3-way-voted spec/wasm_cross set. A break over a heap frame stays a loud wall (an early exit must not skip a per-iteration Drop)."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/loop_break_continue.almd", class = "fixture" },
]

[[contract]]
id        = "C-257"
spec      = "ALS-E25"
title     = "The scalar error operators evaluate identically on both targets"
statement = "ALS-E25: postfix `!` (unwrap-propagate, effect-fn only), postfix `?` (Result -> Option: err -> none, ok(4) -> some(4)), and `??` fallback, through both polarities of an effect call — byte-identical native <-> wasm (fixture pins 5/-1/4). The deep propagation norms stay with the ADR-0006/0008 contracts (C-216/C-217/C-222). ExprKind::Try is parser-unreachable dead vocabulary (the surface `?` builds ToOption); OptionalChain stays outside this contract behind the untracked-subject wall."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/error_operators.almd", class = "fixture" },
]

[[contract]]
id        = "C-258"
spec      = "ALS-E26"
title     = "Named calls and lambdas evaluate identically on both targets"
statement = "ALS-E26: positional named calls, typed/untyped/zero-param lambdas, and inline lambdas in HOF argument position — arguments evaluate left-to-right exactly once, by value; a lambda is a function value behaving identically whether bound-then-called or passed (fixture pins 5/10/42/12 byte-identical native <-> wasm). Arity mismatch is check-time E004."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/call_lambda_ctor.almd", class = "fixture" },
]

[[contract]]
id        = "C-259"
spec      = "ALS-E27"
title     = "Variant constructor references build values eliminated identically by match on both targets"
statement = "ALS-E27: a payload constructor call Rgb(1, 2, 3) and a bare nullary reference Green build variant values whose match elimination is byte-identical native <-> wasm (fixture pins 4/20). Exhaustiveness is ALS-E18's E010."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/call_lambda_ctor.almd", class = "fixture" },
]

[[contract]]
id        = "C-260"
spec      = "ALS-D1"
title     = "The declaration family compiles and runs identically on both targets"
statement = "ALS-D1: one program threading the declaration family — explicit import (json parse -> stringify round-trip prints [1,2]), a variant type declaration eliminated by match, top-level lets (Int and String) read from a fn body, fn and effect-fn declarations — byte-identical native <-> wasm (fixture pins 300/hi/careful/[1,2]). Protocol resolution stays with C-094/C-126; test blocks are evidenced by the whole spec/lang suite running its wasm leg. The fixture now leads with a `module` header under its comment block — the shape #1323 made un-formattable — so it also pins that a commented module header survives fmt; the multi-module execution evidence stays with the spec/integration/modules suite. `strict` is deliberately OUTSIDE: parsed but consumed by nothing (#1321), its row stays UNWRITTEN."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/declaration_forms.almd", class = "fixture" },
]

[[contract]]
id        = "C-261"
spec      = "ALS-E3"
title     = "Canonical float values including the negative-zero sign display identically on both targets"
statement = "ALS-E3 (partial — the fmt-stable half): decimal float literals with digits on both sides of the dot, and float.to_string's canonical display — 1.5, 0.5, the SIGN-PRESERVED -0.0, and the arithmetic result 1.75 — byte-identical native <-> wasm. The FORM half (exponents, underscores, out-of-range literals) is accepted by the parser but OPEN pending #1261's fmt-preservation ruling and is deliberately outside this contract."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/string_float_stable.almd", class = "fixture" },
]

[[contract]]
id        = "C-262"
spec      = "ALS-E5"
title     = "The fmt-stable string escapes and the empty string evaluate identically on both targets"
statement = "ALS-E5 (partial — the fmt-stable half): `\\t` -> tab, `\\n` -> newline, `\\\\` -> one backslash (spellings AND values fmt-stable, pinned in visible brackets), and the empty string — byte-identical native <-> wasm. The FORM half (unicode escapes, heredocs, delimiter choice) and the silent-pass-through of unknown escapes are OPEN pending #1263/#1264 and deliberately outside this contract."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/string_float_stable.almd", class = "fixture" },
]

[[contract]]
id        = "C-263"
spec      = "ALS-D2"
title     = "Recovery nodes never appear in accepted programs; a broken file still reports past its first error"
statement = "ALS-D2: ExprKind::Error / Stmt::Error are recovery vocabulary — placeholders that keep diagnostic collection going past a parse error, never present in an accepted program. Both directions are pinned: the whole spec/wasm_cross corpus (300+ accepted, both-target-executed programs) parses with ZERO recovery nodes, and a deliberately broken file reports diagnostics while parsing CONTINUES past the error (the following declaration is still seen). The fixture leg is any accepted program executing identically with zero recovery artifacts."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/declaration_forms.almd", class = "fixture" },
  { path = "tests/parser_recovery_test.rs", name = "accepted_corpus_contains_zero_recovery_nodes", class = "by-construction" },
  { path = "tests/parser_recovery_test.rs", name = "broken_file_recovers_past_the_error", class = "by-construction" },
]

[[contract]]
id        = "C-264"
spec      = "ALS-E28"
title     = "Scalar-field optional chains yield some/none identically on both targets"
statement = "ALS-E28: `p?.x` over a SCALAR record field evaluates to some(v.x) on the some path and none on the none path, byte-identical native <-> wasm (fixture pins 3 and -9 through `??`). The whole chain is Option-typed — `?.` wraps 'read if present', the unwrapping belongs to `??`/match. #1329 un-walled the wasm leg by moving the desugar to the shared optimize cut point so the synthesized tail-match helper is a Named call that lowers in every position (bind, call argument, `??` operand). HEAP-payload fields stay on the older route: the identical user-written helper hits the pre-existing heap-result-match brick (measured, not an OptionalChain defect), so that class is deliberately outside this contract."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/optional_chain_scalar.almd", class = "fixture" },
]

[[contract]]
id        = "C-265"
spec      = "ALS-S6"
title     = "The guard statement's pass and raise paths behave identically on both targets"
statement = "ALS-S6: `guard cond else err(e)` is an EARLY EXIT — on the true path the following statements run and the tail value flows out (10 pinned); on the false path the else's err propagates to the caller, where a `!` or match receives it (\"must be positive\" pinned) — byte-identical native <-> wasm. Both raise spellings (`err(e)` and `err(e)!`) normalize identically since #1336, which found the recognizer accepted only the Unwrap-wrapped leaf: the class was wider than guard (a hand-written `{ let m = ..; if m > 0 then m * 2 else err(e) }` walled the same way). `guard let` is a separate class (frontend-desugared to a tail-position variant match, still walled) and is outside this contract."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/guard_statement.almd", class = "fixture" },
]

[[contract]]
id        = "C-266"
spec      = "ALS-S7"
title     = "guard let binds the success payload and raises on the failing polarity, identically on both targets"
statement = "ALS-S7: `guard let x = scrut else err(e)` binds the Ok/Some payload for the rest of the block on the success polarity (int.parse(\"21\") -> 42 pinned) and propagates the else's err to the caller on the failing one (\"not a number\" pinned) — byte-identical native <-> wasm; chained guard lets fold into a single carrier bind (#1340). The wall that stood here was POSITIONAL, not polarity- or pattern-shaped: guard let desugars in the frontend to a BLOCK-NESTED variant match, and auto_wrap_abi_body retypes only the body ROOT to the Result carrier, so the nested match kept its scalar type and fell off the carrier-typed tail path. The fix taught the proven bind-shape normalizer about variant matches — the same machinery ALS-S6 uses."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/guard_let_statement.almd", class = "fixture" },
]

[[contract]]
id        = "C-267"
spec      = "ALS-E29"
title     = "The binary operator surface evaluates identically on both targets, including the signed division rulings"
statement = "ALS-E29: Int arithmetic with the SIGNED rulings pinned — `/` truncates toward zero (-7 / 3 = -2, not the floor -3) and `%` takes the dividend's sign (-7 % 3 = -1), the C/Rust convention rather than floor division — plus Float addition, the OVERLOADED `+` on String (catdog) and List (length 3), all six comparisons, `==`/`!=` on Int and String, the `and`/`or` keywords (there is no `&&`/`||`; negation is `not`, ALS-E7), and `**` which desugars to a math.pow/math.fpow call credited 1:1 in the caps ledger. Byte-identical native <-> wasm across 14 observation points."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/binary_operators.almd", class = "fixture" },
]

[[contract]]
id        = "C-268"
spec      = "ALS-E30"
title     = "A `_` in call-argument position is a check-time E046, identically on both targets"
statement = "ALS-E30: `add(_, 10)` is REJECTED at check time as E046 naming the argument position, and the hint steers to a lambda (`(x) => add(x, 10)`) while explicitly denying partial application — binding the form types as the RETURN, and in pipe position it counts as an extra argument (E004). Before the ruling the checker typed the Placeholder as Unknown and it sailed into codegen, emitting invalid Rust behind the compiler-blaming banner (the #1266 class). Type check precedes the target split, so the diagnostic is identical native <-> wasm — the same structure as the E027 fan.timeout tombstone (C-006). The SIBLING forms in ALS-E30, the typed holes `_` (expression position) and `todo(msg)`, deliberately carry NO cross-target promise: they abort the native leg as a panic (exit 101) while the wasm renderer refuses the whole function at build time regardless of whether the hole is reachable, so no cross-target fixture can exist for them and none is claimed."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "tests/diagnostics/e046-call-arg-placeholder/broken.almd", class = "fixture" },
  { path = "tests/diagnostic_harness_test.rs", name = "broken_files_produce_expected_diagnostics", class = "fixture" },
  { path = "tests/typed_hole_diag_test.rs", name = "typed_hole_panics_naming_the_almide_source_line", class = "by-construction" },
]

# ── NESTED VARIANT MATCH IN BIND POSITION, AND THE CROSS-FAMILY REFUSAL ─
[[contract]]
id        = "C-269"
spec      = "ALS-M1"
title     = "A nested variant match bound to a let executes identically on both targets; a constructor pattern the subject lacks is a check-time E048 on both"
statement = "ALS-M1, positive half: a variant `match` whose arm BODY is itself a variant match, bound to a `let` under an explicit `Result[T, E]` carrier and unwrapped with `!`, binds every payload at both nesting levels and produces byte-identical observables native <-> wasm — on the value path and on BOTH failure depths, for a scalar (Int) and a heap (String) payload. The bind is heap-typed, so branch_lift hoists it into a synthesized tail helper; the arm binders must survive that lift carrying their concrete types. NEGATIVE half, the same rule read from the other side: a constructor pattern must name a case the SUBJECT has, and one that does not is rejected at CHECK time as E048 — identically on both targets, since type check precedes the target split. Four cells: `ok`/`err` over an Option, `some`/`none` over a Result (both carry a `try:` naming the same arm position in the right family), a builtin carrier over a USER variant, and a user case borrowed from another variant or aimed at a carrier (both hint the subject's own case list). That refusal is the substance of #1341, whose own diagnosis was WRONG: the reported repro spelled the arms `ok(..)` over a `list.get` subject (an `Int?`), so branch_lift was innocent — `free_vars` already excludes arm binders and the binder was never a helper param — and the program was simply ill-typed. The checker rejected constructor patterns aimed at plain SCALARS only, so the payload bound as `Ty::Unknown`, survived to codegen, and detonated there: a `[COMPILER BUG] internal type resolution failed` banner naming `branch_lift_synth_0` on native and an executable-subset wall on wasm for the family cells, and INVALID GENERATED RUST on native for the foreign-case cell. Compiler-blaming failures for plain type errors (the #1266 misattribution class); E048 replaces them with one named error carrying the fix. Only structurally-known subjects are claimed — a type under inference, or a `Named` opaque alias whose destructure is legal, stays silent."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/nested_variant_match_bind.almd", class = "fixture" },
  { path = "tests/diagnostic_harness_test.rs", class = "by-construction", name = "broken_files_produce_expected_diagnostics" },
]

[[contract]]
id        = "C-270"
spec      = "ALS-R6"
title     = "The shared fp16 block scale decodes the whole fp16 domain identically on both targets"
statement = "Q1_0 and Q8_0 read the same 2-byte fp16 block scale, and native's `fp16_bits_to_f32` (runtime/rs/src/matrix_p2.rs) and wasm's `__fp16_bits`/`__q10_scale` (stdlib/matrix_activations.almd) were THREE separate answers apart on it — all three closed here, pinned across ten raw scales through both q1_0 entry points (`select_rows_q1_0` and `from_q1_0_bytes`). (1) SUBNORMALS: wasm returned the sign bit alone for every exp==0 pattern, flushing 2^-24 … 6.0975e-5 to zero — a silent MAGNITUDE error on real weights, not just a sign one; wasm now normalizes the mantissa the way native's `while m & 0x400 == 0` loop does. (2) exp==31: wasm had no Inf/NaN branch and fell into the normal-number formula, decoding +Inf as the finite 65536 and a NaN pattern as 65600; it now emits the 0xFF exponent, and `float.to_string` already renders inf/-inf/NaN identically on both legs. (3) The SIGN OF ZERO (#1358) — the dequantization-zero ruling recorded on C-229. Non-zero scales are bit-for-bit untouched (1.0, 65504, both smallest-normal and largest-subnormal boundaries pinned with their negated twins). The CONTRAST is pinned in the same fixture: `from_bytes_f16_le`/`_f32_le` are float DECODERS, so a stored -0.0 is the datum and its sign is information — it survives on both targets, and the ruling governs dequantization PRODUCTS only."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/matrix_q_fp16_scale_domain.almd", class = "fixture" },
]

# ── `??` WITH AN INLINE-`!` FALLBACK OVER A HANDLE PAYLOAD ─────────────
[[contract]]
id        = "C-271"
spec      = "ALS-E25"
title     = "`carrier ?? call!` yields the fallback's PAYLOAD, never its Result block, for a handle-carrying payload on both targets"
statement = "A `??` whose FALLBACK is an inline propagating unwrap (`a ?? f(..)!`) produces byte-identical observables native <-> wasm when the FALLBACK ARM RUNS and the payload is heap/handle-carrying (a json `Value`, `String`, `List[Int]`, `Bytes`). Thirteen cells: the three reported json payloads (array, object, number), the ok path (the carrier holds a value, so a fallback that would itself fail is never evaluated — `??` stays lazy in its fallback), the `match { ok(p) => p, err(_) => f(..)! }` spelling that reaches the same arm, an Option carrier, `?`-to-Option followed by `??`, a nested `??` supplying the outer fallback, the String / List / Bytes carriers, the BOTH-fail path (the fallback's own error is the enclosing fn's error), and the scalar (Int) control that never took the handle path. Found by `tools/xtarget-fuzz` (--family all --seed 13322026081302 --index 297) as #1375, a SILENT WRONG VALUE on wasm: `json.parse(\"hi\") ?? json.parse(\"[1,2]\")!` printed `true`, which is `stringify` reading the ok DISCRIMINANT `1` out of the fallback's Result block. The heap-arm lowering applied the `e! = e` pass-through — the identity in a RESULT-typed arm, which propagates the Result verbatim — to a PAYLOAD-typed arm, so the whole Result block landed where the merge expects the payload handle. The `match` spelling detonated a second way, via `branch_lift` hoisting a `!`-bearing arm into a non-effect payload-returning helper: rustc E0277 on native, an always-wrapped body read as the bare payload on wasm. `spec/wasm_cross/declaration_forms.almd` carried the shape only on the OK path, where the fallback arm never runs — the fixture that tests what someone already thought of."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/unwrap_or_unwrap_fallback.almd", class = "fixture" },
]

# ── fs.list_dir IS THE WHOLE DIRECTORY, AT EVERY READDIR BUFFER BOUNDARY ─
[[contract]]
id        = "C-272"
spec      = "ALS-R6"
title     = "`fs.list_dir` returns EVERY entry on both targets, whatever the directory's size, sorted by bytes"
statement = "`fs.list_dir(path)` yields the same `List[String]` native <-> wasm for a directory of ANY size, and the order is contractual: both legs sort the names lexicographically by BYTES (native `names.sort()` over `String` = `str` Ord; wasm's insertion sort over `$str_lt`), so the filesystem's arbitrary readdir order is never observable. WASI's `fd_readdir` is a RESUMABLE api — one pass fills the caller's buffer and reports the bytes written, and the listing continues from the last entry's `d_next` cookie until a pass returns FEWER bytes than the buffer — but the wasm floor (`$read_dir`) did ONE `cookie=0` pass into 4 KiB and parsed whatever landed there. Every directory whose dirents did not fit came back SILENTLY SHORT still wrapped `ok(...)`: a wrong value, not a wall and not an err, indistinguishable from a genuinely smaller directory (#1384 — 200 entries answered 130 = (4096-51)/31, 60 long-named ones answered 41). Six sizes pin the sweep, measured on both legs before and after: empty (0/0) and one (1/1) unchanged; 4096 bytes of dirents EXACTLY (130 entries) already agreed and must keep agreeing, because a pass that exactly fills the buffer is NOT the end and the sweep has to re-read rather than stop; one entry over (131) was 130; a record whose HEADER fits at 4050 but whose NAME runs past 4096 (130) was 129, since the old parser trusted `d_namlen` and read past `bufused`; 200 entries (200) was 130; and 60 x 76-byte names (60) was 41 WITH heap garbage spliced onto the first name — the straddling read, observable in stdout. The same site dropped `fd_readdir`'s errno, so a failed listing read the zeroed `$bufused` scratch and returned `ok([])`; the errno is now kept and a readdir failure becomes the same Err as a failed `path_open`. `$read_dir` was FALLBACK_ONLY in proofs/wat-prelude-audit.toml — no `spec/wasm_cross` fixture reached `fs.list_dir` at all, which is exactly why one pass survived from the beginning; the fixture below upgrades it to CORPUS."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/fs_list_dir_multipass.almd", class = "fixture" },
]

# ── fs WRITE-SIDE ERROR TEXT ───────────────────────────────────────────
[[contract]]
id        = "C-273"
spec      = "ALS-R6"
title     = "The fs write-side floors answer with native's std::io Display text, and fd_write's errno is not dropped"
statement = "Every wasm fs WRITE-side floor answers a failure with the SAME native `std::io` Display text the native leg produces, through the one errno mapping `$read_text_file` already carried. Before #1385 each floor collapsed its whole failure space to one fixed string — `$write_text_file` \"write failed\", `$make_dir` \"mkdir failed\", `$remove_all` \"remove failed\", `$read_dir` \"directory not found\", `$rename` a hand-rolled NOENT/ACCES-only half — so the Result BRANCH agreed cross-target while the PAYLOAD, the half a user actually reads, diverged: eight measured rows, eight divergences. All eight close. Seven are fixture-pinned: `fs.write` onto a directory (`Is a directory (os error 21)`) and through a file component (`Not a directory (os error 20)`), `fs.mkdir_p` through a file component (same), `fs.remove_all` of a missing path and `fs.list_dir` of a missing path and `fs.rename` of a missing source (`No such file or directory (os error 2)`), `fs.list_dir` of a plain file (`Not a directory (os error 20)`). The eighth and ninth — `fs.write` to a mode-444 file and into a mode-555 directory, both `Permission denied (os error 13)` — are MEASURED equal on both legs but NOT fixture-reachable: setting a mode needs `process.exec`, which walls on the wasm leg, so no `spec/wasm_cross` program can build the precondition. The mapped errno set is exactly {NOENT 44, ACCES 2, NOTDIR 54, ISDIR 31} and that boundary is the DELIBERATE, BOUNDED residue of this contract: those four are the errnos whose native number AND Display text are identical on every Unix host, so one baked data segment is correct in a portable `.wasm`. ENOTEMPTY (39 Linux / 66 macOS), ENAMETOOLONG (36 / 63) and EXDEV (same number, different text — `Invalid cross-device link` vs `Cross-device link`) are NOT mapped, and an errno outside the set keeps its floor's generic string; mapping them would trade a visible divergence for a host-specific one baked into a portable artifact. SECOND, latent half: `$write_text_file` built `Ok(())` UNCONDITIONALLY — `fd_write`'s errno was dropped, so ENOSPC / EIO / a short write read as SUCCESS. The sweep that found it could not reach it (every failure it produced was caught by `path_open` first), which is what makes a silent success worth fixing before it is live. The write is now native `fs::write`'s write_all LOOP: a partial `fd_write` resumes at the unwritten bytes, its errno feeds the same mapping, and an accepted-0-bytes call is native's `ErrorKind::WriteZero` (`failed to write whole buffer` — Rust's own const message, not an OS string, so it is platform-independent like the four). The 12800-byte body and the zero-length write in the fixture are the loop's many-iteration and zero-iteration edges. THIRD, the neighbourhood sweep #1368's follow-up did for the read side, done for the write side: `path_create_directory` (the intermediate-prefix drops are by design — the FINAL call's errno subsumes them and is reported), `path_remove_directory` and `path_unlink_file` (both already feed `$remove_path`'s first-error verdict), `path_rename` (captured), `fd_readdir` inside `$remove_path` (its drop cannot manufacture a silent SUCCESS: a pass that reads nothing declares the directory emptied, and `path_remove_directory` then refuses a non-empty one, so the failure still surfaces as an err. It is not a CLEAN error path — a readdir that starts failing mid-walk re-scans the previous pass's stale buffer — but every exit from that shape is an err, and the shape is untouched here), and the `fd_close` drops (native drops its `File` and ignores the close result identically). `fd_seek` / `fd_filestat_set_size` are requested as path_open RIGHTS but never imported or called, so there is nothing to check. The four errno branches live in ONE Rust source (`fs_errno_msg_wat`) rendered inline at each site — §4.1 forbids a new hand-written WAT function, and one source is what stops the floors drifting apart again."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/fs_write_errno.almd", class = "fixture" },
]

# ── ADR-0006 FALLIBLE CALLBACK FOR THE fs STREAMING WALKERS ────────────
[[contract]]
id        = "C-274"
spec      = "ALS-R7"
title     = "A fallible callback makes an fs streaming walk fallible, stopping at the first err"
statement = "ADR-0006's 1-bit fallibility polymorphism reaches the fs streaming walkers (#1144 — the cell C-220 tracked as an omission). A callback whose body propagates (`fs.fold_lines(p, z, (a, l) => step(a, l)!)`, `fs.for_each_line(p, (l) => note(l)!)`) makes the whole call fallible: the checker rewrites the callee to the internal carriers `fs.__fallible_fold_lines` / `fs.__fallible_for_each_line` exactly as `list.map` becomes `list.__fallible_map`, and the carriers are not spellings — naming one in source is E043, now on the same footing as the list family. The NORMATIVE observable is the callback-invocation sequence, not merely the err value: the callback runs through the failing line and is NEVER invoked for any later line (first-err short-circuit), and the callback's own err message rides the caller's err channel unwrapped. Both are byte-identical native <-> wasm for the `Map[String, Int]` accumulator, the shape that has a wasm self-host twin (`fs_fallible_fold_lines_msi`, stdlib/fs_fold_lines.almd); the fixture pins the invocation TRACE (each invocation appends its line to a log read back afterwards) across five shapes — full ok walk, mid-file err, empty file (zero invocations), missing file (the reader's err before any invocation), and an err on the very first line — so an implementation that returned the right err while continuing to call the callback fails, which is precisely the difference between short-circuit and error-collection. Where the walk STOPS READING is deliberately outside the cross-target promise: the native carrier returns out of the BufReader loop at the failing line, leaving the rest of the file unread, while the wasm self-host had already read the file before cutting the first line — the same native-only carve-out C-220 makes for the streaming RSS property. Two cells are REASONED omissions, not gaps: `fold_lines_range` and `fold_lines_chunked` get no fallible form, because a partitioned walk has no defined first err (which chunk errs first is a thread-schedule observable), so an erring chunk body handles its own error. The matrix in tests/fs_streaming_family_gate_test.rs makes both columns executable — it cross-checks the carriers against the frontend's FALLIBLE_HOF_FS dispatch table, so a carrier nothing rewrites to, or a rewrite with no carrier, fails the build. The `for_each_line` cell is native-only, inheriting the PLAIN form's wall (it has no wasm twin at all); spec/stdlib/fs_streaming_test.almd pins its first-err trace on the native leg. That wall is placed, not merely inherited: the capability witness admits `fs.__fallible_for_each_line` even though no wasm twin exists, so `lower_function` stays TOTAL and the honest refusal lands in the RENDERER on the unlinked twin name — the same layer the total family's `_x` accumulator cells wall at. Leaving the carrier unadmitted refused it in lowering instead, which walls the enclosing REAL function and would have forced a row into proofs/walled-real-baseline.txt — a file whose target is permanently 0 (the wall=0 endgame claim, burned down from 217). A missing capability admission is a lowering bug, never a reason to lower that claim. One frontend bug this cell exposed: `base_stdlib_fn_name` split the mono suffix at the FIRST `__`, so a carrier whose name BEGINS with `__` based to the empty string and every name-keyed decision (registry routing, match read-shape tracking) silently missed — the whole function walled instead of routing."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/fs_fallible_stream_callback.almd", class = "fixture" },
  { path = "spec/stdlib/fs_streaming_test.almd", class = "fixture" },
  { path = "tests/fs_streaming_family_gate_test.rs", name = "every_streaming_cell_decides_its_fallible_form", class = "fixture" },

# ── http RESPONSE HEADERS ──────────────────────────────────────────────
[[contract]]
id        = "C-275"
spec      = "ALS-R7"
title     = "The http response-header surface answers identically on both targets, with case-insensitive field names"
statement = "ALS-R7: header field names are CASE-INSENSITIVE under an ASCII fold (RFC 9110 §5.1 — a field name is a `token`, so the fold matches `eq_ignore_ascii_case` and must not reach for the Unicode string.to_lower), a response carries AT MOST ONE entry per field name because every write upserts the matching entry's value in place, and `with_headers` yields EXACTLY the caller's map — no seeded Content-Type, which only `response` (text/plain) and `json` (application/json) carry because they give the caller no other way to name one, while `redirect` carries only Location. #1352 measured NINE divergent cells here: native seeded a Content-Type in `with_headers` and matched case-insensitively in `get_header` while the self-host seeded nothing and matched with `==`, so a wasm-target program inspecting a response it had just built silently read `none`. The seeding side lost the ruling on REACHABILITY — with the seed, `with_headers` cannot express a response without a Content-Type (there is no remove_header), without it both shapes are reachable — and `set_header`'s case-SENSITIVE match lost on both legs at once: setting `content-type` on a response holding `Content-Type` appended a second entry the case-insensitive lookup then shadowed, so the write vanished natively too."
since     = "0.57.1"
status    = "active"
evidence  = [
  { path = "spec/wasm_cross/http_response_headers.almd", class = "fixture" },
]
