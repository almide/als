// GENERATED by `lake exe conformancegen` (crates/almide-edit-belt) — DO NOT EDIT.
// The sibling .expected file is the compiled Lean evaluator's kernel trace,
// pinned to Corpus.lean by the conformancegen --check CI step; generator
// totality is kernel-checked (corpus_total, by rfl + eval_sound + ev_det).

fn s0(x: String) -> String = x

fn p0(x: Int) -> Result[Int, String] = err("hotel")

fn p1(x: Int) -> Result[Int, String] = ok(p0(x) ?? 6)

fn p2(x: Int) -> Result[Int, String] = ok(x)

effect fn e0(x: Int) -> Int = {
  println("hotel")
  println("oscar")
  7
}

effect fn main() -> Unit = {
  let m0: Result[Int, String] = err("bravo")
  match m0 {
    ok(_v) => println("juliet"),
    err(_e) => println("echo"),
  }
  let m1: Result[Int, String] = p1(0)
  match m1 {
    ok(_v) => println("india"),
    err(_e) => println("golf"),
  }
  let m2: Result[Int, String] = p1(7)
  match m2 {
    ok(_v) => println("mike"),
    err(_e) => println("papa"),
  }
  println(s0("papa"))
  match e0(9) {
    ok(_v) => println("oscar"),
    err(_e) => println("kilo"),
  }
  let m5: Result[Int, String] = p2(8)
  match m5 {
    ok(_v) => println("foxtrot"),
    err(_e) => println("foxtrot"),
  }
  match p2(0) {
    ok(_v) => println("echo"),
    err(_e) => println("oscar"),
  }
}
