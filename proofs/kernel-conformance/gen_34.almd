// GENERATED by `lake exe conformancegen` (crates/almide-edit-belt) — DO NOT EDIT.
// The sibling .expected file is the compiled Lean evaluator's kernel trace,
// pinned to Corpus.lean by the conformancegen --check CI step; generator
// totality is kernel-checked (corpus_total, by rfl + eval_sound + ev_det).

fn s0(x: String) -> String = x

fn p0(x: Int) -> Result[Int, String] = ok(6)

fn p1(x: Int) -> Result[Int, String] = ok(p0(x) ?? 5)

fn p2(x: Int) -> Result[Int, String] = ok(9)

effect fn e0(x: Int) -> Int = {
  let me00: Result[Int, String] = p0(x)
  match me00 {
    ok(_v) => println("papa"),
    err(_e) => println("mike"),
  }
  x
}

effect fn e1(x: Int) -> Int = 9

effect fn main() -> Unit = {
  let m0: Result[Int, String] = err("india")
  match m0 {
    ok(_v) => println("oscar"),
    err(_e) => println("oscar"),
  }
  let m1: Result[Int, String] = err("juliet")
  match m1 {
    ok(_v) => println("hotel"),
    err(_e) => println("lima"),
  }
  let m2: Result[Int, String] = p2(4)
  match m2 {
    ok(_v) => println("india"),
    err(_e) => println("alpha"),
  }
  println("papa")
  match p1(5) {
    ok(_v) => println("hotel"),
    err(_e) => println("echo"),
  }
  match p1(5) {
    ok(_v) => println("foxtrot"),
    err(_e) => println("echo"),
  }
}
