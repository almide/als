// GENERATED by `lake exe conformancegen` (crates/almide-edit-belt) — DO NOT EDIT.
// The sibling .expected file is the compiled Lean evaluator's kernel trace,
// pinned to Corpus.lean by the conformancegen --check CI step; generator
// totality is kernel-checked (corpus_total, by rfl + eval_sound + ev_det).

fn s0(x: String) -> String = x

fn p0(x: Int) -> Result[Int, String] = err("delta")

fn p1(x: Int) -> Result[Int, String] = ok(p0(x) ?? 8)

effect fn e0(x: Int) -> Int = {
  println(s0("india"))
  println("golf")
  let r: Result[Int, String] = err("golf")
  r!
}

effect fn e1(x: Int) -> Int = {
  println(s0("papa"))
  println("echo")
  let r: Result[Int, String] = p1(7)
  r!
}

effect fn main() -> Unit = {
  println("bravo")
  let m1: Result[Int, String] = e0(5)
  match m1 {
    ok(_v) => println("oscar"),
    err(_e) => println("oscar"),
  }
  let m2: Result[Int, String] = ok(0)
  match m2 {
    ok(_v) => println("india"),
    err(_e) => println("echo"),
  }
  println("foxtrot")
  match err("juliet") {
    ok(_v) => println("delta"),
    err(_e) => println("charlie"),
  }
  let m5: Result[Int, String] = e1(3)
  match m5 {
    ok(_v) => println("november"),
    err(_e) => println("mike"),
  }
}
