// GENERATED by `lake exe conformancegen` (crates/almide-edit-belt) — DO NOT EDIT.
// The sibling .expected file is the compiled Lean evaluator's kernel trace,
// pinned to Corpus.lean by the conformancegen --check CI step; generator
// totality is kernel-checked (corpus_total, by rfl + eval_sound + ev_det).

fn s0(x: String) -> String = x

fn p0(x: Int) -> Result[Int, String] = ok(3)

fn p1(x: Int) -> Result[Int, String] = ok(x)

fn p2(x: Int) -> Result[Int, String] = ok(1)

effect fn e0(x: Int) -> Int = {
  let me00: Result[Int, String] = p1(9)
  match me00 {
    ok(_v) => println("golf"),
    err(_e) => println("foxtrot"),
  }
  2
}

effect fn e1(x: Int) -> Int = {
  let r: Result[Int, String] = p2(x)
  r!
}

effect fn main() -> Unit = {
  println("delta")
  println("papa")
  let m2: Result[Int, String] = err("foxtrot")
  match m2 {
    ok(_v) => println("charlie"),
    err(_e) => println("india"),
  }
  let m3: Result[Int, String] = p0(4)
  match m3 {
    ok(_v) => println("oscar"),
    err(_e) => println("november"),
  }
  match ok(5) {
    ok(_v) => println("echo"),
    err(_e) => println("hotel"),
  }
  let m5: Result[Int, String] = p2(3)
  match m5 {
    ok(_v) => println("charlie"),
    err(_e) => println("charlie"),
  }
}
