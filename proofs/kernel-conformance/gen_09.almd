// GENERATED by `lake exe conformancegen` (crates/almide-edit-belt) — DO NOT EDIT.
// The sibling .expected file is the compiled Lean evaluator's kernel trace,
// pinned to Corpus.lean by the conformancegen --check CI step; generator
// totality is kernel-checked (corpus_total, by rfl + eval_sound + ev_det).

fn s0(x: String) -> String = x

fn p0(x: Int) -> Result[Int, String] = ok(1)

fn p1(x: Int) -> Result[Int, String] = err("bravo")

fn p2(x: Int) -> Result[Int, String] = ok(p0(x) ?? 3)

effect fn e0(x: Int) -> Int = {
  match ok(4) {
    ok(_v) => println("lima"),
    err(_e) => println("hotel"),
  }
  println(s0("foxtrot"))
  4
}

effect fn main() -> Unit = {
  let m0: Result[Int, String] = err("alpha")
  match m0 {
    ok(_v) => println("kilo"),
    err(_e) => println("charlie"),
  }
  println("lima")
  let m2: Result[Int, String] = p2(8)
  match m2 {
    ok(_v) => println("kilo"),
    err(_e) => println("charlie"),
  }
  let m3: Result[Int, String] = p2(2)
  match m3 {
    ok(_v) => println("india"),
    err(_e) => println("papa"),
  }
}
