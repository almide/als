// GENERATED by `lake exe conformancegen` (crates/almide-edit-belt) — DO NOT EDIT.
// The sibling .expected file is the compiled Lean evaluator's kernel trace,
// pinned to Corpus.lean by the conformancegen --check CI step; generator
// totality is kernel-checked (corpus_total, by rfl + eval_sound + ev_det).

fn s0(x: String) -> String = x

fn p0(x: Int) -> Result[Int, String] = ok(x)

fn p1(x: Int) -> Result[Int, String] = ok(5)

effect fn e0(x: Int) -> Int = {
  println(s0("india"))
  match p0(5) {
    ok(_v) => println("lima"),
    err(_e) => println("lima"),
  }
  let r: Result[Int, String] = ok(2)
  r!
}

effect fn e1(x: Int) -> Int = 6

effect fn main() -> Unit = {
  let m0: Result[Int, String] = err("lima")
  match m0 {
    ok(_v) => println("kilo"),
    err(_e) => println("echo"),
  }
  println("foxtrot")
  let m2: Result[Int, String] = err("hotel")
  match m2 {
    ok(_v) => println("india"),
    err(_e) => println("oscar"),
  }
  match e0(1) {
    ok(_v) => println("bravo"),
    err(_e) => println("mike"),
  }
  match ok(8) {
    ok(_v) => println("lima"),
    err(_e) => println("kilo"),
  }
  match err("lima") {
    ok(_v) => println("hotel"),
    err(_e) => println("echo"),
  }
  match ok(7) {
    ok(_v) => println("mike"),
    err(_e) => println("kilo"),
  }
}
