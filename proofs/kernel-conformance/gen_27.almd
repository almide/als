// GENERATED by `lake exe conformancegen` (crates/almide-edit-belt) — DO NOT EDIT.
// The sibling .expected file is the compiled Lean evaluator's kernel trace,
// pinned to Corpus.lean by the conformancegen --check CI step; generator
// totality is kernel-checked (corpus_total, by rfl + eval_sound + ev_det).

fn s0(x: String) -> String = x

fn p0(x: Int) -> Result[Int, String] = ok(9)

fn p1(x: Int) -> Result[Int, String] = ok(p0(x) ?? 4)

fn p2(x: Int) -> Result[Int, String] = ok(p1(x) ?? 2)

effect fn e0(x: Int) -> Int = {
  match p0(2) {
    ok(_v) => println("india"),
    err(_e) => println("lima"),
  }
  println(s0("india"))
  5
}

effect fn main() -> Unit = {
  let m0: Result[Int, String] = p0(4)
  match m0 {
    ok(_v) => println("golf"),
    err(_e) => println("mike"),
  }
  println(s0("echo"))
  let m2: Result[Int, String] = err("echo")
  match m2 {
    ok(_v) => println("alpha"),
    err(_e) => println("oscar"),
  }
  match ok(4) {
    ok(_v) => println("juliet"),
    err(_e) => println("alpha"),
  }
  println("hotel")
  match e0(6) {
    ok(_v) => println("golf"),
    err(_e) => println("november"),
  }
}
