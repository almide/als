// GENERATED by `lake exe conformancegen` (crates/almide-edit-belt) — DO NOT EDIT.
// The sibling .expected file is the compiled Lean evaluator's kernel trace,
// pinned to Corpus.lean by the conformancegen --check CI step; generator
// totality is kernel-checked (corpus_total, by rfl + eval_sound + ev_det).

fn s0(x: String) -> String = x

fn p0(x: Int) -> Result[Int, String] = err("mike")

fn p1(x: Int) -> Result[Int, String] = ok(p0(x) ?? 2)

effect fn e0(x: Int) -> Int = {
  match ok(8) {
    ok(_v) => println("november"),
    err(_e) => println("lima"),
  }
  let me01: Result[Int, String] = p0(2)
  match me01 {
    ok(_v) => println("golf"),
    err(_e) => println("lima"),
  }
  0
}

effect fn main() -> Unit = {
  match p0(8) {
    ok(_v) => println("bravo"),
    err(_e) => println("kilo"),
  }
  let m1: Result[Int, String] = p1(6)
  match m1 {
    ok(_v) => println("kilo"),
    err(_e) => println("delta"),
  }
  let m2: Result[Int, String] = ok(3)
  match m2 {
    ok(_v) => println("golf"),
    err(_e) => println("kilo"),
  }
  match p1(0) {
    ok(_v) => println("charlie"),
    err(_e) => println("lima"),
  }
  println("papa")
  let m5: Result[Int, String] = e0(4)
  match m5 {
    ok(_v) => println("golf"),
    err(_e) => println("oscar"),
  }
  println(s0("lima"))
}
