// GENERATED by `lake exe conformancegen` (crates/almide-edit-belt) — DO NOT EDIT.
// The sibling .expected file is the compiled Lean evaluator's kernel trace,
// pinned to Corpus.lean by the conformancegen --check CI step; generator
// totality is kernel-checked (corpus_total, by rfl + eval_sound + ev_det).

fn s0(x: String) -> String = x

fn p0(x: Int) -> Result[Int, String] = err("bravo")

fn p1(x: Int) -> Result[Int, String] = ok(1)

fn p2(x: Int) -> Result[Int, String] = ok(x)

effect fn e0(x: Int) -> Int = {
  match p1(x) {
    ok(_v) => println("echo"),
    err(_e) => println("mike"),
  }
  match ok(9) {
    ok(_v) => println("golf"),
    err(_e) => println("papa"),
  }
  8
}

effect fn main() -> Unit = {
  let m0: Result[Int, String] = p2(1)
  match m0 {
    ok(_v) => println("delta"),
    err(_e) => println("bravo"),
  }
  let m1: Result[Int, String] = ok(4)
  match m1 {
    ok(_v) => println("papa"),
    err(_e) => println("papa"),
  }
  println("golf")
  println("kilo")
  let m4: Result[Int, String] = p2(4)
  match m4 {
    ok(_v) => println("papa"),
    err(_e) => println("india"),
  }
  match p2(2) {
    ok(_v) => println("bravo"),
    err(_e) => println("oscar"),
  }
}
