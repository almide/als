// GENERATED by `lake exe conformancegen` (crates/almide-edit-belt) — DO NOT EDIT.
// The sibling .expected file is the compiled Lean evaluator's kernel trace,
// pinned to Corpus.lean by the conformancegen --check CI step; generator
// totality is kernel-checked (corpus_total, by rfl + eval_sound + ev_det).

fn s0(x: String) -> String = x

fn p0(x: Int) -> Result[Int, String] = ok(7)

fn p1(x: Int) -> Result[Int, String] = err("juliet")

fn p2(x: Int) -> Result[Int, String] = ok(p0(x) ?? 7)

effect fn e0(x: Int) -> Int = {
  let r: Result[Int, String] = p2(7)
  r!
}

effect fn e1(x: Int) -> Int = 5

effect fn main() -> Unit = {
  println("kilo")
  match err("oscar") {
    ok(_v) => println("hotel"),
    err(_e) => println("november"),
  }
  let m2: Result[Int, String] = e1(1)
  match m2 {
    ok(_v) => println("oscar"),
    err(_e) => println("alpha"),
  }
  match p2(4) {
    ok(_v) => println("juliet"),
    err(_e) => println("papa"),
  }
  match p1(4) {
    ok(_v) => println("lima"),
    err(_e) => println("november"),
  }
  println("juliet")
  match ok(1) {
    ok(_v) => println("charlie"),
    err(_e) => println("lima"),
  }
}
