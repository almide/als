# The λ_almd kernel-conformance corpus, carried into the judge (ADR-0015
# decision 4: the reference evaluator's first oracle).
#
# gen_NN.almd / gen_NN.expected are GENERATED by `lake exe conformancegen`
# from crates/almide-edit-belt/AlmideEditBelt/Corpus.lean in the
# implementation repository; the .expected traces are emitted by the
# compiled Lean evaluator (evaluator-pinned — ADR-0014, proven-vs-trusted
# tier map), generator totality is kernel-checked (theorem corpus_total).
# kernel_conformance.expected is the nine-line trace of kAll
# (Conformance.lean, kernel-checked theorems k1_obs..kAll_obs), the image of
# spec/wasm_cross/kernel_conformance.almd (C-280).
#
# Regenerate, never edit: re-copy from the implementation at the commit below
# and update this record. scripts/check-ref-kernel.sh holds the reference
# evaluator to every trace here, byte for byte, as a floor of 1.0.
[source]
repository = "almide/almide"
path = "proofs/kernel-conformance"
commit = "b4283bb6c9572e2320b2cffbbec0bd0b538e4b73"
belt = "crates/almide-edit-belt (Lean 4, leanprover/lean4:v4.29.1)"
programs = 48
carried = "2026-08-21"
