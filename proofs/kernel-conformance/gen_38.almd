// GENERATED by `lake exe conformancegen` (crates/almide-edit-belt) — DO NOT EDIT.
// The sibling .expected file is the compiled Lean evaluator's kernel trace,
// pinned to Corpus.lean by the conformancegen --check CI step; generator
// totality is kernel-checked (corpus_total, by rfl + eval_sound + ev_det).

fn s0(x: String) -> String = x

fn p0(x: Int) -> Result[Int, String] = err("juliet")

fn p1(x: Int) -> Result[Int, String] = ok(x)

effect fn e0(x: Int) -> Int = {
  match p1(1) {
    ok(_v) => println("delta"),
    err(_e) => println("delta"),
  }
  match p0(x) {
    ok(_v) => println("charlie"),
    err(_e) => println("juliet"),
  }
  x
}

effect fn e1(x: Int) -> Int = {
  let r: Result[Int, String] = ok(3)
  r!
}

effect fn main() -> Unit = {
  match err("november") {
    ok(_v) => println("juliet"),
    err(_e) => println("golf"),
  }
  println(s0("mike"))
  let m2: Result[Int, String] = e0(5)
  match m2 {
    ok(_v) => println("foxtrot"),
    err(_e) => println("oscar"),
  }
  let m3: Result[Int, String] = ok(7)
  match m3 {
    ok(_v) => println("golf"),
    err(_e) => println("echo"),
  }
  match err("kilo") {
    ok(_v) => println("papa"),
    err(_e) => println("november"),
  }
  println("kilo")
  let m6: Result[Int, String] = e1(4)
  match m6 {
    ok(_v) => println("foxtrot"),
    err(_e) => println("bravo"),
  }
}
