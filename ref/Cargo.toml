[package]
name = "als-ref"
version = "0.1.0"
edition = "2021"
publish = false
description = "The ALS reference evaluator — a fresh, source-level, judge-owned evaluator of Almide programs (ADR-0015). No dependency on any almide-* crate."
license = "MIT"

[[bin]]
name = "als-ref"
path = "src/main.rs"

[dependencies]

[profile.release]
debug = false
