// E032: mutating a FIELD of an immutable record binding through a `mut`
// parameter — the root binding must be `var`.
type Box = { items: List[Int] }

fn main() -> Unit = {
  let b = Box { items: [1] }
  list.push(b.items, 2)
  println(int.to_string(list.len(b.items)))
}
