// E037: an Option on one side of == against its bare payload, raised from a
// match GUARD position.
fn find() -> Option[Int] = some(1)

fn main() -> Unit = {
  println(match 0 { _ if find() == 1 => "hit", _ => "miss" })
}
