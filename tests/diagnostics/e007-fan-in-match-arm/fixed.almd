fn sum2(t: (Int, Int)) -> Int = match t { (a, b) => a + b }

effect fn pick(n: Int) -> Int = match n {
  1 => sum2(fan { 4
    5 }),
  _ => 0,
}

effect fn main() -> Unit = println(int.to_string(pick(1)!))
