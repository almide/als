// ALS-B4: list.push inside a counted loop allocates per iteration.
@bounded
fn collect() -> Int = {
  var xs: List[Int] = []
  for i in 0..<4 { list.push(xs, i) }
  list.len(xs)
}

fn main() -> Unit = println("${collect()}")
