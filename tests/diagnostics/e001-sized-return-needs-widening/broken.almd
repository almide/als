// A sized UInt8 VALUE returned into a canonical `-> Int` slot needs an
// explicit widening call; only literals adopt a width silently.
fn widen(u: UInt8) -> Int = u

fn main() -> Unit = println("${widen(int.to_uint8(5))}")
