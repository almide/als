// Enum brace construction directly in ARGUMENT position.
type Shape = | Circle(Int) | Dot

fn area(s: Shape) -> Int = match s { Circle(r) => r * r, Dot => 0 }

fn main() -> Unit = println(int.to_string(area(Shape { r: 3 })))
