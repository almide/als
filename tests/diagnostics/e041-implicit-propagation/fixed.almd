// E041: implicit propagation (auto-?) was REMOVED — ADR-0008 makes `!` the
// only propagation spelling; this value stays a Result without it.
effect fn get() -> Int = 42

effect fn main() -> Unit = {
  let r = int.parse("7")!
  println("${r + get()!}")
}
