// `if let` binds the inner value of an Option/Result scrutinee, so a plain
// Int scrutinee has nothing to unwrap.
fn main() -> Unit = {
  let n = list.first([5, 6])
  if let v = n { println("${v}") } else { println("none") }
}
