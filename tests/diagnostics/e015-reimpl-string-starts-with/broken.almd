// A two-argument String predicate mirroring stdlib string.starts_with.
fn starts_with(s: String, prefix: String) -> Bool = false

fn main() -> Unit = println(if starts_with("ab", "a") then "y" else "n")
