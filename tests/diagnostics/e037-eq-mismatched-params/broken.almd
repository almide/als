// E037: == on the same container with different type parameters can never
// compare — this used to pass check and die as rustc E0308 at codegen.
fn main() -> Unit = {
  let a: Option[Int] = some(1)
  let b: Option[String] = some("x")
  println("${a == b}")
}
