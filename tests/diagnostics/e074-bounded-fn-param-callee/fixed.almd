// ALS-B7: calling a fn-typed PARAMETER is a call the static call graph cannot
// follow — only @bounded named functions and first-order pure stdlib qualify.
@bounded
fn double(x: Int) -> Int = x * 2

@bounded
fn apply(x: Int) -> Int = double(x)

fn main() -> Unit = println("${apply(3)}")
