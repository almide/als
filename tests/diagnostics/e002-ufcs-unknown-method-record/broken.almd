// #1521: an unknown method on a record used to E001 "expected P but got
// fn() -> Int" — the object was never a function; the method does not exist.
type P = { x: Int }

fn main() -> Unit = {
  let p = P { x: 1 }
  println(int.to_string(p.zzcompute()))
}
