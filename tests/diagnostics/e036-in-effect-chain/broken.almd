// E036: a NAMED error param the mapper then ignores, mid effect chain.
effect fn step(n: Int) -> Result[Int, String] = ok(n + 1)

effect fn main() -> Unit = {
  let bad = result.map_err(step(2), (oops) => "generic")!
  println(int.to_string(bad))
}
