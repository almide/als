// A record value is one aggregate, not a sequence — its fields are read by
// name, not by looping over the value.
type Point = { x: Int, y: Int }

fn main() -> Unit = {
  let p = Point { x: 1, y: 2 }
  for q in p {
    println("${q}")
  }
}
