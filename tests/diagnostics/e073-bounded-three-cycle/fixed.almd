// ALS-B5: the cycle unrolled into one counted loop.
@bounded
fn a(n: Int) -> Int = {
  var left = 0
  for i in 0..<100 {
    if i < n then left = left + 1
  }
  left
}

fn main() -> Unit = println("${a(3)}")
