// The survivor spelling: accessors live on value.*, and value.as_int returns
// a Result — `?` restores the Option the retired alias produced.
import json

fn main() -> Unit = {
  let n = value.as_int(json.parse("7") ?? value.null())?
  println(int.to_string(n ?? 0))
}
