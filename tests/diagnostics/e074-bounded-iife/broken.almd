// ALS-B7: an immediately-invoked lambda is doubly inadmissible — the call is
// indirect and the lambda creates a closure.
@bounded
fn shifted(n: Int) -> Int = ((x: Int) => x + 1)(n)

fn main() -> Unit = println("${shifted(4)}")
