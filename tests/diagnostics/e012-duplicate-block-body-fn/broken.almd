// Duplicates with BLOCK bodies (the registration walks decl heads, not
// body shapes).
fn work() -> Int = {
  let a = 1
  a + 1
}

fn work() -> Int = {
  let b = 2
  b + 2
}

fn main() -> Unit = println(int.to_string(work()))
