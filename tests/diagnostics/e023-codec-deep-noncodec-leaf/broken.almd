// The leaf-nominal rule reaches through TWO container levels: a non-Codec
// record buried in List[List[R]] is still named at the declaration.

type R = { n: Int }

type T: Codec = { rs: List[List[R]] }

effect fn main() -> Unit = println("x")
