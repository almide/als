// E027: the thunk-list fan.settle spelling was removed in Wave 1.
effect fn a() -> Result[Int, String] = ok(1)

effect fn main() -> Unit = {
  let rs = fan.settle([() => a()])
  println(int.to_string(list.len(rs)))
}
