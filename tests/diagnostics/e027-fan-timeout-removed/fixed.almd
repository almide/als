// fan.timeout returned (T5-1) as the oracle-tier block head: a WALL-CLOCK
// deadline (Duration) checked cooperatively at charge sites.
fn work() -> Int = 42

effect fn main() -> Unit = {
  let r = fan.timeout(duration.ms(5000)) { work() } ?? -1
  println(int.to_string(r))
}
