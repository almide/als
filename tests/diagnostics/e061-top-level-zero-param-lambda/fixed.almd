// A module-level let bound to a zero-parameter lambda still has no home in
// the compiled output — the arity does not matter.
fn answer() -> Int = 42

fn main() -> Unit = println(int.to_string(answer()))
