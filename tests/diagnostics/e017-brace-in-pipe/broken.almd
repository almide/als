// Enum brace construction feeding a PIPE.
type Shape = | Circle(Int) | Dot

fn area(s: Shape) -> Int = match s { Circle(r) => r * r, Dot => 0 }

fn main() -> Unit = println(int.to_string(Shape { r: 5 } |> area))
