// Ordering a LIST operand with `>` in a let binding — lists have no operator
// ordering; compare an explicit scalar such as the lengths.
fn main() -> Unit = {
  let a = [3, 1]
  let b = [2]
  let bigger = list.len(a) > list.len(b)
  println(if bigger then "a" else "b")
}
