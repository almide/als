// The typo'd name sits next to several candidates; the suggestion must
// pick the closest, not the first.
type Rgb = { red: Int, green: Int, blue: Int }

fn g(c: Rgb) -> Int = c.gren

fn main() -> Unit = println(int.to_string(g(Rgb(red: 1, green: 2, blue: 3))))
