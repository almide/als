// #1489 pin: the BRACELESS block's recovery point — same discipline as the
// braced one; the later type error must still surface.
fn g(x: Int) -> Int = if x > 0 then {
  let a = x!!
  let b: Int = "nope"
  b
} else 0

fn main() -> Unit = println(int.to_string(g(1)))
