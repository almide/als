// E037: == on the same container head (List) with different element types —
// List[Int] can never equal List[String].
fn main() -> Unit = {
  let a: List[Int] = [1, 2]
  let b: List[String] = ["1", "2"]
  if a == b then println("eq") else println("ne")
}
