// Use fan.any when you meant "the first candidate that SUCCEEDS, in list order".
effect fn a() -> Result[String, String] = ok("a")

effect fn b() -> Result[String, String] = ok("b")

effect fn main() -> Unit = {
  let w = fan.any {
    a()
    b()
  }
  println(w)
}
