effect fn fetch() -> Result[Int, String] = err("boom")

effect fn main() -> Unit = {
  match fetch() {
    ok(n) => println(int.to_string(n)),
    err(e) => println(if e == "x" and 1 > 0 then "a" else "b"),
  }
}
