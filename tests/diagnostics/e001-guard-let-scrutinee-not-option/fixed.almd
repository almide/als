// `guard let` unwraps an Option/Result and exits through the else branch on
// the empty side; a plain Int scrutinee has no inner value to bind.
fn label(xs: List[Int]) -> String = {
  guard let v = list.first(xs) else { "no" }
  "got ${v}"
}

fn main() -> Unit = println(label([3]))
