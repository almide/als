// E036: an UNDERSCORE-PREFIXED name (`_e`) still counts as a named-and-unused
// error param — only the bare `(_)` is the sanctioned discard.
effect fn main() -> Unit = {
  let n = int.parse("7") |> result.map_err((_e) => "bad")!
  println("${n}")
}
