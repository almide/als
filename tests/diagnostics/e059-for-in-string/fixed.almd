// A String is not a sequence in a `for` head — character iteration goes
// through string.chars, which materializes the List the loop can walk.
fn main() -> Unit = {
  let s = "abc"
  for c in string.chars(s) {
    println(c)
  }
}
