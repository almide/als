// Positional args to a record case, the value then PIPED.
type Ev = | Click { x: Int, y: Int } | Quit

fn tag(e: Ev) -> String = match e { Click { x: _, y: _ } => "c", Quit => "q" }

fn main() -> Unit = println(Click(3, 4) |> tag)
