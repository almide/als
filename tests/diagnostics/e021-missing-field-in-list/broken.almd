// A record-case construction MISSING a field, inside a list literal.
type Ev = | Click { x: Int, y: Int } | Quit

fn main() -> Unit = {
  let evs = [Click { x: 1 }, Quit]
  println(int.to_string(list.len(evs)))
}
